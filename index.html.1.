<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="VéloID">
<meta name="theme-color" content="#0d0d0d">
<meta name="description" content="Application atelier vélo — diagnostic, standards, rayonnage, braquet · v4.1">
<link rel="manifest" href="manifest.json">
<link rel="apple-touch-icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' rx='20' fill='%230d0d0d'/><text y='72' x='50' text-anchor='middle' font-size='60' font-family='sans-serif' font-weight='bold' fill='%23e8400c'>V</text></svg>">
<title>VéloID</title>
<script>
// Init anticipé — exécuté avant le DOM pour éviter le flash de thème
var APP_LANG = localStorage.getItem('veloid_lang') || 'fr';
var APP_THEME = localStorage.getItem('veloid_theme') || 'dark';
// Fonctions stub disponibles dès le premier script principal
function applyTheme(){
  var isLight = APP_THEME==='light';
  document.body && document.body.classList.toggle('light-mode', isLight);
  var t = document.getElementById('themeToggle');
  if(t) t.classList.toggle('on', isLight);
}
function setLang(lang){
  APP_LANG = lang;
  try{localStorage.setItem('veloid_lang',lang);}catch(e){}
  ['FR','EN'].forEach(function(l){
    var isActive=(l===lang.toUpperCase());
    ['lang'+l,'settingsLang'+l].forEach(function(id){
      var b=document.getElementById(id);
      if(b) b.classList.toggle('active',isActive);
    });
  });
  var homeTag=document.getElementById('homeTag');
  if(homeTag) homeTag.textContent=lang==='en'?'Identify your workshop standards':'Identifiez vos standards en atelier';
  document.querySelectorAll('[data-i18n]').forEach(function(el){
    var key=el.dataset.i18n;
    if(!key) return;
    var map={
      'Couples de Serrage':'Torque Reference',
      'Shimano · SRAM · Campagnolo · Hope · Fox · RockShox':'Shimano · SRAM · Campagnolo · Hope · Fox · RockShox',
      'Freins · transmission · cadre · suspensions':'Brakes · drivetrain · frame · suspension',
      'Compatibilité Chaîne/Cassette':'Chain/Cassette Compat.',
      'Références · alertes croisées · largeurs':'References · cross-alerts · widths',
      'Shimano · SRAM · Campagnolo · KMC':'Shimano · SRAM · Campagnolo · KMC',
      'Diagnostic Vélo':'Bike Diagnostic',
      'Méthode en M · 13 points · rapport complet':'M-method · 13 points · full report',
      'Score général · export rapport':'Overall score · export report',
      'Référence':'Reference',
      'Glossaire Technique':'Technical Glossary',
      '50+ termes · FR/EN · standards expliqués':'50+ terms · FR/EN · standards explained',
      'Lexique atelier · utile pour les apprentis':'Workshop lexicon · useful for trainees',
    };
    if(lang==='en' && map[key]) el.textContent=map[key];
    else el.textContent=key;
  });
}
function toggleTheme(){
  APP_THEME=APP_THEME==='dark'?'light':'dark';
  try{localStorage.setItem('veloid_theme',APP_THEME);}catch(e){}
  applyTheme();
}
// Appliquer le thème immédiatement (body pas encore dispo, sera réappliqué sur DOMContentLoaded)
document.addEventListener('DOMContentLoaded', function(){
  applyTheme();
  setLang(APP_LANG);
});
</script>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=Outfit:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
*{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
html,body{height:100%;overflow:hidden;position:fixed;width:100%}
body{background:#0d0d0d;color:#f0ede8;font-family:'Outfit',sans-serif}

:root{
  --bg:#0d0d0d;--surface:#161616;--card:#1e1e1e;--card2:#242424;
  --border:#2a2a2a;--border2:#333;
  --red:#e8400c;--red-dim:rgba(232,64,12,0.12);--red-glow:rgba(232,64,12,0.3);
  --blue:#2d7ff9;--green:#22c55e;--gold:#f59e0b;--purple:#a855f7;
  --text:#f0ede8;--text2:#9a9590;--muted:#5a5550;
  --sb:env(safe-area-inset-bottom,16px);--st:env(safe-area-inset-top,0px);
}

.screen{position:absolute;inset:0;display:none;flex-direction:column;overflow:hidden}
.screen.active{display:flex}

/* ── SPLASH ── */
#splash{background:#0d0d0d;align-items:center;justify-content:center;gap:12px}
.splash-logo{font-family:'Syne',sans-serif;font-size:52px;font-weight:800;letter-spacing:-0.03em;animation:blink 1.6s ease infinite}
.splash-logo em{color:var(--red);font-style:normal}
.splash-sub{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.2em;text-transform:uppercase}
@keyframes blink{0%,100%{opacity:1}50%{opacity:.5}}

/* ── HOME ── */
#home{background:var(--bg)}
.home-hdr{padding:calc(var(--st) + 18px) 18px 0;flex-shrink:0}
.home-logo{font-family:'Syne',sans-serif;font-size:30px;font-weight:800;letter-spacing:-.02em}
.home-logo em{color:var(--red);font-style:normal}
.home-tag{font-size:13px;color:var(--text2);margin-top:2px;font-weight:300}

.search-wrap{padding:14px 18px 0;flex-shrink:0}
.search-box{display:flex;align-items:center;gap:10px;background:var(--card);border:1px solid var(--border2);border-radius:12px;padding:11px 14px}
.search-box input{flex:1;background:none;border:none;outline:none;font-family:'Outfit',sans-serif;font-size:15px;color:var(--text)}
.search-box input::placeholder{color:var(--muted)}

.sec-lbl{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.15em;text-transform:uppercase;padding:18px 18px 10px;flex-shrink:0}
.sec-lbl-inner{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.15em;text-transform:uppercase;padding:14px 2px 10px;display:flex;align-items:center;gap:10px}
.sec-lbl-inner::before{content:'';flex:none;width:0px}
.sec-lbl-inner::after{content:'';flex:1;height:1px;background:var(--border2)}
.cards-scroll{flex:1;overflow-y:auto;padding:0 12px calc(var(--sb) + 16px);-webkit-overflow-scrolling:touch}

.diag-card{display:flex;align-items:center;gap:13px;background:var(--card);border:1px solid var(--border);border-radius:14px;padding:14px;margin-bottom:8px;cursor:pointer;position:relative;overflow:hidden;-webkit-user-select:none}
.diag-card:active{transform:scale(.98);background:var(--card2)}
.dc-icon{width:46px;height:46px;background:var(--card2);border-radius:12px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.dc-info{flex:1;min-width:0}
.dc-title{font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:var(--text)}
.dc-desc{font-size:12px;color:var(--text2);margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.dc-meta{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);margin-top:4px}
.dc-arr{color:var(--muted);font-size:18px;flex-shrink:0}

/* ── DIAG SCREEN ── */
#diag{background:var(--bg)}
.diag-hdr{background:var(--surface);border-bottom:1px solid var(--border);padding:calc(var(--st)+10px) 14px 10px;flex-shrink:0;display:flex;align-items:center;gap:10px}
.btn-back{width:36px;height:36px;background:var(--card);border:1px solid var(--border2);border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:20px;cursor:pointer;flex-shrink:0;-webkit-user-select:none}
.btn-back:active{background:var(--card2)}
.dh-info{flex:1;min-width:0}
.dh-title{font-family:'Syne',sans-serif;font-size:16px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.dh-step{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);margin-top:1px}

.prog-dots{display:flex;gap:5px;padding:10px 14px 0;flex-shrink:0}
.pdot{height:4px;border-radius:2px;background:var(--border2);transition:all .3s;flex:1}
.pdot.done{background:var(--green)}
.pdot.active{background:var(--red)}

.step-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:16px 16px 8px}

.step-lbl{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--red);letter-spacing:.15em;text-transform:uppercase;margin-bottom:5px}
.step-q{font-family:'Syne',sans-serif;font-size:21px;font-weight:700;line-height:1.2;margin-bottom:14px}

/* Guide card */
.guide-card{background:var(--card);border:1px solid var(--border2);border-radius:14px;overflow:hidden;margin-bottom:14px}
.guide-head{background:var(--card2);border-bottom:1px solid var(--border);padding:10px 14px;display:flex;align-items:center;gap:8px;cursor:pointer;-webkit-user-select:none}
.guide-head-title{font-size:13px;font-weight:600;color:var(--text2);flex:1}
.gtog{font-size:13px;color:var(--muted);transition:transform .2s}
.gtog.open{transform:rotate(180deg)}
.guide-body{display:none;padding:14px}
.guide-body.open{display:block}

/* SVG diagram zone */
.diag-svg-wrap{background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:14px;margin-bottom:12px;display:flex;justify-content:center;align-items:center}
.diag-svg-wrap svg{width:100%;max-width:280px;height:auto;display:block}

/* legend strip like DT Swiss */
.diag-legend{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px}
.legend-item{display:flex;align-items:center;gap:5px;font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--text2)}
.legend-dot{width:10px;height:10px;border-radius:2px;flex-shrink:0}
.ld-red{background:var(--red)}
.ld-blue{background:var(--blue)}
.ld-green{background:var(--green)}
.ld-gold{background:var(--gold)}

.guide-steps{display:flex;flex-direction:column;gap:9px}
.gstep{display:flex;gap:9px;align-items:flex-start}
.gstep-num{width:21px;height:21px;min-width:21px;background:var(--red);color:#fff;border-radius:50%;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:11px}
.gstep-text{font-size:13px;color:var(--text2);line-height:1.6}
.gstep-text strong{color:var(--text)}
.gstep-text code{font-family:'JetBrains Mono',monospace;font-size:12px;background:var(--card2);padding:1px 5px;border-radius:4px;color:var(--blue)}

.tip-bar{background:rgba(245,158,11,.1);border:1px solid rgba(245,158,11,.25);border-radius:10px;padding:10px 13px;font-size:12px;color:#d4920a;margin-top:8px;line-height:1.5}

/* Choices */
.choice-list{display:flex;flex-direction:column;gap:8px;margin-bottom:8px}
.choice-item{display:flex;align-items:center;gap:11px;background:var(--card);border:2px solid var(--border);border-radius:14px;padding:13px 14px;cursor:pointer;-webkit-user-select:none;transition:all .15s}
.choice-item:active{transform:scale(.98)}
.choice-item.selected{border-color:var(--red);background:var(--red-dim)}
.ci-icon{width:36px;height:36px;border-radius:10px;background:var(--card2);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.ci-icon svg{width:22px;height:22px}
.ci-body{flex:1;min-width:0}
.ci-val{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--red);margin-bottom:2px}
.ci-label{font-size:14px;color:var(--text2);line-height:1.3}
.ci-label strong{color:var(--text)}
.ci-check{width:22px;height:22px;border:2px solid var(--border2);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:12px;color:transparent;flex-shrink:0;transition:all .15s}
.choice-item.selected .ci-check{background:var(--red);border-color:var(--red);color:#fff}

/* Number input */
.num-wrap{background:var(--card);border:2px solid var(--border2);border-radius:14px;padding:14px;margin-bottom:8px;transition:border-color .15s}
.num-wrap:focus-within{border-color:var(--red)}
.num-lbl{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:7px}
.num-row{display:flex;align-items:center;gap:10px}
.num-inp{flex:1;background:none;border:none;outline:none;font-family:'JetBrains Mono',monospace;font-size:30px;font-weight:500;color:var(--text);width:100%}
.num-inp::placeholder{color:var(--muted)}
.num-unit{font-family:'JetBrains Mono',monospace;font-size:15px;color:var(--muted);flex-shrink:0}

.text-wrap{background:var(--card);border:2px solid var(--border2);border-radius:14px;padding:14px;margin-bottom:8px;transition:border-color .15s}
.text-wrap:focus-within{border-color:var(--red)}
.text-inp{background:none;border:none;outline:none;font-family:'JetBrains Mono',monospace;font-size:26px;color:var(--text);width:100%;text-transform:uppercase;letter-spacing:.05em}
.text-inp::placeholder{color:var(--muted);text-transform:none}

/* Footer nav */
.step-foot{padding:10px 14px calc(var(--sb)+10px);flex-shrink:0;background:var(--bg);border-top:1px solid var(--border);display:flex;gap:9px}
.btn-prev{width:48px;height:50px;background:var(--card);border:1px solid var(--border2);border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:20px;cursor:pointer;flex-shrink:0;-webkit-user-select:none}
.btn-prev:active{background:var(--card2)}
.btn-next{flex:1;height:50px;background:var(--red);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:17px;font-weight:800;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px;-webkit-user-select:none;letter-spacing:.02em}
.btn-next:active{background:#c23510}

/* ── RESULT ── */
#result{background:var(--bg)}
.res-hdr{background:var(--surface);border-bottom:1px solid var(--border);padding:calc(var(--st)+10px) 14px 10px;flex-shrink:0;display:flex;align-items:center;gap:10px}
.res-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:0 0 calc(var(--sb)+16px)}
.res-hero{background:linear-gradient(135deg,#1a0a04,#0d0d0d);border-bottom:1px solid var(--border);padding:20px 18px 22px;position:relative;overflow:hidden}
.res-hero::before{content:'';position:absolute;top:-40px;right:-40px;width:160px;height:160px;background:radial-gradient(circle,var(--red-glow),transparent 70%)}
.res-badge{display:inline-flex;align-items:center;gap:5px;background:var(--green);color:#fff;padding:3px 11px;border-radius:20px;font-family:'JetBrains Mono',monospace;font-size:11px;margin-bottom:9px}
.res-std{font-family:'Syne',sans-serif;font-size:26px;font-weight:800;line-height:1.1;position:relative}
.res-detail{font-size:13px;color:var(--text2);margin-top:5px;position:relative}
.res-sec{padding:14px 16px 0}
.rs-lbl{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.15em;text-transform:uppercase;margin-bottom:8px}
.rs-note{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:13px 15px;font-size:14px;color:var(--text2);line-height:1.7}
.rs-note strong{color:var(--text)}
.compat-list{display:flex;flex-direction:column;gap:7px}
.compat-item{display:flex;align-items:center;gap:10px;background:var(--card);border:1px solid var(--border);border-radius:12px;padding:11px 13px;font-size:13px;color:var(--text2)}
.cdot{width:8px;height:8px;min-width:8px;border-radius:50%}
.cdot-g{background:var(--green)}.cdot-o{background:var(--gold)}.cdot-r{background:var(--red)}.cdot-b{background:var(--blue)}
.warn-bar{background:rgba(232,64,12,.1);border:1px solid rgba(232,64,12,.3);border-radius:10px;padding:11px 14px;font-size:13px;color:#f87171;margin:14px 16px 0;line-height:1.5}
.res-foot{padding:14px 16px calc(var(--sb)+14px);display:flex;gap:9px;margin-top:6px}
.btn-restart{flex:1;height:50px;background:var(--card);border:1px solid var(--border2);border-radius:13px;font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:var(--text2);cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px;-webkit-user-select:none}
.btn-restart:active{background:var(--card2)}
.btn-home{flex:1;height:50px;background:var(--red);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px;-webkit-user-select:none}
.btn-home:active{background:#c23510}

/* ── RAYONNAGE SCREEN ── */
#rayonnage{background:var(--bg)}
.ray-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px calc(var(--sb) + 140px)}

.ray-section{margin-bottom:16px}
.ray-sec-title{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text2);margin-bottom:10px;display:flex;align-items:center;gap:8px}
.ray-sec-title::after{content:'';flex:1;height:1px;background:var(--border)}

.ray-field{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:12px 14px;margin-bottom:8px}
.ray-field-lbl{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:5px;display:flex;align-items:center;gap:6px}
.ray-field-lbl .info-btn{width:16px;height:16px;border-radius:50%;background:var(--border2);color:var(--text2);font-size:10px;display:flex;align-items:center;justify-content:center;cursor:pointer;font-family:'JetBrains Mono',monospace;flex-shrink:0}
.ray-inp{background:none;border:none;outline:none;font-family:'JetBrains Mono',monospace;font-size:22px;font-weight:500;color:var(--text);width:100%}
.ray-inp::placeholder{color:var(--muted)}
.ray-inp-row{display:flex;align-items:center;gap:8px}
.ray-unit{font-family:'JetBrains Mono',monospace;font-size:13px;color:var(--muted)}

.ray-select{background:var(--card2);border:1px solid var(--border2);border-radius:8px;padding:9px 12px;color:var(--text);font-family:'Outfit',sans-serif;font-size:14px;width:100%;outline:none;-webkit-appearance:none}

.ray-calc-btn{width:calc(100% - 28px);height:52px;background:var(--blue);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:17px;font-weight:800;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:8px;-webkit-user-select:none;position:fixed;bottom:calc(var(--sb) + 72px);left:14px;right:14px}
.ray-calc-btn:active{background:#1a5fd4}

.ray-result{background:var(--card);border:2px solid var(--blue);border-radius:14px;padding:16px;margin-top:14px;display:none}
.ray-result.show{display:block}
.ray-result-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--blue);margin-bottom:12px;letter-spacing:.05em;text-transform:uppercase}
.ray-lengths{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}
.ray-len-card{background:var(--card2);border-radius:10px;padding:12px}
.ray-len-label{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:4px}
.ray-len-val{font-family:'JetBrains Mono',monospace;font-size:24px;font-weight:500;color:var(--text)}
.ray-len-unit{font-size:12px;color:var(--muted)}
.ray-len-sub{font-size:11px;color:var(--text2);margin-top:2px}

.ray-detail-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-top:8px}
.ray-dg-item{background:var(--surface);border-radius:8px;padding:9px 10px}
.ray-dg-label{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.1em;margin-bottom:2px}
.ray-dg-val{font-family:'JetBrains Mono',monospace;font-size:13px;color:var(--text)}

.ray-schema-wrap{background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:12px;margin-bottom:14px}
.ray-schema-title{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:10px}

/* info-btn inline */
.info-btn{display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px;border-radius:50%;background:var(--border2);color:var(--text2);font-size:11px;font-family:'JetBrains Mono',monospace;font-weight:500;cursor:pointer;border:none;flex-shrink:0;vertical-align:middle;-webkit-user-select:none;transition:background .15s}
.info-btn:active{background:var(--blue)}

/* Info bottom sheet */
.info-overlay{position:fixed;inset:0;background:rgba(0,0,0,.6);z-index:500;display:none;align-items:flex-end}
.info-overlay.show{display:flex;animation:fadeIn .2s ease}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
.info-sheet{background:var(--card);border-radius:20px 20px 0 0;width:100%;max-height:88vh;overflow-y:auto;padding:0 0 calc(var(--sb)+16px);-webkit-overflow-scrolling:touch;animation:slideUp .25s ease}
@keyframes slideUp{from{transform:translateY(40px);opacity:0}to{transform:translateY(0);opacity:1}}
.info-sheet-handle{width:36px;height:4px;background:var(--border2);border-radius:2px;margin:12px auto 0}
.info-sheet-title{font-family:'Syne',sans-serif;font-size:18px;font-weight:800;padding:14px 18px 10px;color:var(--text)}
.info-sheet-svg{padding:0 16px 12px}
.info-sheet-svg svg{width:100%;height:auto;display:block;border-radius:10px;background:var(--surface)}
.info-sheet-body{padding:0 18px;font-size:14px;color:var(--text2);line-height:1.75}
.info-sheet-body strong{color:var(--text)}
.info-sheet-body .step-row{display:flex;gap:10px;align-items:flex-start;margin-bottom:10px}
.info-sheet-body .step-num{width:22px;height:22px;min-width:22px;background:var(--blue);color:#fff;border-radius:50%;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:11px;margin-top:1px}
.info-close-btn{width:calc(100% - 32px);height:46px;background:var(--card2);border:1px solid var(--border2);border-radius:12px;color:var(--text2);font-family:'Outfit',sans-serif;font-size:15px;cursor:pointer;margin:14px 16px 0;display:block}
/* ── SHIS CALCULATOR ── */
#shis{background:var(--bg)}
.sh-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 100px}

/* Intro banner */
.sh-banner{background:rgba(45,127,249,.09);border:1px solid rgba(45,127,249,.22);border-radius:14px;padding:14px 15px;margin-bottom:14px}
.sh-banner-title{font-family:'Syne',sans-serif;font-size:15px;font-weight:800;color:var(--text);margin-bottom:6px}
.sh-banner-body{font-size:12px;color:var(--text2);line-height:1.6}

/* Code preview live */
.sh-live{background:var(--card2);border-radius:12px;padding:12px 14px;margin-bottom:14px;display:flex;align-items:center;justify-content:space-between}
.sh-live-label{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.15em;text-transform:uppercase}
.sh-live-code{font-family:'Syne',sans-serif;font-size:17px;font-weight:800;color:var(--text);letter-spacing:.04em}

/* Section bloc */
.sh-bloc{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:14px;margin-bottom:12px}
.sh-bloc-hdr{display:flex;align-items:center;gap:10px;margin-bottom:12px}
.sh-bloc-icon{width:32px;height:32px;border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;font-size:14px;font-weight:800;font-family:'Syne',sans-serif}
.sh-bloc-icon.top{background:rgba(232,64,12,.18);color:var(--red)}
.sh-bloc-icon.bot{background:rgba(45,127,249,.18);color:var(--blue)}
.sh-bloc-title{flex:1}
.sh-bloc-name{font-family:'Syne',sans-serif;font-size:14px;font-weight:800;color:var(--text)}
.sh-bloc-sub{font-size:11px;color:var(--muted);margin-top:1px}

/* Schema SVG box — VISIBLE directement dans la page */
.sh-schema{background:var(--surface);border:1px solid var(--border2);border-radius:10px;padding:12px;margin-bottom:12px;display:flex;flex-direction:column;align-items:center;gap:8px}
.sh-schema svg{width:100%;height:auto;display:block}
.sh-schema-caption{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.08em;text-align:center;text-transform:uppercase}

/* Row label */
.sh-row-lbl{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:8px}

/* Type buttons EC / ZS / IS */
.sh-type-row{display:flex;gap:8px;margin-bottom:12px}
.sh-type-btn{flex:1;background:var(--card2);border:2px solid var(--border2);border-radius:12px;padding:10px 6px 8px;cursor:pointer;-webkit-user-select:none;transition:all .15s;display:flex;flex-direction:column;align-items:center;gap:3px}
.sh-type-btn.sel-top{border-color:var(--red);background:var(--red-dim)}
.sh-type-btn.sel-bot{border-color:var(--blue);background:rgba(45,127,249,.1)}
.sh-type-btn:active{transform:scale(.97)}
.sh-type-code{font-family:'Syne',sans-serif;font-size:20px;font-weight:800;color:var(--text)}
.sh-type-btn.sel-top .sh-type-code{color:var(--red)}
.sh-type-btn.sel-bot .sh-type-code{color:var(--blue)}
.sh-type-fr{font-size:11px;color:var(--text2);font-weight:600;text-align:center;line-height:1.2}
.sh-type-en{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);text-align:center}

/* Value buttons (diamètres et pivots) */
.sh-vals-grid{display:grid;gap:7px;margin-bottom:8px}
.sh-vals-grid.cols2{grid-template-columns:1fr 1fr}
.sh-vals-grid.cols3{grid-template-columns:1fr 1fr 1fr}
.sh-val-btn{background:var(--card2);border:2px solid var(--border2);border-radius:10px;padding:10px 8px 8px;cursor:pointer;-webkit-user-select:none;transition:all .15s;display:flex;flex-direction:column;align-items:flex-start;gap:2px}
.sh-val-btn:active{transform:scale(.97)}
.sh-val-btn.sel-top{border-color:var(--gold);background:rgba(245,158,11,.1)}
.sh-val-btn.sel-bot{border-color:var(--gold);background:rgba(245,158,11,.1)}
.sh-val-num{font-family:'JetBrains Mono',monospace;font-size:19px;font-weight:500;color:var(--text);line-height:1}
.sh-val-btn.sel-top .sh-val-num,.sh-val-btn.sel-bot .sh-val-num{color:var(--gold)}
.sh-val-mm{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted)}
.sh-val-desc{font-size:10px;color:var(--text2);margin-top:2px;line-height:1.2}
.sh-val-popular{display:inline-block;font-family:'JetBrains Mono',monospace;font-size:8px;color:var(--green);background:rgba(34,197,94,.12);padding:1px 5px;border-radius:3px;margin-top:2px}

/* Hint text */
.sh-hint{font-size:12px;color:var(--muted);font-style:italic;padding:4px 0 8px}

/* Result */
.sh-result{background:linear-gradient(135deg,#0c1624,#0d0d0d);border:2px solid var(--blue);border-radius:16px;padding:18px;margin-top:4px;display:none}
.sh-result.show{display:block}
.sh-result-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--green);letter-spacing:.15em;text-transform:uppercase;margin-bottom:8px}
.sh-result-code{font-family:'Syne',sans-serif;font-size:26px;font-weight:800;color:#fff;margin-bottom:14px;letter-spacing:.04em;line-height:1.1}
.sh-result-halves{display:flex;gap:8px;margin-bottom:12px}
.sh-half{flex:1;background:rgba(255,255,255,.05);border-radius:10px;padding:10px 11px}
.sh-half-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:4px}
.sh-half-code{font-family:'Syne',sans-serif;font-size:15px;font-weight:800;margin-bottom:3px}
.sh-half-def{font-size:11px;color:var(--text2);line-height:1.4}
.sh-diag{font-size:13px;color:var(--text2);line-height:1.6;padding:10px 12px;background:rgba(255,255,255,.04);border-radius:9px;border-left:3px solid var(--blue);margin-bottom:12px}
.sh-diag strong{color:var(--text)}
.sh-btns{display:flex;gap:8px;margin-bottom:12px}
.sh-copy{flex:1;height:46px;background:var(--blue);border:none;border-radius:11px;font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:#fff;cursor:pointer;-webkit-user-select:none}
.sh-copy:active{background:#1a5fd4}
.sh-copy.ok{background:var(--green)}
.sh-reset{height:46px;padding:0 16px;background:var(--card2);border:1px solid var(--border2);border-radius:11px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text2);cursor:pointer;-webkit-user-select:none}
.sh-alert{background:rgba(232,64,12,.07);border:1px solid rgba(232,64,12,.2);border-radius:10px;padding:11px 13px;font-size:12px;color:#f87171;line-height:1.6}
.sh-alert strong{color:#fca5a5}
.sh-diff-note{margin-top:8px;padding:8px 10px;background:rgba(245,158,11,.07);border-radius:8px;font-size:12px;color:#f59e0b}

/* ── BRAQUET & DÉVELOPPEMENT ── */
#braquet{background:var(--bg)}
.braq-deux-cols{display:grid;grid-template-columns:1fr 1fr;gap:8px}
.braq-deux-cols .ray-field{margin-bottom:0}
.braq-circ-display{display:flex;align-items:center;gap:6px;padding:8px 12px;background:rgba(168,85,247,.08);border-radius:8px;margin-top:8px;font-family:'JetBrains Mono',monospace}
.braq-circ-label{font-size:10px;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;flex:1}
.braq-circ-val{font-size:16px;font-weight:600;color:var(--purple)}
.braq-circ-unit{font-size:11px;color:var(--muted)}
.braq-presets-row{display:flex;flex-wrap:wrap;gap:6px;padding:6px 0 2px}
.braq-preset-tag{background:var(--card2);border:1px solid var(--border2);border-radius:6px;padding:5px 10px;font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--text2);cursor:pointer;-webkit-user-select:none;transition:all .15s}
.braq-preset-tag:active{background:rgba(168,85,247,.15);border-color:var(--purple);color:var(--purple)}
.braq-text-inp{width:100%;background:none;border:none;outline:none;font-family:'JetBrains Mono',monospace;font-size:20px;color:var(--text);margin-top:6px;padding:0}
.braq-text-inp::placeholder{color:var(--muted);font-size:14px}
.braq-cad-row{display:grid;grid-template-columns:repeat(4,1fr);gap:7px}
.braq-cad-btn{background:var(--card2);border:2px solid var(--border2);border-radius:10px;padding:9px 4px;cursor:pointer;-webkit-user-select:none;transition:all .15s;display:flex;flex-direction:column;align-items:center;gap:1px;font-family:'JetBrains Mono',monospace;font-size:17px;font-weight:500;color:var(--text)}
.braq-cad-btn span{font-size:9px;color:var(--muted);font-family:'JetBrains Mono',monospace}
.braq-cad-btn small{font-size:9px;color:var(--muted);font-family:'Outfit',sans-serif}
.braq-cad-btn.active{border-color:var(--purple);background:rgba(168,85,247,.12);color:var(--purple)}
.braq-cad-btn.active span,.braq-cad-btn.active small{color:var(--purple)}
.braq-cad-btn:active{transform:scale(.97)}
.braq-result{background:var(--card);border:2px solid var(--purple);border-radius:16px;padding:16px;margin-top:4px}
.braq-res-block{margin-bottom:16px}
.braq-res-title{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--purple);letter-spacing:.15em;text-transform:uppercase;margin-bottom:10px}
.braq-kpi-row{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}
.braq-kpi{background:var(--card2);border-radius:9px;padding:10px 8px;text-align:center}
.braq-kpi-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;margin-bottom:3px}
.braq-kpi-val{font-family:'JetBrains Mono',monospace;font-size:17px;font-weight:600;color:var(--purple)}
.braq-kpi-unit{font-size:9px;color:var(--muted);font-family:'JetBrains Mono',monospace}
.braq-dev-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:10px}
.braq-dev-card{background:var(--card2);border-radius:10px;padding:12px 11px}
.braq-dev-label{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:4px}
.braq-dev-val{font-family:'JetBrains Mono',monospace;font-size:22px;font-weight:600;color:var(--text)}
.braq-dev-unit{font-size:10px;color:var(--muted);font-family:'JetBrains Mono',monospace;margin-top:1px}
.braq-dev-sub{font-size:11px;color:var(--text2);margin-top:3px}
.braq-plage-bar{background:var(--card2);border-radius:10px;padding:12px}
.braq-plage-row{display:flex;justify-content:space-between;align-items:center;margin-bottom:4px}
.braq-plage-lbl{font-size:12px;color:var(--text2)}
.braq-plage-val{font-family:'Syne',sans-serif;font-size:16px;font-weight:800;color:var(--text)}
.braq-gauge-wrap{height:6px;background:var(--border2);border-radius:3px;margin:8px 0 6px;overflow:hidden}
.braq-gauge-bar{height:100%;border-radius:3px;transition:width .4s,background .4s}
.braq-plage-hint{font-size:11px;color:var(--muted);text-align:center}
.braq-speed-row{display:flex;align-items:center;justify-content:space-between;background:var(--card2);border-radius:9px;padding:10px 13px;margin-bottom:6px}
.braq-speed-lbl{font-size:13px;color:var(--text2)}
.braq-speed-val{font-family:'Syne',sans-serif;font-size:18px;font-weight:800;color:var(--text)}
.braq-speed-val.fast{color:var(--green)}.braq-speed-val.slow{color:var(--gold)}
.braq-speed-unit{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--muted)}
.braq-table{border-collapse:collapse;width:100%;font-family:'JetBrains Mono',monospace;font-size:11px}
.braq-table th{background:var(--card2);color:var(--muted);font-size:9px;padding:6px 7px;text-align:center;border:1px solid var(--border);letter-spacing:.05em}
.braq-table th.pl-hdr{color:var(--purple);background:rgba(168,85,247,.1)}
.braq-table td{padding:5px 7px;text-align:center;border:1px solid var(--border);color:var(--text2);font-size:11px}
.braq-table td.best{background:rgba(34,197,94,.12);color:var(--green);font-weight:600}
.braq-table td.worst{background:rgba(245,158,11,.1);color:var(--gold)}
.braq-ecarts-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:5px}
.braq-ecart-item{background:var(--card2);border-radius:8px;padding:8px 6px;text-align:center}
.braq-ecart-combo{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);margin-bottom:3px}
.braq-ecart-val{font-family:'JetBrains Mono',monospace;font-size:13px;font-weight:600}
.braq-reset-full{width:100%;height:44px;background:var(--card2);border:1px solid var(--border2);border-radius:11px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text2);cursor:pointer;margin-top:12px;-webkit-user-select:none}
.braq-reset-full:active{background:var(--card)}
/* ══ DIAGNOSTIC VÉLO ══ */
#diagvelo{background:var(--bg)}
.dv-export-btn{height:30px;padding:0 12px;background:rgba(34,197,94,.15);border:1px solid rgba(34,197,94,.3);border-radius:8px;font-family:'JetBrains Mono',monospace;font-size:10px;color:#22c55e;cursor:pointer;flex-shrink:0;letter-spacing:.04em}
.dv-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 30px}

/* Score global */
.dv-score-card{background:linear-gradient(135deg,#0d1f14,#0d0d0d);border:1px solid rgba(34,197,94,.25);border-radius:14px;padding:14px 16px;display:flex;align-items:center;gap:14px;margin-bottom:10px}
.dv-score-left{flex-shrink:0;text-align:center}
.dv-score-num{font-family:'Syne',sans-serif;font-size:42px;font-weight:800;color:#22c55e;line-height:1}
.dv-score-label{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-top:2px}
.dv-score-stats{display:grid;grid-template-columns:repeat(5,1fr);gap:4px;flex:1}
.dv-stat{background:var(--card2);border-radius:8px;padding:7px 4px;text-align:center;display:flex;flex-direction:column;gap:2px}
.dv-stat-val{font-family:'Syne',sans-serif;font-size:18px;font-weight:800;color:var(--text)}
.dv-stat-lbl{font-size:8px;color:var(--muted);font-family:'JetBrains Mono',monospace;letter-spacing:.04em}
.dv-yellow{color:#eab308}.dv-orange{color:#f97316}.dv-red{color:#ef4444}.dv-purple{color:#a855f7}

/* Barre progression */
.dv-prog-wrap{margin-bottom:14px}
.dv-prog-bar{height:5px;background:var(--border2);border-radius:3px;overflow:hidden;margin-bottom:4px}
.dv-prog-fill{height:100%;background:var(--green);border-radius:3px;transition:width .4s}
.dv-prog-txt{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);text-align:right}

/* Schéma vélo */
.dv-schema-card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:12px;margin-bottom:14px}
.dv-schema-title{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:8px;text-align:center}
.dv-bike-wrap{background:var(--surface);border-radius:10px;padding:8px;overflow:hidden}
.dv-bike-wrap svg{width:100%;height:auto;display:block}
.dv-zone{opacity:0}
.dv-marker{transition:fill .3s,stroke .3s}
.dv-marker-t{pointer-events:none;font-weight:600}
.dv-legend{display:flex;flex-wrap:wrap;gap:8px;justify-content:center;margin-top:10px}
.dv-leg-item{display:flex;align-items:center;gap:4px;font-size:10px;color:var(--text2);font-family:'JetBrains Mono',monospace}
.dv-leg-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0}

/* Cartes de contrôle */
.dv-cards{display:flex;flex-direction:column;gap:10px;margin-bottom:14px}
.dv-card{background:var(--card);border:2px solid var(--border);border-radius:14px;overflow:hidden;transition:border-color .2s}
.dv-card.st-ok{border-color:#22c55e}
.dv-card.st-warn{border-color:#eab308}
.dv-card.st-fix{border-color:#f97316}
.dv-card.st-replace{border-color:#ef4444}
.dv-card.st-danger{border-color:#a855f7}

.dv-card-hdr{display:flex;align-items:center;gap:10px;padding:12px 13px;cursor:pointer;-webkit-user-select:none}
.dv-card-hdr:active{background:var(--card2)}
.dv-card-num{width:24px;height:24px;min-width:24px;background:var(--card2);border-radius:50%;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--muted);flex-shrink:0}
.dv-card-name{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text);flex:1}
.dv-card-badge{height:22px;padding:0 9px;border-radius:11px;font-family:'JetBrains Mono',monospace;font-size:10px;display:flex;align-items:center;font-weight:600;flex-shrink:0}
.dv-card-arr{font-size:16px;color:var(--muted);flex-shrink:0;transition:transform .2s}
.dv-card.open .dv-card-arr{transform:rotate(90deg)}

.dv-card-body{display:none;padding:0 13px 13px;border-top:1px solid var(--border)}
.dv-card.open .dv-card-body{display:block}

/* Status selector */
.dv-status-row{display:flex;gap:5px;flex-wrap:wrap;margin:10px 0 8px}
.dv-st-btn{flex:1;min-width:calc(33% - 4px);padding:7px 4px;border-radius:8px;border:1.5px solid var(--border2);background:var(--card2);font-family:'JetBrains Mono',monospace;font-size:10px;cursor:pointer;-webkit-user-select:none;transition:all .15s;text-align:center;color:var(--muted)}
.dv-st-btn:active{transform:scale(.97)}
.dv-st-btn.sel-ok{border-color:#22c55e;background:rgba(34,197,94,.15);color:#22c55e}
.dv-st-btn.sel-warn{border-color:#eab308;background:rgba(234,179,8,.12);color:#eab308}
.dv-st-btn.sel-fix{border-color:#f97316;background:rgba(249,115,22,.12);color:#f97316}
.dv-st-btn.sel-replace{border-color:#ef4444;background:rgba(239,68,68,.12);color:#ef4444}
.dv-st-btn.sel-danger{border-color:#a855f7;background:rgba(168,85,247,.15);color:#a855f7}

/* Priorité */
.dv-prio-row{display:flex;gap:5px;margin:6px 0 8px}
.dv-prio-btn{flex:1;padding:6px 4px;border-radius:7px;border:1.5px solid var(--border2);background:var(--card2);font-size:11px;cursor:pointer;-webkit-user-select:none;transition:all .15s;text-align:center;color:var(--muted)}
.dv-prio-btn.p-low{border-color:#22c55e;background:rgba(34,197,94,.12);color:#22c55e}
.dv-prio-btn.p-med{border-color:#f97316;background:rgba(249,115,22,.12);color:#f97316}
.dv-prio-btn.p-urg{border-color:#ef4444;background:rgba(239,68,68,.15);color:#ef4444;font-weight:700}

/* Champs texte */
.dv-field-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:4px}
.dv-textarea{width:100%;background:var(--card2);border:1px solid var(--border2);border-radius:8px;padding:8px 10px;color:var(--text);font-family:'Outfit',sans-serif;font-size:13px;resize:none;outline:none;transition:border-color .15s;margin-bottom:8px}
.dv-textarea:focus{border-color:var(--blue)}

/* Photo */
.dv-photo-btn{display:flex;align-items:center;gap:8px;padding:8px 12px;background:var(--card2);border:1px dashed var(--border2);border-radius:8px;font-size:12px;color:var(--muted);cursor:pointer;-webkit-user-select:none;margin-bottom:4px;width:100%}
.dv-photo-preview{display:flex;flex-wrap:wrap;gap:6px;margin-top:6px}
.dv-photo-thumb{width:56px;height:56px;border-radius:6px;object-fit:cover;border:1px solid var(--border2)}
.dv-photo-input{display:none}

/* Résumé */
.dv-summary{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:15px;margin-bottom:12px}
.dv-sum-title{font-family:'Syne',sans-serif;font-size:15px;font-weight:800;color:var(--text);margin-bottom:10px}
.dv-sum-etat{font-size:14px;font-weight:600;margin-bottom:12px;padding:10px 13px;border-radius:10px}
.dv-sum-block{margin-bottom:10px}
.dv-sum-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:6px}
.dv-sum-item{font-size:13px;color:var(--text2);padding:5px 0;border-bottom:1px solid var(--border);line-height:1.5}
.dv-sum-item:last-child{border-bottom:none}
.dv-sum-comment{width:100%;background:var(--card2);border:1px solid var(--border2);border-radius:8px;padding:10px 12px;color:var(--text);font-family:'Outfit',sans-serif;font-size:13px;resize:none;outline:none;margin-top:6px;line-height:1.6}
.dv-sum-comment-wrap{margin-top:10px}

/* Boutons actions */
.dv-actions{display:flex;gap:9px;margin-bottom:10px}
.dv-btn-reset{flex:1;height:48px;background:var(--card2);border:1px solid var(--border2);border-radius:12px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text2);cursor:pointer;-webkit-user-select:none}
.dv-btn-reset:active{background:var(--card)}
.dv-btn-export{flex:1;height:48px;background:var(--green);border:none;border-radius:12px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:#fff;cursor:pointer;-webkit-user-select:none}
.dv-btn-export:active{background:#16a34a}

/* ── FICHE CLIENT/VÉLO ── */
.dv-fiche-card{background:var(--card);border:1px solid rgba(45,127,249,.3);border-radius:14px;padding:14px;margin-bottom:12px}
.dv-fiche-title{font-family:'Syne',sans-serif;font-size:14px;font-weight:800;color:var(--blue);display:flex;align-items:center;gap:7px;margin-bottom:12px}
.dv-fiche-toggle{margin-left:auto;background:none;border:none;color:var(--muted);font-size:16px;cursor:pointer;padding:0;transition:transform .2s}
.dv-fiche-toggle.closed{transform:rotate(-90deg)}
.dv-fiche-section{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--blue);letter-spacing:.15em;text-transform:uppercase;margin-bottom:8px;padding-bottom:4px;border-bottom:1px solid rgba(45,127,249,.2)}
.dv-fiche-row{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:8px}
.dv-fiche-field{display:flex;flex-direction:column;gap:3px}
.dv-fiche-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase}
.dv-fiche-inp{background:var(--card2);border:1px solid var(--border2);border-radius:8px;padding:8px 10px;color:var(--text);font-family:'Outfit',sans-serif;font-size:14px;outline:none;transition:border-color .15s;-webkit-appearance:none}
.dv-fiche-inp:focus{border-color:var(--blue)}
.dv-fiche-inp::placeholder{color:var(--muted);font-size:13px}
.dv-fiche-select{background:var(--card2);border:1px solid var(--border2);border-radius:8px;padding:8px 10px;color:var(--text);font-family:'Outfit',sans-serif;font-size:14px;outline:none;-webkit-appearance:none}

/* ── COUPLES DE SERRAGE ── */
.cpl-section{margin-bottom:16px}
.cpl-section-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--text2);margin-bottom:10px;display:flex;align-items:center;gap:8px}
.cpl-section-title::after{content:'';flex:1;height:1px;background:var(--border2)}
.cpl-grid{display:flex;flex-direction:column;gap:7px}
.cpl-card{background:var(--card);border:1px solid var(--border);border-radius:11px;padding:11px 13px}
.cpl-card.cpl-important{border-color:rgba(245,158,11,.3);background:rgba(245,158,11,.04)}
.cpl-zone{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--text);margin-bottom:7px}
.cpl-val-row{display:flex;align-items:center;justify-content:space-between;margin-bottom:4px}
.cpl-brand{font-family:'JetBrains Mono',monospace;font-size:10px;padding:2px 7px;border-radius:4px;letter-spacing:.04em}
.cpl-brand.shimano{background:rgba(45,127,249,.15);color:#60a0f5}
.cpl-brand.sram{background:rgba(239,68,68,.12);color:#f87171}
.cpl-brand:not(.shimano):not(.sram){background:var(--card2);color:var(--muted)}
.cpl-nm{font-family:'JetBrains Mono',monospace;font-size:15px;font-weight:600;color:var(--text)}
.cpl-note{font-size:11px;color:var(--muted);margin-top:4px;line-height:1.4}


/* ══ HISTORIQUE ══ */
#historique{background:var(--bg)}
.hist-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 30px}
.hist-empty{text-align:center;padding:40px 20px;color:var(--muted);font-family:'JetBrains Mono',monospace;font-size:12px;line-height:2}
.hist-card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:14px;margin-bottom:10px;cursor:pointer;-webkit-user-select:none;position:relative}
.hist-card:active{background:var(--card2)}
.hist-card-top{display:flex;align-items:center;gap:10px;margin-bottom:8px}
.hist-score-badge{width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-size:14px;font-weight:800;color:#fff;flex-shrink:0}
.hist-card-info{flex:1;min-width:0}
.hist-client{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.hist-velo{font-size:12px;color:var(--text2);margin-top:1px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.hist-date{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);margin-top:2px}
.hist-card-bottom{display:flex;gap:6px;flex-wrap:wrap}
.hist-tag{font-family:'JetBrains Mono',monospace;font-size:9px;padding:2px 7px;border-radius:4px;font-weight:600}
.hist-del-btn{position:absolute;top:12px;right:12px;width:28px;height:28px;background:rgba(239,68,68,.12);border:1px solid rgba(239,68,68,.2);border-radius:7px;color:#ef4444;font-size:14px;cursor:pointer;display:flex;align-items:center;justify-content:center}
.hist-actions{display:flex;gap:8px;margin-bottom:14px}
.hist-clear-btn{flex:1;height:44px;background:var(--card);border:1px solid var(--border2);border-radius:11px;font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--muted);cursor:pointer}

/* ══ DEVIS ══ */
#devis{background:var(--bg)}
.devis-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 30px}
.devis-client-bar{background:rgba(45,127,249,.08);border:1px solid rgba(45,127,249,.2);border-radius:12px;padding:11px 14px;margin-bottom:14px;font-size:13px;color:var(--text2);display:flex;align-items:center;gap:10px}
.devis-client-name{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text);flex:1}
.devis-line-card{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:12px 13px;margin-bottom:8px}
.devis-line-top{display:flex;gap:8px;align-items:flex-start;margin-bottom:8px}
.devis-line-desc{flex:1;background:var(--card2);border:1px solid var(--border2);border-radius:8px;padding:8px 10px;color:var(--text);font-family:'Outfit',sans-serif;font-size:14px;outline:none;border:none;width:100%}
.devis-line-desc::placeholder{color:var(--muted)}
.devis-line-bottom{display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:6px;align-items:center}
.devis-input{background:var(--card2);border:1px solid var(--border2);border-radius:7px;padding:7px 9px;color:var(--text);font-family:'JetBrains Mono',monospace;font-size:13px;outline:none;width:100%;text-align:center}
.devis-input::placeholder{color:var(--muted);font-size:11px}
.devis-line-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;margin-bottom:3px;text-align:center}
.devis-del{width:30px;height:30px;background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.2);border-radius:7px;color:#ef4444;font-size:16px;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.devis-add-btn{width:100%;height:44px;background:var(--card);border:2px dashed var(--border2);border-radius:12px;color:var(--muted);font-family:'Syne',sans-serif;font-size:14px;font-weight:700;cursor:pointer;margin-bottom:14px;transition:all .15s}
.devis-add-btn:active{background:var(--card2)}
.devis-total-card{background:linear-gradient(135deg,#0c1624,#0d0d0d);border:2px solid var(--blue);border-radius:14px;padding:16px;margin-bottom:14px}
.devis-total-rows{margin-bottom:12px}
.devis-total-row{display:flex;justify-content:space-between;align-items:center;padding:5px 0;border-bottom:1px solid var(--border)}
.devis-total-row:last-child{border-bottom:none}
.devis-total-lbl{font-size:13px;color:var(--text2)}
.devis-total-val{font-family:'JetBrains Mono',monospace;font-size:14px;color:var(--text)}
.devis-grand-total{display:flex;justify-content:space-between;align-items:center;margin-top:10px;padding-top:10px;border-top:2px solid var(--blue)}
.devis-grand-lbl{font-family:'Syne',sans-serif;font-size:15px;font-weight:800;color:var(--text)}
.devis-grand-val{font-family:'Syne',sans-serif;font-size:24px;font-weight:800;color:var(--blue)}
.devis-tva-row{display:flex;align-items:center;gap:10px;margin-bottom:10px}
.devis-tva-lbl{font-size:13px;color:var(--text2)}
.devis-tva-select{background:var(--card2);border:1px solid var(--border2);border-radius:7px;padding:6px 10px;color:var(--text);font-size:13px;outline:none}
.devis-btns{display:flex;gap:8px}
.devis-copy-btn{flex:1;height:46px;background:var(--blue);border:none;border-radius:11px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:#fff;cursor:pointer}
.devis-reset-btn{height:46px;padding:0 16px;background:var(--card2);border:1px solid var(--border2);border-radius:11px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--muted);cursor:pointer}

/* ══ BIBLIOTHÈQUE ══ */
#biblio{background:var(--bg)}
.biblio-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 30px}
.biblio-search{display:flex;align-items:center;gap:10px;background:var(--card);border:1px solid var(--border2);border-radius:12px;padding:11px 14px;margin-bottom:14px}
.biblio-search input{flex:1;background:none;border:none;outline:none;font-family:'Outfit',sans-serif;font-size:15px;color:var(--text)}
.biblio-search input::placeholder{color:var(--muted)}
.biblio-add-btn-main{width:100%;height:48px;background:var(--green);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:#fff;cursor:pointer;margin-bottom:14px;display:flex;align-items:center;justify-content:center;gap:8px}
.biblio-card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:14px;margin-bottom:10px;cursor:pointer}
.biblio-card:active{background:var(--card2)}
.biblio-card-top{display:flex;align-items:center;gap:12px;margin-bottom:6px}
.biblio-avatar{width:42px;height:42px;border-radius:10px;background:rgba(45,127,249,.15);display:flex;align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-size:18px;font-weight:800;color:var(--blue);flex-shrink:0}
.biblio-card-info{flex:1;min-width:0}
.biblio-client-name{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text)}
.biblio-client-contact{font-size:12px;color:var(--text2);margin-top:2px}
.biblio-velos{display:flex;flex-wrap:wrap;gap:5px;margin-top:6px}
.biblio-velo-tag{background:var(--card2);border:1px solid var(--border2);border-radius:6px;padding:3px 8px;font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--text2)}
.biblio-card-actions{display:flex;gap:6px;margin-top:10px}
.biblio-use-btn{flex:1;height:36px;background:var(--blue);border:none;border-radius:8px;font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:#fff;cursor:pointer}
.biblio-diag-btn{flex:1;height:36px;background:var(--green);border:none;border-radius:8px;font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:#fff;cursor:pointer}
.biblio-del-btn{height:36px;width:36px;background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.2);border-radius:8px;color:#ef4444;font-size:14px;cursor:pointer}
.biblio-empty{text-align:center;padding:40px 20px;color:var(--muted);font-size:13px;line-height:2}

/* Modal bibliothèque */
.biblio-modal{position:fixed;inset:0;z-index:9000;background:rgba(0,0,0,.88);display:flex;align-items:flex-end}
.biblio-modal-sheet{background:var(--card);border-radius:20px 20px 0 0;width:100%;max-height:90vh;overflow-y:auto;padding:0 0 calc(var(--sb)+16px);-webkit-overflow-scrolling:touch}
.biblio-modal-handle{width:36px;height:4px;background:var(--border2);border-radius:2px;margin:12px auto}
.biblio-modal-title{font-family:'Syne',sans-serif;font-size:17px;font-weight:800;padding:4px 18px 14px;color:var(--text)}
.biblio-modal-body{padding:0 16px}
.biblio-modal-field{margin-bottom:12px}
.biblio-modal-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:5px}
.biblio-modal-inp{width:100%;background:var(--card2);border:1px solid var(--border2);border-radius:9px;padding:10px 12px;color:var(--text);font-family:'Outfit',sans-serif;font-size:14px;outline:none}
.biblio-modal-inp::placeholder{color:var(--muted)}
.biblio-modal-save{width:calc(100% - 32px);height:50px;background:var(--green);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:16px;font-weight:800;color:#fff;cursor:pointer;margin:14px 16px 0;display:block}

/* Notification toast */
.velo-toast{position:fixed;bottom:calc(var(--sb)+20px);left:50%;transform:translateX(-50%) translateY(20px);background:#22c55e;color:#fff;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;padding:10px 22px;border-radius:20px;z-index:9999;opacity:0;transition:all .3s;pointer-events:none;white-space:nowrap}
.velo-toast.show{opacity:1;transform:translateX(-50%) translateY(0)}


/* ══ PWA ══ */
#splash{background:#0d0d0d;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px}

/* ══ HISTORIQUE ══ */
#historique{background:var(--bg)}
.hist-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 30px}
.hist-empty{text-align:center;padding:50px 20px;color:var(--muted);font-size:13px;line-height:2.2}
.hist-card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:13px 14px;margin-bottom:9px;position:relative}
.hist-card-hdr{display:flex;align-items:center;gap:11px;margin-bottom:8px}
.hist-score-dot{width:38px;height:38px;border-radius:9px;display:flex;align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-size:13px;font-weight:800;color:#fff;flex-shrink:0}
.hist-info{flex:1;min-width:0}
.hist-client{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.hist-velo{font-size:12px;color:var(--text2);margin-top:1px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.hist-date{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);margin-top:2px}
.hist-tags{display:flex;gap:5px;flex-wrap:wrap}
.hist-tag{font-family:'JetBrains Mono',monospace;font-size:9px;padding:2px 7px;border-radius:4px;font-weight:600}
.hist-card-btns{display:flex;gap:7px;margin-top:9px}
.hist-open-btn{flex:1;height:36px;background:var(--blue);border:none;border-radius:8px;font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:#fff;cursor:pointer}
.hist-devis-btn{flex:1;height:36px;background:var(--green);border:none;border-radius:8px;font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:#fff;cursor:pointer}
.hist-del-btn{width:36px;height:36px;background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.2);border-radius:8px;color:#ef4444;font-size:15px;cursor:pointer}
.hist-top-bar{display:flex;gap:8px;margin-bottom:12px}
.hist-count{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--muted);flex:1;display:flex;align-items:center}
.hist-clear-btn{height:36px;padding:0 14px;background:var(--card);border:1px solid var(--border2);border-radius:9px;font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--muted);cursor:pointer}

/* ══ DEVIS ══ */
#devis{background:var(--bg)}
.devis-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 30px}
.devis-client-bar{background:rgba(45,127,249,.07);border:1px solid rgba(45,127,249,.18);border-radius:12px;padding:11px 14px;margin-bottom:14px;display:flex;align-items:center;gap:10px}
.devis-client-txt{flex:1;font-size:13px;color:var(--text2)}
.devis-client-name{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text)}
.devis-from-diag-btn{height:32px;padding:0 12px;background:var(--blue);border:none;border-radius:8px;font-size:11px;font-weight:700;color:#fff;cursor:pointer;font-family:'JetBrains Mono',monospace;white-space:nowrap}
.devis-section-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.15em;text-transform:uppercase;margin-bottom:8px;display:flex;align-items:center;gap:8px}
.devis-section-lbl::after{content:'';flex:1;height:1px;background:var(--border2)}
.devis-lines{margin-bottom:4px}
.devis-line{background:var(--card);border:1px solid var(--border);border-radius:11px;padding:11px 12px;margin-bottom:7px}
.devis-line-desc-inp{width:100%;background:none;border:none;outline:none;font-family:'Outfit',sans-serif;font-size:14px;color:var(--text);margin-bottom:8px;padding:0}
.devis-line-desc-inp::placeholder{color:var(--muted)}
.devis-line-row{display:grid;grid-template-columns:1fr 1fr 80px auto;gap:6px;align-items:end}
.devis-num-wrap{display:flex;flex-direction:column;gap:3px}
.devis-num-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.08em;text-transform:uppercase}
.devis-num-inp{background:var(--card2);border:1px solid var(--border2);border-radius:7px;padding:7px 8px;color:var(--text);font-family:'JetBrains Mono',monospace;font-size:13px;outline:none;width:100%;text-align:center}
.devis-line-total{font-family:'JetBrains Mono',monospace;font-size:14px;font-weight:600;color:var(--text);text-align:right;padding:7px 0;align-self:end}
.devis-del{width:32px;height:32px;background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.2);border-radius:7px;color:#ef4444;font-size:15px;cursor:pointer;align-self:end}
.devis-add-btn{width:100%;height:44px;background:var(--card);border:2px dashed var(--border2);border-radius:11px;color:var(--muted);font-family:'Syne',sans-serif;font-size:14px;font-weight:700;cursor:pointer;margin-bottom:14px}
.devis-total-card{background:#0c1624;border:2px solid var(--blue);border-radius:14px;padding:15px;margin-bottom:14px}
.devis-total-line{display:flex;justify-content:space-between;padding:5px 0;border-bottom:1px solid #1a2a3a;font-size:13px;color:var(--text2)}
.devis-total-line:last-child{border-bottom:none}
.devis-total-line span:last-child{font-family:'JetBrains Mono',monospace;color:var(--text)}
.devis-grand{display:flex;justify-content:space-between;align-items:center;margin-top:12px;padding-top:12px;border-top:2px solid var(--blue)}
.devis-grand-lbl{font-family:'Syne',sans-serif;font-size:15px;font-weight:800;color:var(--text)}
.devis-grand-val{font-family:'Syne',sans-serif;font-size:26px;font-weight:800;color:var(--blue)}
.devis-tva-row{display:flex;align-items:center;gap:10px;margin-bottom:12px}
.devis-tva-lbl{font-size:13px;color:var(--text2)}
.devis-tva-sel{background:var(--card2);border:1px solid var(--border2);border-radius:7px;padding:6px 10px;color:var(--text);font-size:13px;outline:none}
.devis-btns{display:flex;gap:8px}
.devis-share-btn{flex:1;height:46px;background:var(--green);border:none;border-radius:11px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:#fff;cursor:pointer}
.devis-copy-btn{flex:1;height:46px;background:var(--blue);border:none;border-radius:11px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:#fff;cursor:pointer}
.devis-reset-btn{height:46px;padding:0 14px;background:var(--card2);border:1px solid var(--border2);border-radius:11px;font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--muted);cursor:pointer}

/* ══ BIBLIOTHÈQUE ══ */
#biblio{background:var(--bg)}
.biblio-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px 30px}
.biblio-searchbar{display:flex;align-items:center;gap:10px;background:var(--card);border:1px solid var(--border2);border-radius:12px;padding:10px 14px;margin-bottom:12px}
.biblio-searchbar input{flex:1;background:none;border:none;outline:none;font-family:'Outfit',sans-serif;font-size:15px;color:var(--text)}
.biblio-searchbar input::placeholder{color:var(--muted)}
.biblio-new-btn{width:100%;height:48px;background:var(--blue);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:15px;font-weight:800;color:#fff;cursor:pointer;margin-bottom:14px;display:flex;align-items:center;justify-content:center;gap:8px}
.biblio-card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:13px 14px;margin-bottom:9px}
.biblio-card-top{display:flex;align-items:flex-start;gap:11px;margin-bottom:8px}
.biblio-avatar{width:42px;height:42px;border-radius:10px;background:rgba(45,127,249,.12);display:flex;align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-size:18px;font-weight:800;color:var(--blue);flex-shrink:0}
.biblio-info{flex:1;min-width:0}
.biblio-name{font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:var(--text)}
.biblio-contact{font-size:12px;color:var(--text2);margin-top:2px;line-height:1.5}
.biblio-velos{display:flex;flex-wrap:wrap;gap:5px;margin-top:6px}
.biblio-velo-tag{background:var(--card2);border:1px solid var(--border2);border-radius:6px;padding:3px 8px;font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--text2)}
.biblio-btns{display:flex;gap:6px}
.biblio-use-btn{flex:1;height:36px;background:var(--blue);border:none;border-radius:8px;font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:#fff;cursor:pointer}
.biblio-diag-btn{flex:1;height:36px;background:var(--green);border:none;border-radius:8px;font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:#fff;cursor:pointer}
.biblio-del-btn{width:36px;height:36px;background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.2);border-radius:8px;color:#ef4444;font-size:14px;cursor:pointer}
.biblio-empty{text-align:center;padding:50px 20px;color:var(--muted);font-size:13px;line-height:2.2}

/* Modale biblio */
.velo-modal-overlay{position:fixed;inset:0;z-index:9000;background:rgba(0,0,0,.88);display:flex;align-items:flex-end}
.velo-modal-sheet{background:var(--surface);border-radius:20px 20px 0 0;width:100%;max-height:90vh;overflow-y:auto;-webkit-overflow-scrolling:touch;padding-bottom:calc(var(--sb)+16px)}
.velo-modal-handle{width:36px;height:4px;background:var(--border2);border-radius:2px;margin:12px auto 4px}
.velo-modal-title{font-family:'Syne',sans-serif;font-size:17px;font-weight:800;padding:8px 18px 14px;color:var(--text);border-bottom:1px solid var(--border2)}
.velo-modal-body{padding:14px 16px}
.velo-modal-field{margin-bottom:12px}
.velo-modal-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:5px}
.velo-modal-inp{width:100%;background:var(--card2);border:1px solid var(--border2);border-radius:9px;padding:10px 12px;color:var(--text);font-family:'Outfit',sans-serif;font-size:14px;outline:none;-webkit-appearance:none}
.velo-modal-inp::placeholder{color:var(--muted)}
.velo-modal-row{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.velo-modal-save{width:calc(100% - 32px);height:50px;background:var(--green);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:16px;font-weight:800;color:#fff;cursor:pointer;margin:14px 16px 0;display:block}
.velo-modal-cancel{width:calc(100% - 32px);height:44px;background:var(--card2);border:1px solid var(--border2);border-radius:13px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--muted);cursor:pointer;margin:8px 16px 0;display:block}

/* Toast notification */
.velo-toast{position:fixed;bottom:calc(var(--sb)+24px);left:50%;transform:translateX(-50%) translateY(16px);background:#22c55e;color:#fff;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;padding:11px 22px;border-radius:20px;z-index:9999;opacity:0;transition:all .28s;pointer-events:none;white-space:nowrap;box-shadow:0 4px 20px rgba(34,197,94,.4)}
.velo-toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
.velo-toast.error{background:#ef4444;box-shadow:0 4px 20px rgba(239,68,68,.4)}
/* scrollbar */
::-webkit-scrollbar{width:0;height:0}

/* ══ LIGHT MODE ══ */
body.light-mode{
  --bg:#f5f5f0;--surface:#ffffff;--card:#ffffff;--card2:#f0ede8;
  --border:#e0dcd6;--border2:#d0ccc6;
  --text:#1a1a1a;--text2:#5a5550;--muted:#9a9590;
  background:#f5f5f0;color:#1a1a1a;
}
body.light-mode .res-hero{background:linear-gradient(135deg,#fff1ec,#f5f5f0)}
body.light-mode .sh-result{background:linear-gradient(135deg,#e8f0ff,#f5f5f0)}
body.light-mode .devis-total-card{background:#eaf3ff}
body.light-mode .splash-logo,.light-mode .home-logo{color:#1a1a1a}
body.light-mode .ray-calc-btn:active{background:#1a5fd4}

/* ══ LANG TOGGLE ══ */
.lang-toggle{display:flex;align-items:center;gap:6px;background:var(--card2);border:1px solid var(--border2);border-radius:20px;padding:3px 5px}
.lang-btn{height:26px;padding:0 10px;border:none;border-radius:16px;font-family:'JetBrains Mono',monospace;font-size:11px;font-weight:600;cursor:pointer;transition:all .15s;background:transparent;color:var(--muted)}
.lang-btn.active{background:var(--red);color:#fff}

/* ══ THEME TOGGLE ══ */
.theme-toggle{width:42px;height:24px;background:var(--border2);border-radius:12px;position:relative;cursor:pointer;border:none;transition:background .2s;flex-shrink:0}
.theme-toggle.on{background:var(--gold)}
.theme-toggle::after{content:'';position:absolute;top:3px;left:3px;width:18px;height:18px;background:#fff;border-radius:50%;transition:transform .2s;box-shadow:0 1px 4px rgba(0,0,0,.3)}
.theme-toggle.on::after{transform:translateX(18px)}
.settings-row{display:flex;align-items:center;justify-content:space-between;padding:14px 16px;border-bottom:1px solid var(--border)}
.settings-lbl{font-size:14px;color:var(--text2)}
.settings-lbl strong{color:var(--text);display:block;font-size:15px;margin-bottom:2px}

/* ══ TENSION RAYONS ══ */
.ray-tension-card{background:var(--card);border:2px solid var(--purple);border-radius:14px;padding:16px;margin-top:12px;display:none}
.ray-tension-card.show{display:block}
.ray-tension-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:var(--purple);margin-bottom:12px;text-transform:uppercase;letter-spacing:.05em}
.tension-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:10px}
.tension-side{background:var(--card2);border-radius:10px;padding:12px}
.tension-side-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.1em;margin-bottom:6px}
.tension-nm{font-family:'JetBrains Mono',monospace;font-size:22px;font-weight:500;color:var(--text);line-height:1}
.tension-nm-unit{font-size:11px;color:var(--muted)}
.tension-plage{font-size:11px;color:var(--text2);margin-top:3px}
.tension-gauge-wrap{margin-bottom:6px}
.tension-gauge-label{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);margin-bottom:4px;display:flex;justify-content:space-between}
.tension-gauge-bar{height:8px;background:var(--border2);border-radius:4px;position:relative;overflow:hidden}
.tension-gauge-fill{height:100%;border-radius:4px;transition:width .4s}
.tension-ratio-card{background:var(--surface);border-radius:8px;padding:9px 10px}
.tension-ratio-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);text-transform:uppercase;margin-bottom:3px}
.tension-ratio-val{font-family:'JetBrains Mono',monospace;font-size:14px;color:var(--text)}

/* ══ COMPATIBILITÉ CHAÎNE/CASSETTE ══ */
#compat{background:var(--bg)}
.compat-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px 14px calc(var(--sb)+20px)}
.compat-section{background:var(--card);border:1px solid var(--border);border-radius:14px;margin-bottom:12px;overflow:hidden}
.compat-section-hdr{background:var(--card2);border-bottom:1px solid var(--border);padding:11px 14px;display:flex;align-items:center;gap:8px;cursor:pointer;-webkit-user-select:none}
.compat-section-hdr-title{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;flex:1}
.compat-section-body{padding:12px 14px}
.compat-field-lbl{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-bottom:7px}
.compat-chip-row{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:12px}
.compat-chip{padding:7px 12px;border:2px solid var(--border2);border-radius:20px;font-size:13px;cursor:pointer;-webkit-user-select:none;transition:all .15s;color:var(--text2)}
.compat-chip.sel{border-color:var(--red);background:var(--red-dim);color:var(--text)}
.compat-chip:active{transform:scale(.96)}
.compat-result{background:var(--card);border:2px solid var(--green);border-radius:14px;padding:15px;margin-top:4px;display:none}
.compat-result.show{display:block}
.compat-result-title{font-family:'Syne',sans-serif;font-size:12px;font-weight:700;color:var(--green);text-transform:uppercase;letter-spacing:.06em;margin-bottom:12px}
.compat-chain-list{display:flex;flex-direction:column;gap:7px;margin-bottom:10px}
.compat-chain-item{display:flex;align-items:center;gap:10px;background:var(--card2);border-radius:10px;padding:10px 12px;font-size:13px}
.cc-badge{font-family:'JetBrains Mono',monospace;font-size:10px;padding:2px 8px;border-radius:10px;flex-shrink:0;font-weight:600}
.cc-badge.shimano{background:rgba(45,127,249,.15);color:#2d7ff9}
.cc-badge.sram{background:rgba(232,64,12,.12);color:#e8400c}
.cc-badge.kmc{background:rgba(168,85,247,.12);color:#a855f7}
.cc-badge.campa{background:rgba(245,158,11,.12);color:#d4920a}
.compat-alert-list{display:flex;flex-direction:column;gap:6px}
.compat-alert{display:flex;align-items:flex-start;gap:8px;background:rgba(239,68,68,.08);border:1px solid rgba(239,68,68,.2);border-radius:8px;padding:9px 11px;font-size:12px;color:#f87171;line-height:1.5}
.compat-ok{display:flex;align-items:flex-start;gap:8px;background:rgba(34,197,94,.08);border:1px solid rgba(34,197,94,.2);border-radius:8px;padding:9px 11px;font-size:12px;color:#22c55e;line-height:1.5}
.compat-calc-btn{width:100%;height:50px;background:var(--green);border:none;border-radius:13px;font-family:'Syne',sans-serif;font-size:16px;font-weight:800;color:#fff;cursor:pointer;margin-top:10px;-webkit-user-select:none}
.compat-calc-btn:active{background:#16a34a}

/* ══ GLOSSAIRE ══ */
#glossaire{background:var(--bg)}
.glos-scroll{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:0 0 calc(var(--sb)+20px)}
.glos-search{display:flex;align-items:center;gap:10px;background:var(--card);border-bottom:1px solid var(--border2);padding:12px 16px;flex-shrink:0}
.glos-search input{flex:1;background:none;border:none;outline:none;font-family:'Outfit',sans-serif;font-size:15px;color:var(--text)}
.glos-search input::placeholder{color:var(--muted)}
.glos-letter-bar{display:flex;flex-wrap:wrap;gap:4px;padding:10px 14px;background:var(--surface);border-bottom:1px solid var(--border);flex-shrink:0}
.glos-letter-btn{width:28px;height:28px;border:1px solid var(--border2);border-radius:6px;background:var(--card2);font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--text2);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all .15s}
.glos-letter-btn.active{background:var(--red);border-color:var(--red);color:#fff}
.glos-letter-btn:active{transform:scale(.92)}
.glos-section{padding:0 14px}
.glos-letter-hdr{font-family:'Syne',sans-serif;font-size:22px;font-weight:800;color:var(--red);padding:14px 0 6px;border-bottom:1px solid var(--border2);margin-bottom:10px}
.glos-entry{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:13px 14px;margin-bottom:8px}
.glos-entry-hdr{display:flex;align-items:center;gap:10px;cursor:pointer;-webkit-user-select:none}
.glos-term{font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:var(--text);flex:1}
.glos-tag{font-family:'JetBrains Mono',monospace;font-size:9px;padding:2px 7px;border-radius:8px;flex-shrink:0;font-weight:600}
.glos-tag.fr{background:rgba(45,127,249,.12);color:#2d7ff9}
.glos-tag.en{background:rgba(168,85,247,.12);color:#a855f7}
.glos-tag.std{background:rgba(34,197,94,.1);color:#22c55e}
.glos-arr{color:var(--muted);font-size:16px;transition:transform .2s}
.glos-arr.open{transform:rotate(90deg)}
.glos-def{display:none;margin-top:10px;font-size:13px;color:var(--text2);line-height:1.75;border-top:1px solid var(--border);padding-top:10px}
.glos-def.open{display:block}
.glos-def strong{color:var(--text)}
.glos-def code{font-family:'JetBrains Mono',monospace;font-size:12px;background:var(--card2);padding:1px 5px;border-radius:4px;color:var(--blue)}
.glos-def .see-also{margin-top:8px;font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.05em}
.glos-def .see-also a{color:var(--blue);text-decoration:none;cursor:pointer}
.glos-empty{text-align:center;padding:40px 20px;color:var(--muted);font-size:13px}

/* ══ SETTINGS ══ */
#settings{background:var(--bg)}
.settings-scroll{flex:1;overflow-y:auto;padding:0 0 calc(var(--sb)+20px)}
.settings-section-title{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:.15em;text-transform:uppercase;padding:16px 16px 8px}
.settings-group{background:var(--card);border-top:1px solid var(--border);border-bottom:1px solid var(--border);margin-bottom:20px}
.settings-about{padding:20px 16px;text-align:center}
.settings-version{font-family:'JetBrains Mono',monospace;font-size:13px;color:var(--muted)}
.settings-app-name{font-family:'Syne',sans-serif;font-size:22px;font-weight:800;color:var(--text);margin-bottom:4px}
.settings-app-name em{color:var(--red);font-style:normal}
</style>
</head>
<body>

<!-- SPLASH -->
<div class="screen active" id="splash">
  <div class="splash-logo">Vélo<em>ID</em></div>
  <div class="splash-sub">Diagnostic · Mesure · Rayonnage</div>
</div>

<!-- HOME -->
<div class="screen" id="home">
  <div class="home-hdr" style="display:flex;align-items:flex-start;justify-content:space-between">
    <div>
      <div class="home-logo">Vélo<em>ID</em></div>
      <div class="home-tag" id="homeTag">Identifiez vos standards en atelier</div>
    </div>
    <div style="display:flex;align-items:center;gap:8px;margin-top:4px">
      <div class="lang-toggle">
        <button class="lang-btn active" id="langFR" onclick="setLang('fr')">FR</button>
        <button class="lang-btn" id="langEN" onclick="setLang('en')">EN</button>
      </div>
      <div onclick="showScreen('settings')" style="width:34px;height:34px;background:var(--card);border:1px solid var(--border2);border-radius:10px;display:flex;align-items:center;justify-content:center;cursor:pointer">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="3" stroke="var(--text2)" stroke-width="2"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" stroke="var(--text2)" stroke-width="2"/></svg>
      </div>
    </div>
  </div>
  <div class="search-wrap">
    <div class="search-box">
      <svg width="16" height="16" fill="none" viewBox="0 0 24 24"><circle cx="11" cy="11" r="7" stroke="#5a5550" stroke-width="2"/><path d="m16 16 4 4" stroke="#5a5550" stroke-width="2" stroke-linecap="round"/></svg>
      <input type="search" id="searchInput" placeholder="Chercher un standard…" oninput="filterCards()">
    </div>
  </div>
  <div class="sec-lbl" style="flex-shrink:0">Diagnostics guidés</div>
  <div class="cards-scroll" id="cardsScroll">

    <div class="diag-card" data-tags="boitier pedalier bsa pf30 bb86 pressfit t47" onclick="startDiag('bb')">
      <div class="dc-icon"><!-- Boîtier pédalier SVG --><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#e8400c" stroke-width="2"/><circle cx="14" cy="14" r="5" stroke="#9a9590" stroke-width="1.5"/><line x1="4" y1="14" x2="9" y2="14" stroke="#e8400c" stroke-width="2"/><line x1="19" y1="14" x2="24" y2="14" stroke="#e8400c" stroke-width="2"/><rect x="1" y="12" width="3" height="4" rx="1" fill="#9a9590"/><rect x="24" y="12" width="3" height="4" rx="1" fill="#9a9590"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Boîtier Pédalier</div>
        <div class="dc-desc">BSA · T47 · PF30 · BB86 · BB30…</div>
        <div class="dc-meta">3 mesures · 12 standards</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="axe roue old qr traversant boost thru axle" onclick="startDiag('axe')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#9a9590" stroke-width="1.5"/><line x1="2" y1="14" x2="26" y2="14" stroke="#e8400c" stroke-width="2.5" stroke-linecap="round"/><circle cx="14" cy="14" r="3" fill="#e8400c"/><rect x="0" y="12" width="4" height="4" rx="1" fill="#2d7ff9"/><rect x="24" y="12" width="4" height="4" rx="1" fill="#2d7ff9"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Axe de Roue</div>
        <div class="dc-desc">QR · TA 12/15mm · Boost · OLD…</div>
        <div class="dc-meta">3 mesures · avant &amp; arrière</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="pneu etrto jante bsd 700c 29 26 27.5 tubeless" onclick="startDiag('pneu')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="11" stroke="#e8400c" stroke-width="2.5"/><circle cx="14" cy="14" r="6" stroke="#9a9590" stroke-width="1.5"/><circle cx="14" cy="14" r="2" fill="#e8400c"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Pneu / Jante ETRTO</div>
        <div class="dc-desc">700C · 29" · 27,5" · 26" · BSD…</div>
        <div class="dc-meta">2 valeurs · toutes tailles</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="guidon cintre potence diametre 31.8 35mm" onclick="startDiag('guidon')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M2 14h24" stroke="#e8400c" stroke-width="2.5" stroke-linecap="round"/><path d="M8 14v-5a4 4 0 0 1 8 0" stroke="#9a9590" stroke-width="1.5" stroke-linecap="round"/><circle cx="14" cy="9" r="2" fill="#e8400c"/><rect x="6" y="13" width="4" height="3" rx="1" fill="#9a9590"/><rect x="18" y="13" width="4" height="3" rx="1" fill="#9a9590"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Guidon &amp; Potence</div>
        <div class="dc-desc">22,2 · 25,4 · 31,8 · 35 mm…</div>
        <div class="dc-meta">2 mesures · cintre ou colonne</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="tige selle dropper 27.2 31.6 30.9 diametre" onclick="startDiag('selle')">
      <div class="dc-icon"><!-- Tige de selle : tube vertical + selle stylisée --><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="12" y="10" width="4" height="16" rx="2" fill="#e8400c" opacity=".8"/><path d="M6 10 Q14 6 22 10" stroke="#e8400c" stroke-width="2" fill="none" stroke-linecap="round"/><line x1="14" y1="10" x2="14" y2="6" stroke="#9a9590" stroke-width="1.5"/><line x1="8" y1="16" x2="20" y2="16" stroke="#2d7ff9" stroke-width="1" stroke-dasharray="2,2"/><text x="21" y="17" font-size="6" fill="#2d7ff9" font-family="monospace">Ø</text></svg></div>
      <div class="dc-info">
        <div class="dc-title">Tige de Selle</div>
        <div class="dc-desc">27,2 · 30,9 · 31,6 mm…</div>
        <div class="dc-meta">1 mesure · 9 standards</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="frein disque is pm flat mount 160 180 203 rotor" onclick="startDiag('frein')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#e8400c" stroke-width="2"/><circle cx="14" cy="14" r="4" fill="none" stroke="#9a9590" stroke-width="1.5"/><circle cx="14" cy="4.5" r="1.5" fill="#9a9590"/><circle cx="22.2" cy="9.5" r="1.5" fill="#9a9590"/><circle cx="22.2" cy="18.5" r="1.5" fill="#9a9590"/><circle cx="14" cy="23.5" r="1.5" fill="#9a9590"/><circle cx="5.8" cy="18.5" r="1.5" fill="#9a9590"/><circle cx="5.8" cy="9.5" r="1.5" fill="#9a9590"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Frein à Disque</div>
        <div class="dc-desc">IS · Post Mount · Flat Mount · Rotor…</div>
        <div class="dc-meta">3 mesures · montage + Ø</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="chaine vitesses shimano sram campagnolo 11v 12v" onclick="startDiag('chaine')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="3" y="11" width="5" height="6" rx="1.5" stroke="#e8400c" stroke-width="1.5"/><rect x="11" y="11" width="5" height="6" rx="1.5" stroke="#9a9590" stroke-width="1.5"/><rect x="19" y="11" width="5" height="6" rx="1.5" stroke="#e8400c" stroke-width="1.5"/><line x1="8" y1="14" x2="11" y2="14" stroke="#9a9590" stroke-width="1.5"/><line x1="16" y1="14" x2="19" y2="14" stroke="#9a9590" stroke-width="1.5"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Chaîne &amp; Vitesses</div>
        <div class="dc-desc">6→13v · largeur · compatibilité…</div>
        <div class="dc-meta">2 questions · marque + vitesses</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="freehub corps roue libre hg microspline xd xdr cassette" onclick="startDiag('freehub')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="9" stroke="#9a9590" stroke-width="1.5"/><circle cx="14" cy="14" r="5" stroke="#e8400c" stroke-width="2"/><path d="M14 5v4M14 19v4M5 14h4M19 14h4" stroke="#9a9590" stroke-width="1.5" stroke-linecap="round"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Corps de Roue Libre</div>
        <div class="dc-desc">HG · Micro Spline · XD · XDR · N3W…</div>
        <div class="dc-meta">3 questions · identification</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="pedalier bcd spider plateau direct mount" onclick="startDiag('bcd')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="4" fill="#e8400c"/><circle cx="14" cy="6" r="2" fill="#9a9590"/><circle cx="21" cy="10" r="2" fill="#9a9590"/><circle cx="21" cy="18" r="2" fill="#9a9590"/><circle cx="14" cy="22" r="2" fill="#9a9590"/><circle cx="7" cy="18" r="2" fill="#9a9590"/><circle cx="7" cy="10" r="2" fill="#9a9590"/><circle cx="14" cy="6" r="6" fill="none" stroke="#e8400c" stroke-width="1" stroke-dasharray="3,2" transform="translate(0,8)"/></svg></div>
      <div class="dc-info">
        <div class="dc-title">Plateau / BCD</div>
        <div class="dc-desc">104 · 110 · 130 mm · Direct Mount…</div>
        <div class="dc-meta">2 mesures · BCD &amp; bras</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="etrto decoder code pneu" onclick="startDiag('etrto_dec')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="3" y="6" width="22" height="16" rx="3" stroke="#e8400c" stroke-width="1.5"/><text x="14" y="17" text-anchor="middle" font-size="8" fill="#e8400c" font-family="monospace" font-weight="bold">37-622</text></svg></div>
      <div class="dc-info">
        <div class="dc-title">Décodeur ETRTO</div>
        <div class="dc-desc">Entrez un code → toutes les infos</div>
        <div class="dc-meta">Outil express</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" data-tags="shis decoder code jeu direction headset" onclick="startDiag('shis_dec')">
      <div class="dc-icon"><svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="3" y="6" width="22" height="16" rx="3" stroke="#9a9590" stroke-width="1.5"/><text x="14" y="17" text-anchor="middle" font-size="7" fill="#2d7ff9" font-family="monospace" font-weight="bold">ZS44|28</text></svg></div>
      <div class="dc-info">
        <div class="dc-title">Décodeur SHIS</div>
        <div class="dc-desc">Entrez un code → description complète</div>
        <div class="dc-meta">Outil express</div>
      </div><div class="dc-arr">›</div>
    </div>

    <!-- ── Outils de calcul ── -->
    <div class="sec-lbl-inner" style="margin:10px 0 4px">Outils de calcul</div>

    <div class="diag-card" onclick="showScreen('shis')">
      <div class="dc-icon" style="background:rgba(232,64,12,.12)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="11" y="2" width="6" height="24" rx="2" stroke="#e8400c" stroke-width="1.8"/><ellipse cx="14" cy="8" rx="7" ry="4" stroke="#e8400c" stroke-width="1.4"/><ellipse cx="14" cy="20" rx="9" ry="4.5" stroke="#e8400c" stroke-width="1.4"/></svg>
      </div>
      <div class="dc-info">
        <div class="dc-title">Jeu de Direction SHIS</div>
        <div class="dc-desc">Calculateur guidé · EC · ZS · IS · Tapered…</div>
        <div class="dc-meta">6 mesures · code SHIS haut + bas</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" onclick="showScreen('rayonnage')">
      <div class="dc-icon" style="background:rgba(45,127,249,.12)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#2d7ff9" stroke-width="1.8"/><line x1="14" y1="4" x2="14" y2="14" stroke="#2d7ff9" stroke-width="1.4"/><line x1="14" y1="14" x2="21" y2="8.5" stroke="#2d7ff9" stroke-width="1.4"/><line x1="14" y1="14" x2="7" y2="8.5" stroke="#2d7ff9" stroke-width="1.4"/><line x1="14" y1="14" x2="21" y2="19.5" stroke="#2d7ff9" stroke-width="1.4"/><line x1="14" y1="14" x2="7" y2="19.5" stroke="#2d7ff9" stroke-width="1.4"/><line x1="14" y1="14" x2="14" y2="24" stroke="#2d7ff9" stroke-width="1.4"/><circle cx="14" cy="14" r="2.5" fill="#2d7ff9"/></svg>
      </div>
      <div class="dc-info">
        <div class="dc-title">Calcul Rayonnage</div>
        <div class="dc-desc">Longueur des rayons · ERD · PCD · flancs</div>
        <div class="dc-meta">7 mesures · gauche &amp; droite</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" onclick="showScreen('braquet')">
      <div class="dc-icon" style="background:rgba(168,85,247,.12)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="9" stroke="#a855f7" stroke-width="1.8"/><circle cx="14" cy="14" r="4" stroke="#a855f7" stroke-width="1.4"/><circle cx="14" cy="5" r="2" fill="#a855f7"/><circle cx="23" cy="14" r="2" fill="#a855f7"/><circle cx="14" cy="23" r="2" fill="#a855f7"/><circle cx="5" cy="14" r="2" fill="#a855f7"/><path d="M20 6 L16 11" stroke="#a855f7" stroke-width="1.2" stroke-linecap="round"/></svg>
      </div>
      <div class="dc-info">
        <div class="dc-title">Braquet &amp; Développement</div>
        <div class="dc-desc">Plage · vitesse max · distance / tour de roue</div>
        <div class="dc-meta">Plateau · cassette · roue · cadence</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" onclick="showScreen('couples')">
      <div class="dc-icon" style="background:rgba(245,158,11,.12)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#f59e0b" stroke-width="1.8"/><circle cx="14" cy="14" r="4" stroke="#f59e0b" stroke-width="1.4"/><path d="M14 4 L14 8 M14 20 L14 24 M4 14 L8 14 M20 14 L24 14" stroke="#f59e0b" stroke-width="2" stroke-linecap="round"/><path d="M7 7 L10 10 M18 18 L21 21 M21 7 L18 10 M10 18 L7 21" stroke="#f59e0b" stroke-width="1.5" stroke-linecap="round"/></svg>
      </div>
      <div class="dc-info">
        <div class="dc-title" data-i18n="Couples de Serrage">Couples de Serrage</div>
        <div class="dc-desc" data-i18n="Shimano · SRAM · Campagnolo · Hope · Fox · RockShox">Shimano · SRAM · Campagnolo · Hope · Fox · RockShox</div>
        <div class="dc-meta" data-i18n="Freins · transmission · cadre · suspensions">Freins · transmission · cadre · suspensions</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" onclick="showScreen('compat')">
      <div class="dc-icon" style="background:rgba(34,197,94,.1)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="3" y="11" width="5" height="6" rx="1.5" stroke="#22c55e" stroke-width="1.5"/><rect x="11" y="11" width="5" height="6" rx="1.5" stroke="#22c55e" stroke-width="1.5"/><rect x="19" y="11" width="5" height="6" rx="1.5" stroke="#22c55e" stroke-width="1.5"/><line x1="8" y1="14" x2="11" y2="14" stroke="#22c55e" stroke-width="1.5"/><line x1="16" y1="14" x2="19" y2="14" stroke="#22c55e" stroke-width="1.5"/><path d="M6 6 L22 6 M6 22 L22 22" stroke="#22c55e" stroke-width="1.2" stroke-dasharray="3,2" stroke-linecap="round"/></svg>
      </div>
      <div class="dc-info">
        <div class="dc-title" data-i18n="Compatibilité Chaîne/Cassette">Compatibilité Chaîne/Cassette</div>
        <div class="dc-desc" data-i18n="Références · alertes croisées · largeurs">Références · alertes croisées · largeurs</div>
        <div class="dc-meta" data-i18n="Shimano · SRAM · Campagnolo · KMC">Shimano · SRAM · Campagnolo · KMC</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" onclick="showScreen('diagvelo')">
      <div class="dc-icon" style="background:rgba(34,197,94,.1)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M4 20 Q8 12 14 10 Q20 8 24 14" stroke="#22c55e" stroke-width="1.8" fill="none" stroke-linecap="round"/><circle cx="6" cy="20" r="4" stroke="#22c55e" stroke-width="1.6"/><circle cx="22" cy="20" r="4" stroke="#22c55e" stroke-width="1.6"/><circle cx="14" cy="13" r="2.5" stroke="#22c55e" stroke-width="1.4"/><path d="M14 15.5 L14 20" stroke="#22c55e" stroke-width="1.4" stroke-linecap="round"/><path d="M11 20 L17 20" stroke="#22c55e" stroke-width="1.4" stroke-linecap="round"/></svg>
      </div>
      <div class="dc-info">
        <div class="dc-title" data-i18n="Diagnostic Vélo">Diagnostic Vélo</div>
        <div class="dc-desc" data-i18n="Méthode en M · 13 points · rapport complet">Méthode en M · 13 points · rapport complet</div>
        <div class="dc-meta" data-i18n="Score général · export rapport">Score général · export rapport</div>
      </div><div class="dc-arr">›</div>
    </div>

    <!-- ── Référence ── -->
    <div class="sec-lbl-inner" style="margin:10px 0 4px" data-i18n="Référence">Référence</div>

    <div class="diag-card" onclick="showScreen('glossaire')">
      <div class="dc-icon" style="background:rgba(168,85,247,.1)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="4" y="4" width="16" height="20" rx="3" stroke="#a855f7" stroke-width="1.8"/><line x1="8" y1="10" x2="16" y2="10" stroke="#a855f7" stroke-width="1.4" stroke-linecap="round"/><line x1="8" y1="14" x2="16" y2="14" stroke="#a855f7" stroke-width="1.4" stroke-linecap="round"/><line x1="8" y1="18" x2="13" y2="18" stroke="#a855f7" stroke-width="1.4" stroke-linecap="round"/><circle cx="20" cy="20" r="5" fill="#a855f7" stroke="none"/><text x="20" y="24" text-anchor="middle" font-size="8" fill="white" font-family="monospace" font-weight="bold">?</text></svg>
      </div>
      <div class="dc-info">
        <div class="dc-title" data-i18n="Glossaire Technique">Glossaire Technique</div>
        <div class="dc-desc" data-i18n="50+ termes · FR/EN · standards expliqués">50+ termes · FR/EN · standards expliqués</div>
        <div class="dc-meta" data-i18n="Lexique atelier · utile pour les apprentis">Lexique atelier · utile pour les apprentis</div>
      </div><div class="dc-arr">›</div>
    </div>

    <!-- ── Atelier ── -->
    <div class="sec-lbl-inner" style="margin:10px 0 4px">Atelier</div>

    <div class="diag-card" onclick="showScreen('biblio')">
      <div class="dc-icon" style="background:rgba(45,127,249,.12)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
          <rect x="4" y="6" width="20" height="16" rx="3" stroke="#2d7ff9" stroke-width="1.8"/>
          <circle cx="11" cy="12" r="3" stroke="#2d7ff9" stroke-width="1.4"/>
          <path d="M4 20 Q8 14 11 14 Q14 14 18 18" stroke="#2d7ff9" stroke-width="1.4" fill="none" stroke-linecap="round"/>
          <line x1="18" y1="10" x2="22" y2="10" stroke="#2d7ff9" stroke-width="1.4" stroke-linecap="round"/>
          <line x1="18" y1="14" x2="22" y2="14" stroke="#2d7ff9" stroke-width="1.4" stroke-linecap="round"/>
        </svg>
      </div>
      <div class="dc-info">
        <div class="dc-title">Clients &amp; Vélos</div>
        <div class="dc-desc">Fiches clients · historique · contacts</div>
        <div class="dc-meta">Bibliothèque · recherche rapide</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" onclick="showScreen('historique')">
      <div class="dc-icon" style="background:rgba(168,85,247,.1)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
          <circle cx="14" cy="14" r="10" stroke="#a855f7" stroke-width="1.8"/>
          <polyline points="14,8 14,14 18,17" stroke="#a855f7" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>
      <div class="dc-info">
        <div class="dc-title">Historique</div>
        <div class="dc-desc">Diagnostics récents · rappeler une fiche</div>
        <div class="dc-meta">Sauvegarde locale · rapide</div>
      </div><div class="dc-arr">›</div>
    </div>

    <div class="diag-card" onclick="showScreen('devis')">
      <div class="dc-icon" style="background:rgba(34,197,94,.1)">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
          <rect x="5" y="4" width="18" height="20" rx="3" stroke="#22c55e" stroke-width="1.8"/>
          <line x1="9" y1="10" x2="19" y2="10" stroke="#22c55e" stroke-width="1.4" stroke-linecap="round"/>
          <line x1="9" y1="14" x2="19" y2="14" stroke="#22c55e" stroke-width="1.4" stroke-linecap="round"/>
          <line x1="9" y1="18" x2="15" y2="18" stroke="#22c55e" stroke-width="1.4" stroke-linecap="round"/>
          <circle cx="21" cy="21" r="5" fill="#22c55e"/>
          <text x="21" y="25" text-anchor="middle" font-size="7" fill="#fff" font-weight="bold">€</text>
        </svg>
      </div>
      <div class="dc-info">
        <div class="dc-title">Devis Rapide</div>
        <div class="dc-desc">Prestations · pièces · total TTC</div>
        <div class="dc-meta">Copier · partager · imprimer</div>
      </div><div class="dc-arr">›</div>
    </div>

  </div>
</div>

<!-- ══ SHIS CALCULATEUR ══ -->
<div class="screen" id="shis">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Jeu de Direction SHIS</div>
      <div class="dh-step">Haut + Bas · code généré automatiquement</div>
    </div>
  </div>

  <div class="sh-scroll" id="shisScroll">

    <!-- INTRO -->
    <div class="sh-banner">
      <div class="sh-banner-title">Code SHIS — 6 mesures au pied à coulisse</div>
      <div class="sh-banner-body">
        Le SHIS identifie votre jeu de direction. Il se compose de <strong>2 parties indépendantes</strong> : haut et bas du tube de direction. Chaque partie = <strong>Type + Ø douille + Ø pivot</strong>.<br>
        <span style="color:#f59e0b;font-size:11px">⚠️ Le haut et le bas peuvent avoir des types différents — ex : EC en haut, ZS en bas.</span>
      </div>
    </div>

    <!-- CODE LIVE -->
    <div class="sh-live">
      <div>
        <div class="sh-live-label">Code SHIS en cours</div>
        <div class="sh-live-code" id="shLiveCode">? — ?</div>
      </div>
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2" stroke="#2d7ff9" stroke-width="2" stroke-linecap="round"/></svg>
    </div>

    <!-- ══════ PARTIE HAUTE ══════ -->
    <div class="sh-bloc">
      <div class="sh-bloc-hdr">
        <div class="sh-bloc-icon top">H</div>
        <div class="sh-bloc-title">
          <div class="sh-bloc-name">Partie HAUTE — Top</div>
          <div class="sh-bloc-sub">Mesures ①②③ · en haut du tube de direction</div>
        </div>
      </div>

      <!-- Schéma EC/ZS/IS HAUT — directement visible -->
      <div class="sh-schema">
        <svg viewBox="0 0 320 140" fill="none" xmlns="http://www.w3.org/2000/svg">
          <!-- Tube de direction fond commun -->
          <rect x="128" y="10" width="64" height="128" rx="4" fill="#161616" stroke="#2a2a2a" stroke-width="2"/>

          <!-- EC — coupelle qui dépasse EN HAUT -->
          <ellipse cx="160" cy="18" rx="44" ry="11" fill="#e8400c" opacity=".9"/>
          <ellipse cx="160" cy="25" rx="44" ry="11" fill="#e8400c" opacity=".45"/>
          <line x1="208" y1="18" x2="248" y2="10" stroke="#e8400c" stroke-width="1.2" stroke-dasharray="3,2"/>
          <text x="252" y="8" font-size="11" fill="#e8400c" font-family="monospace" font-weight="bold">EC</text>
          <text x="252" y="20" font-size="8" fill="#e8400c" font-family="monospace">External Cup</text>
          <text x="252" y="31" font-size="8" fill="#9a9590" font-family="monospace">Cuvette externe</text>
          <text x="252" y="41" font-size="7" fill="#5a5550" font-family="monospace">dépasse du tube</text>

          <!-- ZS — dans un lamage, affleure -->
          <rect x="126" y="62" width="68" height="16" rx="3" fill="#2d7ff9" opacity=".8"/>
          <line x1="197" y1="70" x2="248" y2="58" stroke="#2d7ff9" stroke-width="1.2" stroke-dasharray="3,2"/>
          <text x="252" y="55" font-size="11" fill="#2d7ff9" font-family="monospace" font-weight="bold">ZS</text>
          <text x="252" y="67" font-size="8" fill="#2d7ff9" font-family="monospace">Zero Stack</text>
          <text x="252" y="78" font-size="8" fill="#9a9590" font-family="monospace">Semi-intégrée</text>
          <text x="252" y="88" font-size="7" fill="#5a5550" font-family="monospace">affleure le tube</text>

          <!-- IS — roulement dans le tube -->
          <ellipse cx="160" cy="118" rx="20" ry="13" fill="#22c55e" opacity=".7"/>
          <text x="57" y="112" font-size="11" fill="#22c55e" font-family="monospace" font-weight="bold">IS</text>
          <text x="42" y="124" font-size="8" fill="#22c55e" font-family="monospace">Integrated</text>
          <text x="44" y="135" font-size="8" fill="#9a9590" font-family="monospace">Intégrée</text>
          <text x="24" y="115" font-size="7" fill="#5a5550" font-family="monospace">roulement</text>
          <text x="28" y="124" font-size="7" fill="#5a5550" font-family="monospace">dans tube</text>
          <line x1="128" y1="118" x2="92" y2="118" stroke="#22c55e" stroke-width="1.2" stroke-dasharray="3,2"/>

          <!-- Séparateurs horizontaux -->
          <line x1="133" y1="48" x2="187" y2="48" stroke="#333" stroke-width="1" stroke-dasharray="4,3"/>
          <line x1="133" y1="95" x2="187" y2="95" stroke="#333" stroke-width="1" stroke-dasharray="4,3"/>
        </svg>
        <div class="sh-schema-caption">① — Type de cuvette supérieure</div>
      </div>

      <!-- ① TYPE HAUT -->
      <div class="sh-row-lbl">① Type de cuvette supérieure</div>
      <div class="sh-type-row" id="sh_type_haut">
        <button class="sh-type-btn" onclick="shSet('type_haut','EC',this,'top')">
          <span class="sh-type-code">EC</span>
          <span class="sh-type-fr">Cuvette externe</span>
          <span class="sh-type-en">External Cup</span>
        </button>
        <button class="sh-type-btn" onclick="shSet('type_haut','ZS',this,'top')">
          <span class="sh-type-code">ZS</span>
          <span class="sh-type-fr">Semi-intégrée</span>
          <span class="sh-type-en">Zero Stack</span>
        </button>
        <button class="sh-type-btn" onclick="shSet('type_haut','IS',this,'top')">
          <span class="sh-type-code">IS</span>
          <span class="sh-type-fr">Intégrée</span>
          <span class="sh-type-en">Integrated</span>
        </button>
      </div>

      <!-- ② Ø douille haut -->
      <div class="sh-schema">
        <svg viewBox="0 0 320 110" fill="none" xmlns="http://www.w3.org/2000/svg">
          <text x="160" y="12" text-anchor="middle" font-size="10" fill="#9a9590" font-family="monospace">② Ø INTÉRIEUR DOUILLE HAUTE</text>
          <rect x="90" y="18" width="140" height="80" rx="6" fill="#161616" stroke="#2a2a2a" stroke-width="2.5"/>
          <ellipse cx="160" cy="34" rx="56" ry="16" fill="none" stroke="#f59e0b" stroke-width="2" stroke-dasharray="5,3"/>
          <!-- pied à coulisse -->
          <line x1="106" y1="34" x2="214" y2="34" stroke="#2d7ff9" stroke-width="2" stroke-dasharray="4,3"/>
          <rect x="102" y="26" width="5" height="16" rx="2" fill="#2d7ff9"/>
          <rect x="213" y="26" width="5" height="16" rx="2" fill="#2d7ff9"/>
          <line x1="106" y1="58" x2="214" y2="58" stroke="#f59e0b" stroke-width="1.5"/>
          <line x1="106" y1="52" x2="106" y2="64" stroke="#f59e0b" stroke-width="2"/>
          <line x1="214" y1="52" x2="214" y2="64" stroke="#f59e0b" stroke-width="2"/>
          <text x="160" y="74" text-anchor="middle" font-size="10" fill="#f59e0b" font-family="monospace" font-weight="bold">Ø intérieur</text>
          <text x="160" y="92" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">EC/ZS : 30 · 34 · 37 · 41 · 44 · 49 mm</text>
          <text x="160" y="104" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">IS : 38 · 41 · 42 mm</text>
        </svg>
        <div class="sh-schema-caption">Pied à coulisse — mode mesure intérieure (becs inversés)</div>
      </div>

      <div class="sh-row-lbl">② Ø intérieur douille haute (mm)</div>
      <div class="sh-hint" id="sh_hint_dh">— Sélectionnez d'abord le type ① —</div>
      <div class="sh-vals-grid cols3" id="sh_diam_haut" style="display:none"></div>

      <!-- ③ Ø pivot haut -->
      <div class="sh-schema" style="margin-top:12px">
        <svg viewBox="0 0 320 120" fill="none" xmlns="http://www.w3.org/2000/svg">
          <text x="160" y="12" text-anchor="middle" font-size="10" fill="#9a9590" font-family="monospace">③ Ø EXTÉRIEUR COLONNE FOURCHE — HAUT</text>
          <rect x="135" y="18" width="50" height="95" rx="6" fill="#e8400c" opacity=".28" stroke="#e8400c" stroke-width="2"/>
          <text x="160" y="42" text-anchor="middle" font-size="9" fill="#e8400c" font-family="monospace">Colonne fourche</text>
          <line x1="70" y1="70" x2="250" y2="70" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="5,3"/>
          <line x1="135" y1="64" x2="135" y2="76" stroke="#2d7ff9" stroke-width="2.5"/>
          <line x1="185" y1="64" x2="185" y2="76" stroke="#2d7ff9" stroke-width="2.5"/>
          <text x="160" y="62" text-anchor="middle" font-size="9" fill="#2d7ff9" font-family="monospace" font-weight="bold">Ø ext. pivot haut</text>
          <text x="160" y="90" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">25.4 (1") · 28.6 (1⅛") · 31.8 (1¼") · 38.1 (1½")</text>
          <text x="160" y="106" text-anchor="middle" font-size="9" fill="#5a5550" font-family="monospace">28.6mm = standard · fourche tapered : haut reste 28.6</text>
        </svg>
        <div class="sh-schema-caption">Là où s'insère la potence</div>
      </div>

      <div class="sh-row-lbl">③ Ø pivot fourche haut (mm)</div>
      <div class="sh-vals-grid cols2" id="sh_pivot_haut">
        <button class="sh-val-btn" onclick="shSet('pivot_haut','25.4',this,'top')">
          <div class="sh-val-num">25.4</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1" — Vintage</div>
        </button>
        <button class="sh-val-btn" onclick="shSet('pivot_haut','28.6',this,'top')">
          <div class="sh-val-num">28.6</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1⅛" — Standard</div>
          <div class="sh-val-popular">★ Le + courant</div>
        </button>
        <button class="sh-val-btn" onclick="shSet('pivot_haut','31.8',this,'top')">
          <div class="sh-val-num">31.8</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1¼" — Rare</div>
        </button>
        <button class="sh-val-btn" onclick="shSet('pivot_haut','38.1',this,'top')">
          <div class="sh-val-num">38.1</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1½" — DH/Oversize</div>
        </button>
      </div>
    </div>

    <!-- Séparateur -->
    <div style="display:flex;align-items:center;gap:10px;margin:4px 0 12px">
      <div style="flex:1;height:1px;background:var(--border2)"></div>
      <div style="font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);background:var(--card);border:1px solid var(--border2);border-radius:20px;padding:3px 12px">— code —</div>
      <div style="flex:1;height:1px;background:var(--border2)"></div>
    </div>

    <!-- ══════ PARTIE BASSE ══════ -->
    <div class="sh-bloc">
      <div class="sh-bloc-hdr">
        <div class="sh-bloc-icon bot">B</div>
        <div class="sh-bloc-title">
          <div class="sh-bloc-name">Partie BASSE — Bottom</div>
          <div class="sh-bloc-sub">Mesures ④⑤⑥ · en bas du tube de direction</div>
        </div>
      </div>

      <!-- Schéma EC/ZS/IS BAS -->
      <div class="sh-schema">
        <svg viewBox="0 0 320 140" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect x="128" y="2" width="64" height="128" rx="4" fill="#161616" stroke="#2a2a2a" stroke-width="2"/>

          <!-- IS en haut du dessin = bas du tube -->
          <ellipse cx="160" cy="22" rx="20" ry="13" fill="#22c55e" opacity=".7"/>
          <text x="57" y="16" font-size="11" fill="#22c55e" font-family="monospace" font-weight="bold">IS</text>
          <text x="42" y="28" font-size="8" fill="#22c55e" font-family="monospace">Integrated</text>
          <text x="44" y="39" font-size="8" fill="#9a9590" font-family="monospace">Intégrée</text>
          <line x1="128" y1="22" x2="92" y2="22" stroke="#22c55e" stroke-width="1.2" stroke-dasharray="3,2"/>

          <!-- ZS -->
          <rect x="126" y="58" width="68" height="16" rx="3" fill="#2d7ff9" opacity=".8"/>
          <line x1="197" y1="66" x2="248" y2="56" stroke="#2d7ff9" stroke-width="1.2" stroke-dasharray="3,2"/>
          <text x="252" y="52" font-size="11" fill="#2d7ff9" font-family="monospace" font-weight="bold">ZS</text>
          <text x="252" y="64" font-size="8" fill="#2d7ff9" font-family="monospace">Zero Stack</text>
          <text x="252" y="75" font-size="8" fill="#9a9590" font-family="monospace">Semi-intégrée</text>

          <!-- EC — coupelle qui dépasse EN BAS -->
          <ellipse cx="160" cy="118" rx="44" ry="11" fill="#e8400c" opacity=".9"/>
          <ellipse cx="160" cy="111" rx="44" ry="11" fill="#e8400c" opacity=".4"/>
          <line x1="208" y1="118" x2="248" y2="125" stroke="#e8400c" stroke-width="1.2" stroke-dasharray="3,2"/>
          <text x="252" y="120" font-size="11" fill="#e8400c" font-family="monospace" font-weight="bold">EC</text>
          <text x="252" y="132" font-size="8" fill="#e8400c" font-family="monospace">External Cup</text>

          <line x1="133" y1="44" x2="187" y2="44" stroke="#333" stroke-width="1" stroke-dasharray="4,3"/>
          <line x1="133" y1="92" x2="187" y2="92" stroke="#333" stroke-width="1" stroke-dasharray="4,3"/>
        </svg>
        <div class="sh-schema-caption">④ — Type de cuvette inférieure</div>
      </div>

      <div style="background:rgba(245,158,11,.08);border:1px solid rgba(245,158,11,.2);border-radius:8px;padding:8px 10px;font-size:12px;color:#f59e0b;margin-bottom:10px">
        ⚠️ Indépendant du haut — peut être un type <strong>différent</strong> (ex : EC haut + ZS bas)
      </div>

      <!-- ④ TYPE BAS -->
      <div class="sh-row-lbl">④ Type de cuvette inférieure</div>
      <div class="sh-type-row" id="sh_type_bas">
        <button class="sh-type-btn" onclick="shSet('type_bas','EC',this,'bot')">
          <span class="sh-type-code">EC</span>
          <span class="sh-type-fr">Cuvette externe</span>
          <span class="sh-type-en">External Cup</span>
        </button>
        <button class="sh-type-btn" onclick="shSet('type_bas','ZS',this,'bot')">
          <span class="sh-type-code">ZS</span>
          <span class="sh-type-fr">Semi-intégrée</span>
          <span class="sh-type-en">Zero Stack</span>
        </button>
        <button class="sh-type-btn" onclick="shSet('type_bas','IS',this,'bot')">
          <span class="sh-type-code">IS</span>
          <span class="sh-type-fr">Intégrée</span>
          <span class="sh-type-en">Integrated</span>
        </button>
      </div>

      <!-- ⑤ Ø douille bas -->
      <div class="sh-schema" style="margin-top:4px">
        <svg viewBox="0 0 320 110" fill="none" xmlns="http://www.w3.org/2000/svg">
          <text x="160" y="12" text-anchor="middle" font-size="10" fill="#9a9590" font-family="monospace">⑤ Ø INTÉRIEUR DOUILLE BASSE</text>
          <rect x="90" y="18" width="140" height="80" rx="6" fill="#161616" stroke="#2a2a2a" stroke-width="2.5"/>
          <ellipse cx="160" cy="80" rx="62" ry="16" fill="none" stroke="#f59e0b" stroke-width="2" stroke-dasharray="5,3"/>
          <line x1="98" y1="80" x2="222" y2="80" stroke="#2d7ff9" stroke-width="2" stroke-dasharray="4,3"/>
          <rect x="94" y="72" width="5" height="16" rx="2" fill="#2d7ff9"/>
          <rect x="221" y="72" width="5" height="16" rx="2" fill="#2d7ff9"/>
          <line x1="98" y1="54" x2="222" y2="54" stroke="#f59e0b" stroke-width="1.5"/>
          <line x1="98" y1="48" x2="98" y2="60" stroke="#f59e0b" stroke-width="2"/>
          <line x1="222" y1="48" x2="222" y2="60" stroke="#f59e0b" stroke-width="2"/>
          <text x="160" y="43" text-anchor="middle" font-size="10" fill="#f59e0b" font-family="monospace" font-weight="bold">Ø intérieur bas</text>
          <text x="160" y="28" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">EC/ZS: 30·34·37·41·44·49·55·56mm</text>
          <text x="160" y="38" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">IS: 38·41·42·47·49·52mm</text>
        </svg>
        <div class="sh-schema-caption">Vue du bas · partie inférieure du tube de direction</div>
      </div>

      <div class="sh-row-lbl">⑤ Ø intérieur douille basse (mm)</div>
      <div class="sh-hint" id="sh_hint_db">— Sélectionnez d'abord le type ④ —</div>
      <div class="sh-vals-grid cols3" id="sh_diam_bas" style="display:none"></div>

      <!-- ⑥ Ø pivot bas -->
      <div class="sh-schema" style="margin-top:12px">
        <svg viewBox="0 0 320 140" fill="none" xmlns="http://www.w3.org/2000/svg">
          <text x="160" y="12" text-anchor="middle" font-size="10" fill="#9a9590" font-family="monospace">⑥ Ø PORTÉE CONIQUE BASSE — FOURCHE</text>
          <!-- Colonne haut droite -->
          <rect x="143" y="16" width="34" height="52" rx="4" fill="#e8400c" opacity=".22" stroke="#e8400c" stroke-width="1.5"/>
          <text x="160" y="36" text-anchor="middle" font-size="8" fill="#e8400c" font-family="monospace">28.6mm</text>
          <!-- Portée conique évasée -->
          <path d="M138 68 L98 118 L222 118 L182 68 Z" fill="#f59e0b" opacity=".18" stroke="#f59e0b" stroke-width="2"/>
          <text x="160" y="86" text-anchor="middle" font-size="8" fill="#f59e0b" font-family="monospace">Portée conique</text>
          <!-- Mesure -->
          <line x1="60" y1="104" x2="260" y2="104" stroke="#f59e0b" stroke-width="1.5" stroke-dasharray="4,3"/>
          <line x1="98" y1="98" x2="98" y2="110" stroke="#f59e0b" stroke-width="2.5"/>
          <line x1="222" y1="98" x2="222" y2="110" stroke="#f59e0b" stroke-width="2.5"/>
          <text x="160" y="97" text-anchor="middle" font-size="9" fill="#f59e0b" font-family="monospace" font-weight="bold">Ø portée bas</text>
          <!-- Valeurs -->
          <text x="52" y="130" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">26mm</text>
          <text x="52" y="139" text-anchor="middle" font-size="7" fill="#5a5550" font-family="monospace">1" vintage</text>
          <text x="120" y="130" text-anchor="middle" font-size="9" fill="#e8400c" font-family="monospace">30mm</text>
          <text x="120" y="139" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace">1⅛" droit</text>
          <text x="200" y="130" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">33mm</text>
          <text x="200" y="139" text-anchor="middle" font-size="7" fill="#5a5550" font-family="monospace">1¼"</text>
          <text x="270" y="130" text-anchor="middle" font-size="9" fill="#2d7ff9" font-family="monospace">40mm</text>
          <text x="270" y="139" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace">1½" tapered</text>
        </svg>
        <div class="sh-schema-caption">Crown Race Seat — là où s'insère le cône inférieur</div>
      </div>

      <div class="sh-row-lbl">⑥ Ø portée fourche basse (mm)</div>
      <div class="sh-vals-grid cols2" id="sh_pivot_bas">
        <button class="sh-val-btn" onclick="shSet('pivot_bas','26',this,'bot')">
          <div class="sh-val-num">26</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1" — Vintage</div>
        </button>
        <button class="sh-val-btn" onclick="shSet('pivot_bas','30',this,'bot')">
          <div class="sh-val-num">30</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1⅛" — Fourche droite</div>
          <div class="sh-val-popular">★ Droit</div>
        </button>
        <button class="sh-val-btn" onclick="shSet('pivot_bas','33',this,'bot')">
          <div class="sh-val-num">33</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1¼" — Interméd.</div>
        </button>
        <button class="sh-val-btn" onclick="shSet('pivot_bas','40',this,'bot')">
          <div class="sh-val-num">40</div><div class="sh-val-mm">mm</div>
          <div class="sh-val-desc">1½" — Conique tapered</div>
          <div class="sh-val-popular">★ Tapered</div>
        </button>
      </div>
    </div>

    <!-- ══════ RÉSULTAT ══════ -->
    <div class="sh-result" id="shResult">
      <div class="sh-result-lbl">✓ Code SHIS identifié</div>
      <div class="sh-result-code" id="shCode">—</div>
      <div class="sh-result-halves" id="shHalves"></div>
      <div class="sh-diag" id="shDiag"></div>
      <div class="sh-btns">
        <button class="sh-copy" onclick="shCopy()" id="shCopyBtn">📋 Copier le code</button>
        <button class="sh-reset" onclick="shReset()">↺ Réinitialiser</button>
      </div>
      <div class="sh-alert">
        ⚠️ <strong>Avant commande :</strong> vérifiez la hauteur de cuvette, l'angle de contact des roulements (36/45° ou 45/45°), la compatibilité fabricant et la référence exacte.
      </div>
    </div>

  </div>
</div>
<div class="screen" id="rayonnage">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Calcul Rayonnage</div>
      <div class="dh-step">Longueur des rayons</div>
    </div>
  </div>
  <div class="ray-scroll" id="rayScroll">

    <!-- SECTION JANTE -->
    <div class="ray-section">
      <div class="ray-sec-title">🔵 Jante</div>

      <div class="ray-field">
        <div class="ray-field-lbl">ERD — Diamètre efficace <button class="info-btn" onclick="showTip('erd')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_erd" placeholder="584" inputmode="decimal"><span class="ray-unit">mm</span></div>
      </div>
      <div class="ray-field">
        <div class="ray-field-lbl">Nombre de trous jante <button class="info-btn" onclick="showTip('trous')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_trous" placeholder="32" inputmode="numeric"><span class="ray-unit">trous</span></div>
      </div>
      <div class="ray-field">
        <div class="ray-field-lbl">Offset jante (OSB) <button class="info-btn" onclick="showTip('osb')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_osb" placeholder="0" inputmode="decimal"><span class="ray-unit">mm</span></div>
      </div>
    </div>

    <!-- SECTION MOYEU -->
    <div class="ray-section">
      <div class="ray-sec-title">⚙️ Moyeu</div>

      <div class="ray-field">
        <div class="ray-field-lbl">OLD — Largeur totale moyeu <button class="info-btn" onclick="showTip('old')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_old" placeholder="100" inputmode="decimal"><span class="ray-unit">mm</span></div>
      </div>
      <div class="ray-field">
        <div class="ray-field-lbl">PCD Gauche — Ø cercle trous flanc G <button class="info-btn" onclick="showTip('pcdg')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_pcdg" placeholder="46" inputmode="decimal"><span class="ray-unit">mm</span></div>
      </div>
      <div class="ray-field">
        <div class="ray-field-lbl">PCD Droit — Ø cercle trous flanc D <button class="info-btn" onclick="showTip('pcdd')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_pcdd" placeholder="46" inputmode="decimal"><span class="ray-unit">mm</span></div>
      </div>
      <div class="ray-field">
        <div class="ray-field-lbl">Distance flanc G → plan central <button class="info-btn" onclick="showTip('dg')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_dg" placeholder="35" inputmode="decimal"><span class="ray-unit">mm</span></div>
      </div>
      <div class="ray-field">
        <div class="ray-field-lbl">Distance flanc D → plan central <button class="info-btn" onclick="showTip('dd')">i</button></div>
        <div class="ray-inp-row"><input class="ray-inp" type="number" id="r_dd" placeholder="17" inputmode="decimal"><span class="ray-unit">mm</span></div>
      </div>
    </div>

    <!-- SECTION CONFIGURATION -->
    <div class="ray-section">
      <div class="ray-sec-title">🔧 Configuration</div>

      <div class="ray-field">
        <div class="ray-field-lbl">Croisements <button class="info-btn" onclick="showTip('crois')">i</button></div>
        <select class="ray-select" id="r_cross">
          <option value="0">Radial (0 croisement)</option>
          <option value="1">1 croisement</option>
          <option value="2">2 croisements</option>
          <option value="3" selected>3 croisements</option>
          <option value="4">4 croisements</option>
        </select>
      </div>
      <div class="ray-field">
        <div class="ray-field-lbl">Trou de départ rayon <button class="info-btn" onclick="showTip('trou')">i</button></div>
        <select class="ray-select" id="r_hole">
          <option value="1" selected>Premier trou (standard)</option>
          <option value="2">Deuxième trou</option>
        </select>
      </div>
    </div>

    <div class="ray-result" id="rayResult">
      <div class="ray-result-title">📐 Longueurs calculées</div>
      <div class="ray-lengths" id="rayLengths"></div>
      <div class="ray-detail-grid" id="rayDetails"></div>
      <div class="tip-bar" style="margin-top:10px">
        ⚠️ Arrondissez au <strong>demi-millimètre inférieur</strong>. Commandez ±1mm pour avoir du choix.
      </div>
    </div>

    <!-- TENSION RAYONS -->
    <div class="ray-tension-card" id="rayTensionCard">
      <div class="ray-tension-title">⚡ Tension estimée des rayons</div>
      <div class="tension-grid" id="tensionGrid"></div>
      <div id="tensionRatio"></div>
      <div class="tip-bar" style="margin-top:10px">
        🎯 Mesurez avec un <strong>tensiomètre</strong> (ex : Park Tool TM-1). La valeur calculée est une estimation théorique.
      </div>
    </div>

  </div>
  <button class="ray-calc-btn" onclick="calcRayonnage()">
    <svg width="20" height="20" fill="none" viewBox="0 0 24 24"><path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" stroke="#fff" stroke-width="2" stroke-linecap="round"/></svg>
    Calculer les longueurs
  </button>
</div>

<!-- ══ SHIS CALCULATEUR ══ -->

<!-- ══ BRAQUET & DÉVELOPPEMENT ══ -->
<div class="screen" id="braquet">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Braquet &amp; Développement</div>
      <div class="dh-step">Développement · vitesse · plage · écarts</div>
    </div>
  </div>

  <div class="ray-scroll" id="braqScroll">

    <!-- ══ ROUE ══ -->
    <div class="ray-section">
      <div class="ray-sec-title">🔵 Roue</div>

      <div class="ray-field">
        <div class="ray-field-lbl">Format de roue (présélection)</div>
        <select class="ray-select" id="br_preset" onchange="braqPreset()">
          <option value="">— Choisir un format commun —</option>
          <optgroup label="700C / 29&quot; (BSD 622)">
            <option value="622:23">700C pneu 23mm</option>
            <option value="622:25">700C pneu 25mm</option>
            <option value="622:28">700C pneu 28mm</option>
            <option value="622:32">700C pneu 32mm</option>
            <option value="622:38">700C pneu 38mm</option>
            <option value="622:47">700C pneu 47mm (gravel)</option>
            <option value="622:54">29&quot; pneu 2.1&quot; (54mm)</option>
            <option value="622:57">29&quot; pneu 2.25&quot; (57mm)</option>
            <option value="622:62">29&quot; pneu 2.4&quot; (62mm)</option>
          </optgroup>
          <optgroup label="27.5&quot; / 650B (BSD 584)">
            <option value="584:50">27.5&quot; pneu 2.0&quot; (50mm)</option>
            <option value="584:57">27.5&quot; pneu 2.25&quot; (57mm)</option>
            <option value="584:62">27.5&quot; pneu 2.4&quot; (62mm)</option>
          </optgroup>
          <optgroup label="26&quot; (BSD 559)">
            <option value="559:50">26&quot; pneu 2.0&quot; (50mm)</option>
            <option value="559:57">26&quot; pneu 2.25&quot; (57mm)</option>
          </optgroup>
          <option value="custom">⚙️ Saisie manuelle</option>
        </select>
      </div>

      <div class="braq-deux-cols">
        <div class="ray-field">
          <div class="ray-field-lbl">Ø jante (BSD mm)</div>
          <div class="ray-inp-row">
            <input class="ray-inp" type="number" id="br_diam_jante" placeholder="622" inputmode="numeric" oninput="braqCompute()">
            <span class="ray-unit">mm</span>
          </div>
        </div>
        <div class="ray-field">
          <div class="ray-field-lbl">Largeur pneu (mm)</div>
          <div class="ray-inp-row">
            <input class="ray-inp" type="number" id="br_larg_pneu" placeholder="28" inputmode="numeric" oninput="braqCompute()">
            <span class="ray-unit">mm</span>
          </div>
        </div>
      </div>

      <!-- Circonférence calculée -->
      <div class="braq-circ-display" id="braqCircDisplay" style="display:none">
        <span class="braq-circ-label">Circonférence calculée</span>
        <span class="braq-circ-val" id="braqCircVal">—</span>
        <span class="braq-circ-unit">mm</span>
      </div>
    </div>

    <!-- ══ PLATEAUX ══ -->
    <div class="ray-section">
      <div class="ray-sec-title">🔴 Plateau(x)</div>
      <div class="ray-field">
        <div class="ray-field-lbl">Dents plateau(x) <span style="color:var(--muted);font-size:9px">(virgule si plusieurs)</span></div>
        <input class="ray-inp braq-text-inp" type="text" id="br_plateaux"
          placeholder="ex: 50,34  ou  42  ou  44,30" inputmode="text" oninput="braqCompute()">
      </div>

      <!-- Présets plateaux communs -->
      <div class="braq-presets-row">
        <button class="braq-preset-tag" onclick="braqSetPl('50,34')">50/34</button>
        <button class="braq-preset-tag" onclick="braqSetPl('52,36')">52/36</button>
        <button class="braq-preset-tag" onclick="braqSetPl('53,39')">53/39</button>
        <button class="braq-preset-tag" onclick="braqSetPl('46,30')">46/30</button>
        <button class="braq-preset-tag" onclick="braqSetPl('40')">40×</button>
        <button class="braq-preset-tag" onclick="braqSetPl('42')">42×</button>
        <button class="braq-preset-tag" onclick="braqSetPl('44')">44×</button>
      </div>
    </div>

    <!-- ══ CASSETTE ══ -->
    <div class="ray-section">
      <div class="ray-sec-title">⚙️ Cassette</div>
      <div class="braq-deux-cols">
        <div class="ray-field">
          <div class="ray-field-lbl">Petit pignon (min)</div>
          <div class="ray-inp-row">
            <input class="ray-inp" type="number" id="br_pig_min" placeholder="11" inputmode="numeric" oninput="braqCompute()">
            <span class="ray-unit">T</span>
          </div>
        </div>
        <div class="ray-field">
          <div class="ray-field-lbl">Grand pignon (max)</div>
          <div class="ray-inp-row">
            <input class="ray-inp" type="number" id="br_pig_max" placeholder="34" inputmode="numeric" oninput="braqCompute()">
            <span class="ray-unit">T</span>
          </div>
        </div>
      </div>

      <!-- Présets cassettes communs -->
      <div class="braq-presets-row">
        <button class="braq-preset-tag" onclick="braqSetCas('11','28')">11-28</button>
        <button class="braq-preset-tag" onclick="braqSetCas('11','30')">11-30</button>
        <button class="braq-preset-tag" onclick="braqSetCas('11','32')">11-32</button>
        <button class="braq-preset-tag" onclick="braqSetCas('11','34')">11-34</button>
        <button class="braq-preset-tag" onclick="braqSetCas('10','42')">10-42</button>
        <button class="braq-preset-tag" onclick="braqSetCas('10','50')">10-50</button>
        <button class="braq-preset-tag" onclick="braqSetCas('10','51')">10-51</button>
      </div>
    </div>

    <!-- ══ CADENCE ══ -->
    <div class="ray-section">
      <div class="ray-sec-title">🔄 Cadence</div>
      <div class="ray-field">
        <div class="braq-cad-row" id="br_cad_row">
          <button class="braq-cad-btn" onclick="braqSetCad(70,this)">70<span>rpm</span><small>Grimpe</small></button>
          <button class="braq-cad-btn active" onclick="braqSetCad(90,this)">90<span>rpm</span><small>Standard</small></button>
          <button class="braq-cad-btn" onclick="braqSetCad(100,this)">100<span>rpm</span><small>Véloce</small></button>
          <button class="braq-cad-btn" onclick="braqSetCad(110,this)">110<span>rpm</span><small>Sprint</small></button>
        </div>
        <div class="ray-inp-row" style="margin-top:10px">
          <input class="ray-inp" type="number" id="br_cadence" value="90" placeholder="90"
            inputmode="numeric" style="font-size:26px" oninput="braqClearCadBtns();braqCompute()">
          <span class="ray-unit">rpm</span>
        </div>
      </div>
    </div>

    <!-- ══ RÉSULTATS ══ -->
    <div class="braq-result" id="braqResult" style="display:none">

      <!-- 1. Circonférence + Plage globale -->
      <div class="braq-res-block">
        <div class="braq-res-title">📐 Paramètres roue</div>
        <div class="braq-kpi-row">
          <div class="braq-kpi">
            <div class="braq-kpi-lbl">Circonférence</div>
            <div class="braq-kpi-val" id="res_circ">—</div>
            <div class="braq-kpi-unit">mm</div>
          </div>
          <div class="braq-kpi">
            <div class="braq-kpi-lbl">Braquet MIN</div>
            <div class="braq-kpi-val" id="res_braq_min">—</div>
            <div class="braq-kpi-unit">ratio</div>
          </div>
          <div class="braq-kpi">
            <div class="braq-kpi-lbl">Braquet MAX</div>
            <div class="braq-kpi-val" id="res_braq_max">—</div>
            <div class="braq-kpi-unit">ratio</div>
          </div>
        </div>
      </div>

      <!-- 2. Développement min/max -->
      <div class="braq-res-block">
        <div class="braq-res-title">📏 Développement</div>
        <div class="braq-dev-grid">
          <div class="braq-dev-card" style="border-left:3px solid var(--gold)">
            <div class="braq-dev-label">MIN</div>
            <div class="braq-dev-val" id="res_dev_min">—</div>
            <div class="braq-dev-unit">m / tour pédalier</div>
            <div class="braq-dev-sub" id="res_dev_min_combo"></div>
          </div>
          <div class="braq-dev-card" style="border-left:3px solid var(--green)">
            <div class="braq-dev-label">MAX</div>
            <div class="braq-dev-val" id="res_dev_max">—</div>
            <div class="braq-dev-unit">m / tour pédalier</div>
            <div class="braq-dev-sub" id="res_dev_max_combo"></div>
          </div>
        </div>
        <!-- Plage de transmission -->
        <div class="braq-plage-bar">
          <div class="braq-plage-row">
            <span class="braq-plage-lbl">Plage de transmission</span>
            <span class="braq-plage-val" id="res_plage">—×</span>
          </div>
          <div class="braq-plage-row">
            <span class="braq-plage-lbl">Plage en %</span>
            <span class="braq-plage-val" id="res_plage_pct">—</span>
          </div>
          <div class="braq-gauge-wrap">
            <div class="braq-gauge-bar" id="res_gauge"></div>
          </div>
          <div class="braq-plage-hint" id="res_plage_hint"></div>
        </div>
      </div>

      <!-- 3. Vitesses à la cadence choisie -->
      <div class="braq-res-block">
        <div class="braq-res-title">🚀 Vitesses à <span id="res_cad_lbl">90</span> rpm</div>
        <div id="res_vitesses"></div>
      </div>

      <!-- 4. Tableau développements complet -->
      <div class="braq-res-block">
        <div class="braq-res-title">📊 Développements complets (m)</div>
        <div style="overflow-x:auto" id="res_tableau"></div>
        <div style="font-size:11px;color:var(--muted);margin-top:6px;display:flex;gap:12px">
          <span><span style="color:var(--green)">■</span> Meilleur braquet</span>
          <span><span style="color:var(--gold)">■</span> Braquet min</span>
        </div>
      </div>

      <!-- 5. Écarts entre pignons -->
      <div class="braq-res-block">
        <div class="braq-res-title">📉 Écart entre pignons adjacents (%)</div>
        <div id="res_ecarts"></div>
        <div style="font-size:11px;color:var(--muted);margin-top:6px">
          Écart idéal : 10–15% · &lt;10% = serré · &gt;20% = saut perceptible
        </div>
      </div>

      <!-- Formule rappel -->
      <div class="tip-bar" style="margin-top:12px">
        💡 Dév. = (plateau÷pignon) × π × (Ø jante + 2×larg. pneu) / 1000
        · Vitesse = Dév. × cadence × 60 / 1000
      </div>

      <button class="braq-reset-full" onclick="braqReset()">↺ Réinitialiser</button>
    </div>

    <div style="height:30px"></div>
  </div>
</div>

<!-- ══ DIAGNOSTIC VÉLO ══ -->
<div class="screen" id="diagvelo">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Diagnostic Vélo</div>
      <div class="dh-step">Méthode en M · 13 points de contrôle</div>
    </div>
    <button class="dv-export-btn" onclick="dvExport()">↗ Rapport</button>
  </div>

  <div class="dv-scroll" id="dvScroll">

    <!-- ── FICHE CLIENT / VÉLO ── -->
    <div class="dv-fiche-card">
      <div class="dv-fiche-title">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2" stroke="#2d7ff9" stroke-width="2" stroke-linecap="round"/><circle cx="12" cy="7" r="4" stroke="#2d7ff9" stroke-width="2"/></svg>
        Fiche Client &amp; Vélo
        <button class="dv-fiche-toggle" id="dvFicheToggle" onclick="dvToggleFiche()">▾</button>
      </div>
      <div id="dvFicheBody">
        <!-- Client -->
        <div class="dv-fiche-section">CLIENT</div>
        <div class="dv-fiche-row">
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Nom</div>
            <input class="dv-fiche-inp" type="text" id="dvClientNom" placeholder="Dupont" autocomplete="family-name">
          </div>
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Prénom</div>
            <input class="dv-fiche-inp" type="text" id="dvClientPrenom" placeholder="Jean" autocomplete="given-name">
          </div>
        </div>
        <div class="dv-fiche-field" style="margin-bottom:8px">
          <div class="dv-fiche-lbl">Téléphone</div>
          <input class="dv-fiche-inp" type="tel" id="dvClientTel" placeholder="06 12 34 56 78" autocomplete="tel">
        </div>
        <div class="dv-fiche-field" style="margin-bottom:8px">
          <div class="dv-fiche-lbl">Email</div>
          <input class="dv-fiche-inp" type="email" id="dvClientMail" placeholder="jean.dupont@mail.com" autocomplete="email">
        </div>

        <!-- Vélo -->
        <div class="dv-fiche-section" style="margin-top:10px">VÉLO</div>
        <div class="dv-fiche-row">
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Marque</div>
            <input class="dv-fiche-inp" type="text" id="dvVeloMarque" placeholder="Trek, Specialized…">
          </div>
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Modèle</div>
            <input class="dv-fiche-inp" type="text" id="dvVeloModele" placeholder="Domane, Tarmac…">
          </div>
        </div>
        <div class="dv-fiche-row">
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Année</div>
            <input class="dv-fiche-inp" type="number" id="dvVeloAnnee" placeholder="2023" inputmode="numeric" maxlength="4">
          </div>
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Couleur</div>
            <input class="dv-fiche-inp" type="text" id="dvVeloCouleur" placeholder="Noir mat…">
          </div>
        </div>
        <div class="dv-fiche-field" style="margin-bottom:8px">
          <div class="dv-fiche-lbl">N° de série</div>
          <input class="dv-fiche-inp" type="text" id="dvVeloSerie" placeholder="Gravé sous le boîtier…" style="font-family:'JetBrains Mono',monospace;font-size:13px">
        </div>
        <div class="dv-fiche-row">
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Kilométrage</div>
            <input class="dv-fiche-inp" type="number" id="dvVeloKm" placeholder="3500" inputmode="numeric">
          </div>
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Type</div>
            <select class="dv-fiche-select" id="dvVeloType">
              <option value="">—</option>
              <option>Route</option>
              <option>Gravel</option>
              <option>VTT XC</option>
              <option>VTT Trail</option>
              <option>VTT Enduro</option>
              <option>VTT DH</option>
              <option>E-Bike</option>
              <option>Ville / Trekking</option>
              <option>BMX</option>
              <option>Autre</option>
            </select>
          </div>
        </div>

        <!-- Atelier -->
        <div class="dv-fiche-section" style="margin-top:10px">ATELIER</div>
        <div class="dv-fiche-row">
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Mécanicien</div>
            <input class="dv-fiche-inp" type="text" id="dvMeca" placeholder="Nom du mécanicien">
          </div>
          <div class="dv-fiche-field">
            <div class="dv-fiche-lbl">Date</div>
            <input class="dv-fiche-inp" type="date" id="dvDate">
          </div>
        </div>
        <div class="dv-fiche-field">
          <div class="dv-fiche-lbl">Atelier / Boutique</div>
          <input class="dv-fiche-inp" type="text" id="dvAtelier" placeholder="Nom de l'atelier">
        </div>
      </div>
    </div>

    <!-- ── SCORE GLOBAL ── -->
    <div class="dv-score-card">
      <div class="dv-score-left">
        <div class="dv-score-num" id="dvScore">—</div>
        <div class="dv-score-label">Score vélo</div>
      </div>
      <div class="dv-score-stats">
        <div class="dv-stat"><span class="dv-stat-val" id="dvNbOk">0</span><span class="dv-stat-lbl">OK</span></div>
        <div class="dv-stat"><span class="dv-stat-val dv-yellow" id="dvNbWarn">0</span><span class="dv-stat-lbl">Surveill.</span></div>
        <div class="dv-stat"><span class="dv-stat-val dv-orange" id="dvNbFix">0</span><span class="dv-stat-lbl">Régler</span></div>
        <div class="dv-stat"><span class="dv-stat-val dv-red" id="dvNbReplace">0</span><span class="dv-stat-lbl">Remplacer</span></div>
        <div class="dv-stat"><span class="dv-stat-val dv-purple" id="dvNbDanger">0</span><span class="dv-stat-lbl">Danger</span></div>
      </div>
    </div>

    <!-- Barre de progression -->
    <div class="dv-prog-wrap">
      <div class="dv-prog-bar"><div class="dv-prog-fill" id="dvProgFill"></div></div>
      <div class="dv-prog-txt" id="dvProgTxt">0 / 13 points contrôlés</div>
    </div>

    <!-- ── SCHÉMA VÉLO INTERACTIF ── -->
    <div class="dv-schema-card">
      <div class="dv-schema-title">Schéma interactif — touchez une zone pour accéder au point</div>
      <div class="dv-bike-wrap">
        <svg id="dvBikeSvg" viewBox="0 0 420 245" fill="none" xmlns="http://www.w3.org/2000/svg">

          <!-- ══ FOND SOL ══ -->
          <line x1="30" y1="218" x2="390" y2="218" stroke="#252525" stroke-width="1.5"/>

          <!-- ══ ROUE ARRIÈRE ══ -->
          <!-- Pneu -->
          <circle cx="100" cy="170" r="48" stroke="#3a3a3a" stroke-width="7"/>
          <!-- Jante -->
          <circle cx="100" cy="170" r="38" stroke="#4d4d4d" stroke-width="2.5"/>
          <!-- Fond de jante -->
          <circle cx="100" cy="170" r="34" stroke="#2e2e2e" stroke-width="1"/>
          <!-- Rayons (12) -->
          <g stroke="#353535" stroke-width="1.2">
            <line x1="100" y1="132" x2="100" y2="158"/><line x1="100" y1="182" x2="100" y2="208"/>
            <line x1="62" y1="170" x2="88" y2="170"/><line x1="112" y1="170" x2="138" y2="170"/>
            <line x1="73" y1="143" x2="92" y2="162"/><line x1="108" y1="178" x2="127" y2="197"/>
            <line x1="127" y1="143" x2="108" y2="162"/><line x1="92" y1="178" x2="73" y2="197"/>
            <line x1="65" y1="153" x2="88" y2="164"/><line x1="112" y1="176" x2="135" y2="187"/>
            <line x1="135" y1="153" x2="112" y2="164"/><line x1="88" y1="176" x2="65" y2="187"/>
          </g>
          <!-- Moyeu AR -->
          <circle cx="100" cy="170" r="7" fill="#2a2a2a" stroke="#5a5a5a" stroke-width="2"/>
          <circle cx="100" cy="170" r="3" fill="#444"/>

          <!-- ══ ROUE AVANT ══ -->
          <circle cx="320" cy="170" r="48" stroke="#3a3a3a" stroke-width="7"/>
          <circle cx="320" cy="170" r="38" stroke="#4d4d4d" stroke-width="2.5"/>
          <circle cx="320" cy="170" r="34" stroke="#2e2e2e" stroke-width="1"/>
          <g stroke="#353535" stroke-width="1.2">
            <line x1="320" y1="132" x2="320" y2="158"/><line x1="320" y1="182" x2="320" y2="208"/>
            <line x1="282" y1="170" x2="308" y2="170"/><line x1="332" y1="170" x2="358" y2="170"/>
            <line x1="293" y1="143" x2="312" y2="162"/><line x1="328" y1="178" x2="347" y2="197"/>
            <line x1="347" y1="143" x2="328" y2="162"/><line x1="312" y1="178" x2="293" y2="197"/>
            <line x1="285" y1="153" x2="308" y2="164"/><line x1="332" y1="176" x2="355" y2="187"/>
            <line x1="355" y1="153" x2="332" y2="164"/><line x1="308" y1="176" x2="285" y2="187"/>
          </g>
          <circle cx="320" cy="170" r="7" fill="#2a2a2a" stroke="#5a5a5a" stroke-width="2"/>
          <circle cx="320" cy="170" r="3" fill="#444"/>

          <!-- ══ CADRE (traits épais, géométrie route) ══ -->
          <!-- Tube supérieur -->
          <line x1="208" y1="88" x2="300" y2="100" stroke="#585858" stroke-width="8" stroke-linecap="round"/>
          <!-- Tube de selle -->
          <line x1="178" y1="170" x2="208" y2="88" stroke="#585858" stroke-width="8" stroke-linecap="round"/>
          <!-- Tube diagonal (down tube) -->
          <line x1="178" y1="170" x2="300" y2="100" stroke="#525252" stroke-width="6.5" stroke-linecap="round"/>
          <!-- Haubans (chainstays diagonaux) -->
          <line x1="100" y1="170" x2="206" y2="92" stroke="#4a4a4a" stroke-width="4" stroke-linecap="round"/>
          <line x1="100" y1="170" x2="210" y2="96" stroke="#4a4a4a" stroke-width="3.5" stroke-linecap="round"/>
          <!-- Bases (seat stays) -->
          <line x1="100" y1="170" x2="178" y2="170" stroke="#585858" stroke-width="8" stroke-linecap="round"/>

          <!-- ══ FOURCHE ══ -->
          <line x1="300" y1="100" x2="322" y2="168" stroke="#505050" stroke-width="5.5" stroke-linecap="round"/>
          <line x1="296" y1="103" x2="318" y2="168" stroke="#474747" stroke-width="4" stroke-linecap="round"/>

          <!-- ══ BOÎTE DE DIRECTION ══ -->
          <rect x="295" y="93" width="13" height="30" rx="4" fill="#3e3e3e" stroke="#6a6a6a" stroke-width="1.5"/>

          <!-- ══ POTENCE ══ -->
          <line x1="302" y1="98" x2="307" y2="78" stroke="#5a5a5a" stroke-width="5" stroke-linecap="round"/>
          <!-- Cintre route (drops) -->
          <path d="M282 72 Q294 68 307 70 Q318 72 323 80 Q326 86 322 92" stroke="#5a5a5a" stroke-width="4.5" fill="none" stroke-linecap="round"/>
          <path d="M282 72 Q278 80 282 88" stroke="#5a5a5a" stroke-width="4.5" fill="none" stroke-linecap="round"/>
          <!-- Guidoline -->
          <path d="M283 72 Q294 68 307 70 Q318 72 323 80" stroke="#3a3a3a" stroke-width="2" fill="none" stroke-linecap="round"/>

          <!-- ══ TIGE + SELLE ══ -->
          <line x1="207" y1="92" x2="207" y2="68" stroke="#5a5a5a" stroke-width="5" stroke-linecap="round"/>
          <!-- Selle (profil route) -->
          <path d="M188 64 Q198 59 207 60 Q218 59 224 63 Q228 66 224 68 Q216 71 207 70 Q196 71 188 67 Z" fill="#4a4a4a" stroke="#6e6e6e" stroke-width="1.5"/>

          <!-- ══ PÉDALIER ══ -->
          <!-- Plateau grand -->
          <circle cx="178" cy="170" r="26" stroke="#565656" stroke-width="2.5"/>
          <!-- Plateau petit -->
          <circle cx="178" cy="170" r="18" stroke="#484848" stroke-width="1.5"/>
          <!-- Corps BB -->
          <circle cx="178" cy="170" r="8" fill="#383838" stroke="#686868" stroke-width="1.8"/>
          <!-- Manivelle D -->
          <line x1="178" y1="144" x2="178" y2="134" stroke="#666" stroke-width="5.5" stroke-linecap="round"/>
          <rect x="171" y="128" width="14" height="6" rx="2.5" fill="#555" stroke="#888" stroke-width="1"/>
          <!-- Manivelle G -->
          <line x1="178" y1="196" x2="178" y2="206" stroke="#666" stroke-width="5.5" stroke-linecap="round"/>
          <rect x="171" y="206" width="14" height="6" rx="2.5" fill="#555" stroke="#888" stroke-width="1"/>

          <!-- ══ CHAÎNE ══ -->
          <line x1="176" y1="152" x2="114" y2="160" stroke="#4a4a4a" stroke-width="3" stroke-dasharray="5,3"/>
          <line x1="176" y1="187" x2="114" y2="180" stroke="#4a4a4a" stroke-width="3" stroke-dasharray="5,3"/>

          <!-- ══ CASSETTE ══ -->
          <circle cx="100" cy="170" r="18" stroke="#5a5a5a" stroke-width="2.5"/>
          <circle cx="100" cy="170" r="13" stroke="#484848" stroke-width="2"/>
          <circle cx="100" cy="170" r="8" stroke="#404040" stroke-width="1.5"/>

          <!-- ══ DÉRAILLEUR ARRIÈRE ══ -->
          <path d="M114 170 L122 175 L125 185 L118 192" stroke="#5a5a5a" stroke-width="2" fill="none" stroke-linecap="round"/>
          <circle cx="118" cy="176" r="4" fill="none" stroke="#5a5a5a" stroke-width="1.5"/>
          <circle cx="122" cy="188" r="4" fill="none" stroke="#5a5a5a" stroke-width="1.5"/>
          <rect x="110" y="168" width="14" height="10" rx="2.5" fill="#303030" stroke="#5a5a5a" stroke-width="1.2"/>

          <!-- ══ FREIN AVANT (étrier) ══ -->
          <rect x="312" y="122" width="8" height="18" rx="3" fill="#303030" stroke="#5a5a5a" stroke-width="1.5"/>
          <line x1="316" y1="122" x2="316" y2="114" stroke="#5a5a5a" stroke-width="1.5" stroke-dasharray="2,2"/>
          <line x1="310" y1="132" x2="322" y2="132" stroke="#5a5a5a" stroke-width="1.5"/>

          <!-- ══ FREIN ARRIÈRE (étrier) ══ -->
          <rect x="92" y="122" width="8" height="18" rx="3" fill="#303030" stroke="#5a5a5a" stroke-width="1.5"/>
          <line x1="96" y1="122" x2="96" y2="114" stroke="#5a5a5a" stroke-width="1.5" stroke-dasharray="2,2"/>
          <line x1="90" y1="132" x2="102" y2="132" stroke="#5a5a5a" stroke-width="1.5"/>

          <!-- ══ MARQUEURS ANNOTÉS (style affiche technique) ══
               Chaque marqueur = cercle coloré + ligne de rappel + étiquette
          -->

          <!-- 1 — Roue avant -->
          <line x1="320" y1="126" x2="320" y2="105" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM1" cx="320" cy="100" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt1" x="320" y="104" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace" font-weight="bold">RAV</text>
          <circle cx="320" cy="100" r="10" fill="transparent" class="dv-zone" onclick="dvJumpTo(1)" style="cursor:pointer"/>

          <!-- 2 — Frein avant -->
          <line x1="320" y1="134" x2="398" y2="118" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM2" cx="404" cy="116" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt2" x="404" y="120" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">FAV</text>
          <rect x="305" y="115" width="40" height="32" fill="transparent" class="dv-zone" onclick="dvJumpTo(2)" style="cursor:pointer"/>

          <!-- 3 — Direction -->
          <line x1="302" y1="108" x2="398" y2="142" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM3" cx="404" cy="144" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt3" x="404" y="148" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">DIR</text>
          <rect x="290" y="90" width="24" height="36" fill="transparent" class="dv-zone" onclick="dvJumpTo(3)" style="cursor:pointer"/>

          <!-- 4 — Cintre/Potence -->
          <line x1="302" y1="78" x2="398" y2="80" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM4" cx="404" cy="80" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt4" x="404" y="84" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">CIN</text>
          <rect x="270" y="62" width="58" height="30" fill="transparent" class="dv-zone" onclick="dvJumpTo(4)" style="cursor:pointer"/>

          <!-- 5 — Cadre/Fourche -->
          <line x1="245" y1="116" x2="245" y2="44" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM5" cx="245" cy="38" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt5" x="245" y="42" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">CDR</text>
          <rect x="170" y="85" width="120" height="88" fill="transparent" class="dv-zone" onclick="dvJumpTo(5)" style="cursor:pointer"/>

          <!-- 6 — Boîtier pédalier -->
          <line x1="178" y1="145" x2="178" y2="38" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM6" cx="178" cy="32" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt6" x="178" y="36" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">BPD</text>
          <circle cx="178" cy="170" r="26" fill="transparent" class="dv-zone" onclick="dvJumpTo(6)" style="cursor:pointer"/>

          <!-- 7 — Pédalier/transmission -->
          <line x1="165" y1="150" x2="118" y2="38" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM7" cx="118" cy="32" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt7" x="118" y="36" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">TRN</text>
          <rect x="152" y="135" width="24" height="34" fill="transparent" class="dv-zone" onclick="dvJumpTo(7)" style="cursor:pointer"/>

          <!-- 8 — Chaîne -->
          <line x1="148" y1="172" x2="60" y2="222" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM8" cx="55" cy="228" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt8" x="55" y="232" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">CHN</text>
          <rect x="112" y="156" width="66" height="22" fill="transparent" class="dv-zone" onclick="dvJumpTo(8)" style="cursor:pointer"/>

          <!-- 9 — Cassette -->
          <line x1="88" y1="158" x2="20" y2="130" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM9" cx="14" cy="128" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt9" x="14" y="132" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">CAS</text>
          <circle cx="100" cy="170" r="20" fill="transparent" class="dv-zone" onclick="dvJumpTo(9)" style="cursor:pointer"/>

          <!-- 10 — Dérailleur arrière -->
          <line x1="118" y1="182" x2="20" y2="172" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM10" cx="14" cy="172" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt10" x="14" y="176" text-anchor="middle" font-size="6.5" fill="#9a9590" font-family="monospace" font-weight="bold">DER</text>
          <rect x="108" y="165" width="26" height="30" fill="transparent" class="dv-zone" onclick="dvJumpTo(10)" style="cursor:pointer"/>

          <!-- 11 — Frein arrière -->
          <line x1="96" y1="130" x2="20" y2="216" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM11" cx="14" cy="220" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt11" x="14" y="224" text-anchor="middle" font-size="6.5" fill="#9a9590" font-family="monospace" font-weight="bold">FAR</text>
          <rect x="86" y="116" width="22" height="28" fill="transparent" class="dv-zone" onclick="dvJumpTo(11)" style="cursor:pointer"/>

          <!-- 12 — Roue arrière -->
          <line x1="100" y1="124" x2="100" y2="105" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM12" cx="100" cy="100" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt12" x="100" y="104" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">RAR</text>
          <circle cx="100" cy="170" r="48" fill="transparent" class="dv-zone" onclick="dvJumpTo(12)" style="cursor:pointer"/>

          <!-- 13 — Selle/tige -->
          <line x1="207" y1="60" x2="207" y2="38" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
          <circle class="dv-marker" id="dvM13" cx="207" cy="32" r="10" fill="#1e1e1e" stroke="#555" stroke-width="1.5"/>
          <text id="dvMt13" x="207" y="36" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace" font-weight="bold">SEL</text>
          <rect x="184" y="55" width="50" height="20" fill="transparent" class="dv-zone" onclick="dvJumpTo(13)" style="cursor:pointer"/>

        </svg>
      </div>
      <!-- Légende couleurs -->
      <div class="dv-legend">
        <span class="dv-leg-item"><span class="dv-leg-dot" style="background:#22c55e"></span>OK</span>
        <span class="dv-leg-item"><span class="dv-leg-dot" style="background:#eab308"></span>Surveiller</span>
        <span class="dv-leg-item"><span class="dv-leg-dot" style="background:#f97316"></span>À régler</span>
        <span class="dv-leg-item"><span class="dv-leg-dot" style="background:#ef4444"></span>Remplacer</span>
        <span class="dv-leg-item"><span class="dv-leg-dot" style="background:#a855f7"></span>Danger</span>
      </div>
    </div>

    <!-- ── CARTES DE CONTRÔLE ── -->
    <div class="dv-cards" id="dvCards"></div>

    <!-- ── RÉSUMÉ ── -->
    <div class="dv-summary" id="dvSummary" style="display:none">
      <div class="dv-sum-title">📋 Résumé diagnostic</div>
      <div class="dv-sum-etat" id="dvSumEtat"></div>
      <div id="dvSumContent"></div>
      <div class="dv-sum-comment-wrap">
        <div class="dv-sum-lbl">💬 Commentaire final</div>
        <textarea class="dv-sum-comment" id="dvSumComment" placeholder="Notes mécanicien, conseils au client…" rows="3"></textarea>
      </div>
    </div>

    <!-- ── ACTIONS ── -->
    <div class="dv-actions">
      <button class="dv-btn-reset" onclick="dvReset()">↺ Réinitialiser</button>
      <button class="dv-btn-export" onclick="dvExport()">↗ Exporter rapport</button>
    </div>

    <div style="height:30px"></div>
  </div>
</div>

  <div class="screen" id="couples">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Couples de Serrage</div>
      <div class="dh-step">Shimano · SRAM · Campagnolo · Hope · DT Swiss · Fox · RockShox</div>
    </div>
  </div>

  <div class="ray-scroll" style="padding-bottom:30px">

    <!-- Avertissement -->
    <div style="background:rgba(245,158,11,.09);border:1px solid rgba(245,158,11,.25);border-radius:12px;padding:12px 14px;margin-bottom:14px;font-size:12px;color:#d4920a;line-height:1.6">
      ⚠️ <strong>Ces valeurs sont indicatives.</strong> Toujours consulter les notices techniques des composants. Les couples peuvent varier selon les matériaux (alu vs carbone) et les traitements de surface.
    </div>

    <!-- ── ROUES ── -->
    <div class="cpl-section">
      <div class="cpl-section-title">🔵 Roues — Écrous &amp; Axes</div>
      <div class="cpl-grid">
        <div class="cpl-card">
          <div class="cpl-zone">Axe traversant avant</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">12–15 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">12–15 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Axe traversant arrière</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">12–15 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">12–15 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Écrou serrage rapide (QR)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">—</span>
          </div>
          <div class="cpl-note">Serrage manuel sans clé</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Cassette (lockring)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">40 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">40 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Roue libre (freehub)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">25–35 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM XD</span><span class="cpl-nm">40 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Center Lock (rotor)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">40 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">40 Nm</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ── FREINS DISQUE ── -->
    <div class="cpl-section">
      <div class="cpl-section-title">🔴 Freins à disque</div>
      <div class="cpl-grid">
        <div class="cpl-card">
          <div class="cpl-zone">Vis rotor 6 trous (Torx T25)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">2–4 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">4 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Étrier IS / PM (vis M5)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">6–8 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">6–8 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Étrier Flat Mount (vis M5)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">6–8 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">6–8 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Raccord hydraulique (olive)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">5–6 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">5–6 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Levier de frein sur cintre</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">6–8 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">5–7 Nm</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ── TRANSMISSION ── -->
    <div class="cpl-section">
      <div class="cpl-section-title">⚙️ Transmission</div>
      <div class="cpl-grid">
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Boîtier BSA (gauche, fileté G)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">35–50 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">35–50 Nm</span>
          </div>
        </div>
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Boîtier BSA (droite, fileté D)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">35–50 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">35–50 Nm</span>
          </div>
          <div class="cpl-note">⚠️ Filetage sens inversé côté gauche</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Manivelle Hollowtech II (vis M8)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">12–14 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Manivelle SRAM DUB / GXP</div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">47–54 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Pédale (axe M14)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">35–55 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">35–55 Nm</span>
          </div>
          <div class="cpl-note">⚠️ Pédale gauche : filetage inversé</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Plateau (vis BCD M8)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">8–10 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">8–10 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Dérailleur arrière (vis pivot)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">8–10 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">8–10 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Dérailleur arrière (patte)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">8–10 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">8–10 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Galets dérailleur</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">2,5–3 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">2,5–3 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Dérailleur avant (vis de fixation)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">5–7 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">5–7 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Levier/commande vitesse cintre</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">6–8 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">5–7 Nm</span>
          </div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Vis tension câble dérailleur</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Shimano</span><span class="cpl-nm">1–2 Nm</span>
          </div>
          <div class="cpl-val-row">
            <span class="cpl-brand sram">SRAM</span><span class="cpl-nm">1–2 Nm</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ── DIRECTION ── -->
    <div class="cpl-section">
      <div class="cpl-section-title">🎯 Direction &amp; Pilotage</div>
      <div class="cpl-grid">
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Potence sur colonne (vis M5)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Standard</span><span class="cpl-nm">5–7 Nm</span>
          </div>
          <div class="cpl-note">⚠️ Carbone : max 5 Nm, pâte carbone recommandée</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Potence sur cintre (vis M5)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Standard</span><span class="cpl-nm">5–7 Nm</span>
          </div>
          <div class="cpl-note">⚠️ Cintre carbone : 4–5 Nm max</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Vis de précharge jeu direction</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Standard</span><span class="cpl-nm">0,5–1 Nm</span>
          </div>
          <div class="cpl-note">Puis bloquer les vis de potence</div>
        </div>
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Expandeur A-Head (star nut)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Standard</span><span class="cpl-nm">5–8 Nm</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ── SELLE ── -->
    <div class="cpl-section">
      <div class="cpl-section-title">🪑 Selle &amp; Tige de selle</div>
      <div class="cpl-grid">
        <div class="cpl-card">
          <div class="cpl-zone">Collier de tige de selle (1 vis)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Standard alu</span><span class="cpl-nm">5–7 Nm</span>
          </div>
          <div class="cpl-note">Carbone : 3–5 Nm · pâte carbone</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Fixation selle sur tige</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Standard</span><span class="cpl-nm">8–12 Nm</span>
          </div>
          <div class="cpl-note">Carbone : 6–8 Nm · pâte carbone</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Dropper post (corps)</div>
          <div class="cpl-val-row">
            <span class="cpl-brand shimano">Selon marque</span><span class="cpl-nm">3–6 Nm</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ── CAMPAGNOLO ── -->
    <div class="cpl-section">
      <div class="cpl-section-title">🟡 Campagnolo</div>
      <div class="cpl-grid">
        <div class="cpl-card">
          <div class="cpl-zone">Boîtier pédalier (Ultra Torque)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Campa</span><span class="cpl-nm">42 Nm</span></div>
          <div class="cpl-note">Couples de la coupelle ITA</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Manivelle gauche Ultra Torque</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Campa</span><span class="cpl-nm">42 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Cassette lockring Campagnolo</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Campa</span><span class="cpl-nm">35 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Levier de frein / dérailleur</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Campa</span><span class="cpl-nm">8–10 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Vis plateau (bras manivelle)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Campa</span><span class="cpl-nm">8–10 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Axe traversant fourche</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Campa</span><span class="cpl-nm">12–15 Nm</span></div>
        </div>
      </div>
    </div>

    <!-- ── HOPE ── -->
    <div class="cpl-section">
      <div class="cpl-section-title" style="color:#e8400c">🔴 Hope Technology</div>
      <div class="cpl-grid">
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Axe traversant avant Hope (M12)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">12 Nm</span></div>
          <div class="cpl-note">Modèles Pro 4 · Pro 5</div>
        </div>
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Axe traversant arrière Hope (M12)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">12 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Lockring cassette Hope</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">40 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Corps de roue libre Hope</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">40 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Vis rotor 6 trous Hope</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">4 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Étrier frein Hope (vis M5)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">6 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Raccord hydraulique Hope</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">5 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Boîtier pédalier Hope (filetage)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(232,64,12,.12);color:#e8400c">Hope</span><span class="cpl-nm">40–50 Nm</span></div>
        </div>
      </div>
    </div>

    <!-- ── DT SWISS ── -->
    <div class="cpl-section">
      <div class="cpl-section-title" style="color:#2d7ff9">🔵 DT Swiss</div>
      <div class="cpl-grid">
        <div class="cpl-card">
          <div class="cpl-zone">Axe traversant avant (M12×100)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(45,127,249,.12);color:#2d7ff9">DT Swiss</span><span class="cpl-nm">15 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Axe traversant arrière (M12×142/148)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(45,127,249,.12);color:#2d7ff9">DT Swiss</span><span class="cpl-nm">15 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Écrou de fixation roue DT</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(45,127,249,.12);color:#2d7ff9">DT Swiss</span><span class="cpl-nm">40 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Nipple rayon interne (vis)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(45,127,249,.12);color:#2d7ff9">DT Swiss</span><span class="cpl-nm">Tensiomètre</span></div>
          <div class="cpl-note">Tension cible 80–120 kgf selon modèle</div>
        </div>
      </div>
    </div>

    <!-- ── FOX SUSPENSION ── -->
    <div class="cpl-section">
      <div class="cpl-section-title" style="color:#f59e0b">🟠 Fox Racing Shox</div>
      <div class="cpl-grid">
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Fourche — Vis de bouchon de jambe</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Fox</span><span class="cpl-nm">5–7 Nm</span></div>
          <div class="cpl-note">Ne pas dépasser — risque de rupture</div>
        </div>
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Fourche — Vis de réglage externe</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Fox</span><span class="cpl-nm">1–1,5 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Amortisseur — Vis de fixation (M6)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Fox</span><span class="cpl-nm">8–12 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Valve Schrader chambre à air</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Fox</span><span class="cpl-nm">Main seulement</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Bouchon supérieur jambe</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Fox</span><span class="cpl-nm">15 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Axe de roue avant fourche Fox</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(245,158,11,.15);color:#d4920a">Fox</span><span class="cpl-nm">15 Nm</span></div>
          <div class="cpl-note">Fox 34/36/38 · Torque cap</div>
        </div>
      </div>
    </div>

    <!-- ── ROCKSHOX ── -->
    <div class="cpl-section">
      <div class="cpl-section-title" style="color:#a855f7">🟣 RockShox (SRAM)</div>
      <div class="cpl-grid">
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Fourche — Bouchon de jambe inférieur</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(168,85,247,.12);color:#a855f7">RockShox</span><span class="cpl-nm">3–5 Nm</span></div>
          <div class="cpl-note">⚠️ Ne pas dépasser — pièce fragile</div>
        </div>
        <div class="cpl-card cpl-important">
          <div class="cpl-zone">Fourche — Bouchon de jambe supérieur</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(168,85,247,.12);color:#a855f7">RockShox</span><span class="cpl-nm">15 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Amortisseur — Vis de fixation</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(168,85,247,.12);color:#a855f7">RockShox</span><span class="cpl-nm">8–12 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Vis de réglage rebond/compression</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(168,85,247,.12);color:#a855f7">RockShox</span><span class="cpl-nm">Main seulement</span></div>
          <div class="cpl-note">Clips de réglage — sans outil</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Axe de roue avant (Maxle Stealth)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(168,85,247,.12);color:#a855f7">RockShox</span><span class="cpl-nm">15 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Vis de purge (bleed port)</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(168,85,247,.12);color:#a855f7">RockShox</span><span class="cpl-nm">0,3–0,5 Nm</span></div>
          <div class="cpl-note">Très faible — juste pour étanchéité</div>
        </div>
      </div>
    </div>

    <!-- ── CHRIS KING ── -->
    <div class="cpl-section">
      <div class="cpl-section-title" style="color:#22c55e">🟢 Chris King</div>
      <div class="cpl-grid">
        <div class="cpl-card">
          <div class="cpl-zone">Jeu de direction — bague de serrage</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(34,197,94,.1);color:#22c55e">CKing</span><span class="cpl-nm">Main + outil</span></div>
          <div class="cpl-note">Serrage précis au ressenti sans couple — voir instructions CK</div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Axe traversant avant R45</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(34,197,94,.1);color:#22c55e">CKing</span><span class="cpl-nm">12–15 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Axe traversant arrière R45</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(34,197,94,.1);color:#22c55e">CKing</span><span class="cpl-nm">12–15 Nm</span></div>
        </div>
        <div class="cpl-card">
          <div class="cpl-zone">Lockring cassette CK</div>
          <div class="cpl-val-row"><span class="cpl-brand" style="background:rgba(34,197,94,.1);color:#22c55e">CKing</span><span class="cpl-nm">40 Nm</span></div>
        </div>
      </div>
    </div>

    <!-- ── LÉGENDE ── -->
    <div style="background:var(--card);border:1px solid var(--border);border-radius:12px;padding:12px 14px;margin-top:4px">
      <div style="font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);letter-spacing:.12em;text-transform:uppercase;margin-bottom:8px">Matériaux — ajustements couples</div>
      <div style="font-size:12px;color:var(--text2);line-height:1.8">
        🔩 <strong style="color:var(--text)">Alu/Acier</strong> : valeurs nominales<br>
        🏁 <strong style="color:var(--text)">Carbone</strong> : réduire de 20–30%, pâte carbone<br>
        💧 <strong style="color:var(--text)">Titane</strong> : réduire de 10–20%, graisse cuivrée<br>
        🌊 <strong style="color:var(--text)">Composant mouillé</strong> : couple légèrement inférieur
      </div>
    </div>

    <div style="height:20px"></div>
  </div>
</div>

<!-- INFO PANEL (bottom sheet with SVG schema) -->
<div class="info-overlay" id="infoOverlay" onclick="closeInfo()">
  <div class="info-sheet" onclick="event.stopPropagation()">
    <div class="info-sheet-handle"></div>
    <div class="info-sheet-title" id="infoTitle"></div>
    <div class="info-sheet-svg" id="infoSvg"></div>
    <div class="info-sheet-body" id="infoBody"></div>
    <button class="info-close-btn" onclick="closeInfo()">Fermer</button>
  </div>
</div>


<script>
// ═══════════════════════════════
// NAVIGATION
// ═══════════════════════════════
function showScreen(id){
  document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
  // Init modules on first show
  if(id==='historique') histRender();
  if(id==='biblio')     biblioRender();
  if(id==='devis' && !devisLines.length) devisInit();
}
function showTab(id){ showScreen(id); } // alias conservé pour compatibilité

// ═══════════════════════════════
// SHIS CALCULATOR — clean rewrite
// ═══════════════════════════════
const SHIS_DIAM = {
  top:{ EC:[30,34,37,41,44,49], ZS:[30,34,37,41,44,49], IS:[38,41,42] },
  bot:{ EC:[30,34,37,41,44,49,55,56], ZS:[30,34,37,41,44,49,55,56], IS:[38,41,42,47,49,52] }
};
const SHIS_DIAM_DESC = {
  30:'Route vintage', 34:'Route std', 37:'Interméd.', 38:'IS vintage',
  41:'Oversize/IS', 42:'IS standard', 44:'XC/Enduro', 47:'IS moyen',
  49:'Grand Ø', 52:'IS tapered', 55:'EC grand', 56:'EC max'
};
const SHIS_TYPE = {
  EC:{ fr:'Cuvette externe',  en:'External Cup', detail:'La coupelle dépasse visiblement hors du tube.', col:'#e8400c' },
  ZS:{ fr:'Semi-intégrée',   en:'Zero Stack',   detail:'Affleure le tube dans un lamage usiné.',       col:'#2d7ff9' },
  IS:{ fr:'Intégrée',        en:'Integrated',   detail:'Roulement direct dans le tube, sans coupelle.', col:'#22c55e' }
};

let SH = { type_haut:null, diam_haut:null, pivot_haut:null, type_bas:null, diam_bas:null, pivot_bas:null };

function shSet(field, val, btn, side){
  SH[field] = val;
  const grid = btn.closest('.sh-type-row, .sh-vals-grid');
  if(grid) grid.querySelectorAll('button').forEach(b=>{
    b.classList.remove('sel-top','sel-bot');
  });
  btn.classList.add(side==='top' ? 'sel-top' : 'sel-bot');

  if(field==='type_haut'){ SH.diam_haut=null; shRenderDiam('haut', val); }
  if(field==='type_bas') { SH.diam_bas=null;  shRenderDiam('bas', val);  }

  shUpdateLive();
  shCompute();
}

function shRenderDiam(side, type){
  const key   = side==='haut' ? 'top' : 'bot';
  const diams = SHIS_DIAM[key][type] || [];
  const grid  = document.getElementById(`sh_diam_${side}`);
  const hint  = document.getElementById(`sh_hint_d${side==='haut'?'h':'b'}`);
  const color = side==='haut' ? 'top' : 'bot';
  if(!grid) return;
  grid.innerHTML = diams.map(d=>`
    <button class="sh-val-btn" onclick="shSet('diam_${side}','${d}',this,'${color}')">
      <div class="sh-val-num">${d}</div>
      <div class="sh-val-mm">mm</div>
      <div class="sh-val-desc">${SHIS_DIAM_DESC[d]||''}</div>
    </button>`).join('');
  grid.style.display = 'grid';
  if(hint) hint.style.display = 'none';
}

function shUpdateLive(){
  const s = SH;
  const h = (s.type_haut && s.diam_haut && s.pivot_haut) ? `${s.type_haut}${s.diam_haut}/${s.pivot_haut}` : '?';
  const b = (s.type_bas  && s.diam_bas  && s.pivot_bas)  ? `${s.type_bas}${s.diam_bas}/${s.pivot_bas}`   : '?';
  const el = document.getElementById('shLiveCode');
  if(el){ el.textContent = `${h} — ${b}`; el.style.color = (h!=='?'||b!=='?') ? 'var(--text)' : 'var(--muted)'; }
}

function shCompute(){
  const s = SH;
  if(!(s.type_haut&&s.diam_haut&&s.pivot_haut&&s.type_bas&&s.diam_bas&&s.pivot_bas)){
    document.getElementById('shResult').classList.remove('show'); return;
  }
  const codeH = `${s.type_haut}${s.diam_haut}/${s.pivot_haut}`;
  const codeB = `${s.type_bas}${s.diam_bas}/${s.pivot_bas}`;
  const full  = `${codeH} — ${codeB}`;
  document.getElementById('shCode').textContent = full;

  const dH = SHIS_TYPE[s.type_haut], dB = SHIS_TYPE[s.type_bas];
  document.getElementById('shHalves').innerHTML = `
    <div class="sh-half">
      <div class="sh-half-lbl">⬆ Haut — Top</div>
      <div class="sh-half-code" style="color:${dH.col}">${codeH}</div>
      <div class="sh-half-def"><strong style="color:${dH.col}">${s.type_haut}</strong> — ${dH.fr} <em style="color:var(--muted);font-size:10px">(${dH.en})</em><br><span style="font-size:11px;color:var(--muted)">${dH.detail}</span></div>
    </div>
    <div class="sh-half">
      <div class="sh-half-lbl">⬇ Bas — Bottom</div>
      <div class="sh-half-code" style="color:${dB.col}">${codeB}</div>
      <div class="sh-half-def"><strong style="color:${dB.col}">${s.type_bas}</strong> — ${dB.fr} <em style="color:var(--muted);font-size:10px">(${dB.en})</em><br><span style="font-size:11px;color:var(--muted)">${dB.detail}</span></div>
    </div>`;

  const pH=parseFloat(s.pivot_haut), pB=parseFloat(s.pivot_bas);
  let diag='', diagCol='var(--blue)';
  if(pH<=26&&pB<=26)       { diag='🔵 Fourche <strong>1" vintage</strong> — Configuration ancienne, rare.'; }
  else if(pH<=28.7&&pB<=30.5){ diag='✅ Fourche <strong>droite 1⅛"</strong> — Pivot uniforme. Standard le plus courant.'; diagCol='#22c55e'; }
  else if(pH<=28.7&&pB>=38)  { diag='✅ Fourche <strong>conique / Tapered</strong> — 1⅛" haut → 1½" bas. Très courant VTT Trail/Enduro.'; diagCol='#22c55e'; }
  else if(pH>=38&&pB>=38)    { diag='🔵 Fourche <strong>1½" droite oversize</strong> — DH, VTT gros pivot.'; }
  else                       { diag='⚠️ Configuration mixte — vérifiez la compatibilité cadre + fourche.'; diagCol='#f59e0b'; }

  let diffNote = '';
  if(s.type_haut !== s.type_bas){
    diffNote = `<div class="sh-diff-note">⚠️ Haut <strong style="color:${dH.col}">${s.type_haut}</strong> ≠ Bas <strong style="color:${dB.col}">${s.type_bas}</strong> — C'est tout à fait normal sur certains cadres !</div>`;
  }
  document.getElementById('shDiag').innerHTML = `<div style="color:${diagCol}">${diag}</div>${diffNote}`;
  document.getElementById('shResult').classList.add('show');
  document.getElementById('shResult').scrollIntoView({behavior:'smooth',block:'nearest'});
}

function shCopy(){
  const code = document.getElementById('shCode').textContent;
  const btn  = document.getElementById('shCopyBtn');
  const done = ()=>{ btn.textContent='✓ Copié !'; btn.classList.add('ok'); setTimeout(()=>{ btn.textContent='📋 Copier le code'; btn.classList.remove('ok'); },2200); };
  if(navigator.clipboard) navigator.clipboard.writeText(code).then(done);
  else{ const t=document.createElement('textarea'); t.value=code; document.body.appendChild(t); t.select(); document.execCommand('copy'); document.body.removeChild(t); done(); }
}

function shReset(){
  SH={type_haut:null,diam_haut:null,pivot_haut:null,type_bas:null,diam_bas:null,pivot_bas:null};
  document.querySelectorAll('.sh-type-btn,.sh-val-btn').forEach(b=>b.classList.remove('sel-top','sel-bot'));
  ['sh_diam_haut','sh_diam_bas'].forEach(id=>{ const e=document.getElementById(id); if(e){e.innerHTML='';e.style.display='none';} });
  ['sh_hint_dh','sh_hint_db'].forEach(id=>{ const e=document.getElementById(id); if(e) e.style.display='block'; });
  document.getElementById('shResult').classList.remove('show');
  document.getElementById('shLiveCode').textContent='? — ?';
  document.getElementById('shisScroll').scrollTo({top:0,behavior:'smooth'});
}

// ═══════════════════════════════
// BRAQUET & DÉVELOPPEMENT — formules exactes
// ═══════════════════════════════

function braqPreset(){
  const val = document.getElementById('br_preset').value;
  if(!val || val === 'custom') return;
  const [diam, larg] = val.split(':');
  document.getElementById('br_diam_jante').value = diam;
  document.getElementById('br_larg_pneu').value  = larg;
  braqCompute();
}
function braqSetPl(v){ document.getElementById('br_plateaux').value=v; braqCompute(); }
function braqSetCas(mn,mx){ document.getElementById('br_pig_min').value=mn; document.getElementById('br_pig_max').value=mx; braqCompute(); }
function braqSetCad(val,btn){ document.getElementById('br_cadence').value=val; braqClearCadBtns(); btn.classList.add('active'); braqCompute(); }
function braqClearCadBtns(){ document.querySelectorAll('.braq-cad-btn').forEach(b=>b.classList.remove('active')); }

function braqCompute(){
  const diametreJante = parseFloat(document.getElementById('br_diam_jante').value);
  const largeurPneu   = parseFloat(document.getElementById('br_larg_pneu').value);
  const plStr         = document.getElementById('br_plateaux').value;
  const pignonMin     = parseInt(document.getElementById('br_pig_min').value);
  const pignonMax     = parseInt(document.getElementById('br_pig_max').value);
  const cadence       = parseInt(document.getElementById('br_cadence').value)||90;

  // Affichage circonf live
  if(!isNaN(diametreJante)&&!isNaN(largeurPneu)&&diametreJante>0&&largeurPneu>0){
    const cLive = (diametreJante + 2*largeurPneu)*Math.PI;
    document.getElementById('braqCircDisplay').style.display='flex';
    document.getElementById('braqCircVal').textContent=cLive.toFixed(0);
  } else {
    document.getElementById('braqCircDisplay').style.display='none';
  }

  if(isNaN(diametreJante)||isNaN(largeurPneu)||!plStr||isNaN(pignonMin)||isNaN(pignonMax)||pignonMin<1||pignonMax<pignonMin){
    document.getElementById('braqResult').style.display='none'; return;
  }

  const plateaux=plStr.split(/[,;\s]+/).map(s=>parseInt(s.trim())).filter(n=>!isNaN(n)&&n>0).sort((a,b)=>b-a);
  if(!plateaux.length){ document.getElementById('braqResult').style.display='none'; return; }

  // Pignons min→max
  const pignons=[];
  for(let p=pignonMin;p<=pignonMax;p++) pignons.push(p);

  const plateauMin=Math.min(...plateaux), plateauMax=Math.max(...plateaux);

  // Formule 1 — Circonférence (m)
  const circonference = ((diametreJante + 2*largeurPneu)*Math.PI)/1000;

  // Formule 5 & 6 — Braquets
  const braquetMin = plateauMin/pignonMax;
  const braquetMax = plateauMax/pignonMin;

  // Formule 7 & 8 — Plage
  const plage        = braquetMax/braquetMin;
  const plagePourcent = plage*100;

  // Développement min/max (formule 3)
  const devMin = braquetMin*circonference;
  const devMax = braquetMax*circonference;

  // ── KPI Roue ──
  document.getElementById('res_circ').textContent     = (circonference*1000).toFixed(0);
  document.getElementById('res_braq_min').textContent = braquetMin.toFixed(2);
  document.getElementById('res_braq_max').textContent = braquetMax.toFixed(2);

  // ── Développement ──
  document.getElementById('res_dev_min').textContent       = devMin.toFixed(2);
  document.getElementById('res_dev_max').textContent       = devMax.toFixed(2);
  document.getElementById('res_dev_min_combo').textContent = plateauMin+'T / '+pignonMax+'T';
  document.getElementById('res_dev_max_combo').textContent = plateauMax+'T / '+pignonMin+'T';

  // Plage gauge
  const plageLabel = plage>=5?'Très large — enduro/gravel':plage>=4?'Large — VTT':plage>=3?'Correct — route':plage>=2?'Resserré — compétition':'Très serré — CLM';
  const gaugeW = Math.min(100,((plage-1)/5)*100);
  const gaugeCol = plage>=4?'var(--green)':plage>=2.5?'var(--blue)':'var(--gold)';
  document.getElementById('res_plage').textContent     = plage.toFixed(2)+'×';
  document.getElementById('res_plage_pct').textContent = plagePourcent.toFixed(0)+'%';
  document.getElementById('res_gauge').style.width     = gaugeW+'%';
  document.getElementById('res_gauge').style.background= gaugeCol;
  document.getElementById('res_plage_hint').textContent= plageLabel;

  // ── Formule 4 : Vitesses ──
  document.getElementById('res_cad_lbl').textContent = cadence;
  let vitHtml='';
  plateaux.forEach(pl=>{
    const vitArr=pignons.map(pg=>{
      const ratio=pl/pg;                          // formule 2
      const dev=ratio*circonference;               // formule 3
      const vit=dev*cadence*60/1000;              // formule 4
      return {pg,vit,dev};
    });
    const vitMn=vitArr[vitArr.length-1], vitMx=vitArr[0];
    vitHtml+=`<div class="braq-speed-row">
      <div class="braq-speed-lbl">Plateau <strong style="color:var(--purple)">${pl}T</strong>
        <span style="color:var(--muted);font-size:10px"> · ${pignonMin}→${pignonMax}T</span></div>
      <div style="display:flex;align-items:baseline;gap:5px">
        <span class="braq-speed-val slow">${vitMn.vit.toFixed(1)}</span>
        <span class="braq-speed-unit">→</span>
        <span class="braq-speed-val fast">${vitMx.vit.toFixed(1)}</span>
        <span class="braq-speed-unit">km/h</span>
      </div>
    </div>`;
  });
  document.getElementById('res_vitesses').innerHTML=vitHtml;

  // ── Tableau développements ──
  let allDevs=[];
  plateaux.forEach(pl=>pignons.forEach(pg=>allDevs.push((pl/pg)*circonference)));
  const gMax=Math.max(...allDevs), gMin2=Math.min(...allDevs);
  let tbl=`<table class="braq-table"><thead><tr><th>Pignon↓</th>`;
  plateaux.forEach(pl=>{ tbl+=`<th class="pl-hdr">${pl}T</th>`; });
  tbl+=`</tr></thead><tbody>`;
  pignons.forEach(pg=>{
    tbl+=`<tr><td style="color:var(--muted);font-size:10px;font-family:'JetBrains Mono',monospace">${pg}T</td>`;
    plateaux.forEach(pl=>{
      const dev=(pl/pg)*circonference;
      const cls=dev>=gMax*0.94?'best':dev<=gMin2*1.06?'worst':'';
      tbl+=`<td class="${cls}">${dev.toFixed(2)}</td>`;
    });
    tbl+=`</tr>`;
  });
  tbl+=`</tbody></table>`;
  document.getElementById('res_tableau').innerHTML=tbl;

  // ── Formule 9 : Écarts entre pignons ──
  let ecartHtml='<div class="braq-ecarts-grid">';
  for(let i=0;i<pignons.length-1;i++){
    // Formule 9 : écart = ((pignon_suivant - pignon_actuel) / pignon_actuel) * 100
    const ecart=((pignons[i+1]-pignons[i])/pignons[i])*100;
    const ecartCol=ecart>20?'var(--red)':ecart>15?'var(--gold)':'var(--green)';
    ecartHtml+=`<div class="braq-ecart-item">
      <div class="braq-ecart-combo">${pignons[i]}→${pignons[i+1]}T</div>
      <div class="braq-ecart-val" style="color:${ecartCol}">+${ecart.toFixed(1)}%</div>
    </div>`;
  }
  ecartHtml+='</div>';
  document.getElementById('res_ecarts').innerHTML=ecartHtml;

  document.getElementById('braqResult').style.display='block';
  document.getElementById('braqResult').scrollIntoView({behavior:'smooth',block:'nearest'});
}

function braqReset(){
  ['br_diam_jante','br_larg_pneu','br_plateaux','br_pig_min','br_pig_max'].forEach(id=>{
    const e=document.getElementById(id); if(e) e.value='';
  });
  document.getElementById('br_cadence').value='90';
  document.getElementById('br_preset').value='';
  document.getElementById('braqCircDisplay').style.display='none';
  document.getElementById('braqResult').style.display='none';
  braqClearCadBtns();
  const defBtn=document.querySelector('.braq-cad-btn:nth-child(2)');
  if(defBtn) defBtn.classList.add('active');
  document.getElementById('braqScroll').scrollTo({top:0,behavior:'smooth'});
}

// ═══════════════════════════════
// DIAGNOSTIC VÉLO — Méthode en M
// ═══════════════════════════════

const DV_POINTS = [
  { id:1,  name:'Roue avant',               icon:'🔵', zone:'dvM1'  },
  { id:2,  name:'Frein avant',              icon:'🔴', zone:'dvM2'  },
  { id:3,  name:'Direction / jeu de dir.',  icon:'⚙️', zone:'dvM3'  },
  { id:4,  name:'Poste de pilotage',        icon:'🎯', zone:'dvM4'  },
  { id:5,  name:'Cadre / Fourche',          icon:'🚲', zone:'dvM5'  },
  { id:6,  name:'Boîtier de pédalier',      icon:'⭕', zone:'dvM6'  },
  { id:7,  name:'Pédalier / transmission',  icon:'🔩', zone:null    },
  { id:8,  name:'Chaîne',                   icon:'🔗', zone:'dvM8'  },
  { id:9,  name:'Cassette / roue libre',    icon:'⚙️', zone:'dvM9'  },
  { id:10, name:'Dérailleur arrière',       icon:'🔧', zone:'dvM10' },
  { id:11, name:'Frein arrière',            icon:'🔴', zone:'dvM11' },
  { id:12, name:'Roue arrière',             icon:'🔵', zone:'dvM12' },
  { id:13, name:'Selle / tige de selle',    icon:'🪑', zone:'dvM13' },
];

// État par point : { status, priority, note, action, photos }
let dvState = {};

const DV_STATUS = {
  ok:      { label:'OK',           cls:'sel-ok',      col:'#22c55e', score:0  },
  warn:    { label:'À surveiller', cls:'sel-warn',    col:'#eab308', score:15 },
  fix:     { label:'À régler',     cls:'sel-fix',     col:'#f97316', score:30 },
  replace: { label:'À remplacer',  cls:'sel-replace', col:'#ef4444', score:50 },
  danger:  { label:'Danger',       cls:'sel-danger',  col:'#a855f7', score:80 },
};

function dvInit(){
  dvState = {};
  DV_POINTS.forEach(p => {
    dvState[p.id] = { status:null, priority:null, note:'', action:'', photos:[] };
  });
  dvRenderCards();
  dvUpdateScore();
}

function dvRenderCards(){
  const container = document.getElementById('dvCards');
  container.innerHTML = DV_POINTS.map(p => `
    <div class="dv-card" id="dvCard${p.id}">
      <div class="dv-card-hdr" onclick="dvToggleCard(${p.id})">
        <div class="dv-card-num">${p.id}</div>
        <div class="dv-card-name">${p.icon} ${p.name}</div>
        <div class="dv-card-badge" id="dvBadge${p.id}" style="display:none"></div>
        <div class="dv-card-arr">›</div>
      </div>
      <div class="dv-card-body">
        <!-- Status -->
        <div class="dv-field-lbl" style="margin-top:8px">État</div>
        <div class="dv-status-row">
          <button class="dv-st-btn" onclick="dvSetStatus(${p.id},'ok',this)">✓ OK</button>
          <button class="dv-st-btn" onclick="dvSetStatus(${p.id},'warn',this)">👁 Surveiller</button>
          <button class="dv-st-btn" onclick="dvSetStatus(${p.id},'fix',this)">🔧 À régler</button>
          <button class="dv-st-btn" onclick="dvSetStatus(${p.id},'replace',this)">🔴 Remplacer</button>
          <button class="dv-st-btn" onclick="dvSetStatus(${p.id},'danger',this)">⚠️ Danger</button>
        </div>
        <!-- Priorité -->
        <div class="dv-field-lbl">Priorité</div>
        <div class="dv-prio-row">
          <button class="dv-prio-btn" onclick="dvSetPrio(${p.id},'low',this)">Faible</button>
          <button class="dv-prio-btn" onclick="dvSetPrio(${p.id},'med',this)">Moyen</button>
          <button class="dv-prio-btn" onclick="dvSetPrio(${p.id},'urg',this)">Urgent</button>
        </div>
        <!-- Remarque -->
        <div class="dv-field-lbl">Remarque</div>
        <textarea class="dv-textarea" rows="2" placeholder="Observations…"
          oninput="dvState[${p.id}].note=this.value" id="dvNote${p.id}"></textarea>
        <!-- Action -->
        <div class="dv-field-lbl">Action recommandée</div>
        <textarea class="dv-textarea" rows="2" placeholder="Intervention préconisée…"
          oninput="dvState[${p.id}].action=this.value" id="dvAction${p.id}"></textarea>
        <!-- Photo -->
        <label class="dv-photo-btn" for="dvPhoto${p.id}">
          📷 Ajouter une photo
          <input type="file" class="dv-photo-input" id="dvPhoto${p.id}" accept="image/*" capture="environment"
            onchange="dvAddPhoto(${p.id},this)">
        </label>
        <div class="dv-photo-preview" id="dvPhotos${p.id}"></div>
      </div>
    </div>`).join('');
}

function dvToggleCard(id){
  const card = document.getElementById(`dvCard${id}`);
  card.classList.toggle('open');
}

function dvJumpTo(id){
  // Ouvrir la carte et scroller vers elle
  const card = document.getElementById(`dvCard${id}`);
  if(!card) return;
  if(!card.classList.contains('open')) card.classList.add('open');
  setTimeout(() => card.scrollIntoView({ behavior:'smooth', block:'start' }), 100);
}

function dvSetStatus(id, status, btn){
  dvState[id].status = status;
  const st = DV_STATUS[status];

  // Reset buttons in this card
  const card = document.getElementById(`dvCard${id}`);
  card.querySelectorAll('.dv-st-btn').forEach(b => {
    b.className = 'dv-st-btn';
  });
  btn.classList.add(st.cls);

  // Update card border
  card.className = `dv-card open st-${status}`;

  // Badge
  const badge = document.getElementById(`dvBadge${id}`);
  badge.style.display = 'flex';
  badge.style.background = st.col + '22';
  badge.style.color = st.col;
  badge.style.border = `1px solid ${st.col}55`;
  badge.textContent = st.label;

  // Marqueur sur schéma
  const point = DV_POINTS.find(p => p.id === id);
  if(point && point.zone){
    const marker = document.getElementById(point.zone);
    const markerTxt = document.getElementById(point.zone.replace('dvM','dvMt'));
    if(marker){
      marker.setAttribute('fill', st.col);
      marker.setAttribute('stroke', st.col);
      marker.setAttribute('r', '10');
    }
    if(markerTxt) markerTxt.setAttribute('fill', '#fff');
    // Ligne de rappel : chercher la ligne précédente dans le SVG
    const lineEl = marker ? marker.previousElementSibling : null;
    if(lineEl && lineEl.tagName === 'line') lineEl.setAttribute('stroke', st.col);
  }

  dvUpdateScore();
  dvUpdateSummary();
}

function dvSetPrio(id, prio, btn){
  dvState[id].priority = prio;
  const card = document.getElementById(`dvCard${id}`);
  card.querySelectorAll('.dv-prio-btn').forEach(b => {
    b.className = 'dv-prio-btn';
  });
  btn.classList.add(`p-${prio}`);
  dvUpdateSummary();
}

function dvAddPhoto(id, input){
  if(!input.files.length) return;
  const file = input.files[0];
  const reader = new FileReader();
  reader.onload = e => {
    dvState[id].photos.push(e.target.result);
    const prev = document.getElementById(`dvPhotos${id}`);
    const img = document.createElement('img');
    img.className = 'dv-photo-thumb';
    img.src = e.target.result;
    prev.appendChild(img);
  };
  reader.readAsDataURL(file);
}

function dvUpdateScore(){
  const filled = DV_POINTS.filter(p => dvState[p.id].status !== null);
  const nbOk      = filled.filter(p => dvState[p.id].status === 'ok').length;
  const nbWarn    = filled.filter(p => dvState[p.id].status === 'warn').length;
  const nbFix     = filled.filter(p => dvState[p.id].status === 'fix').length;
  const nbReplace = filled.filter(p => dvState[p.id].status === 'replace').length;
  const nbDanger  = filled.filter(p => dvState[p.id].status === 'danger').length;
  const total     = DV_POINTS.length;

  // Score sur 100 : pénalités selon état
  let penalite = 0;
  filled.forEach(p => {
    penalite += DV_STATUS[dvState[p.id].status].score;
  });
  const maxPenalite = filled.length * 80;
  const score = filled.length === 0 ? '—'
    : Math.max(0, Math.round(100 - (penalite / Math.max(maxPenalite,1)) * 100));

  // Couleur du score
  const scoreEl = document.getElementById('dvScore');
  scoreEl.textContent = filled.length === 0 ? '—' : score + '/100';
  scoreEl.style.color = typeof score === 'number'
    ? (score >= 80 ? '#22c55e' : score >= 60 ? '#f97316' : '#ef4444') : '#9a9590';

  document.getElementById('dvNbOk').textContent      = nbOk;
  document.getElementById('dvNbWarn').textContent    = nbWarn;
  document.getElementById('dvNbFix').textContent     = nbFix;
  document.getElementById('dvNbReplace').textContent = nbReplace;
  document.getElementById('dvNbDanger').textContent  = nbDanger;

  // Progression
  const pct = (filled.length / total) * 100;
  document.getElementById('dvProgFill').style.width = pct + '%';
  document.getElementById('dvProgTxt').textContent  = `${filled.length} / ${total} points contrôlés`;
}

function dvUpdateSummary(){
  const filled = DV_POINTS.filter(p => dvState[p.id].status !== null);
  if(filled.length === 0){ document.getElementById('dvSummary').style.display='none'; return; }

  const urgents   = DV_POINTS.filter(p => dvState[p.id].priority === 'urg');
  const dangers   = DV_POINTS.filter(p => dvState[p.id].status === 'danger');
  const replaces  = DV_POINTS.filter(p => dvState[p.id].status === 'replace');
  const fixes     = DV_POINTS.filter(p => dvState[p.id].status === 'fix');
  const warned    = DV_POINTS.filter(p => dvState[p.id].status === 'warn');

  // État général
  const etatEl = document.getElementById('dvSumEtat');
  let etatTxt, etatCol;
  if(dangers.length > 0){
    etatTxt = '🚨 DANGER — Vélo à ne pas utiliser avant réparation'; etatCol = '#a855f7';
  } else if(replaces.length > 0){
    etatTxt = '🔴 État critique — Remplacement(s) nécessaire(s)'; etatCol = '#ef4444';
  } else if(fixes.length > 0){
    etatTxt = '🟠 À réviser — Réglage(s) nécessaire(s)'; etatCol = '#f97316';
  } else if(warned.length > 0){
    etatTxt = '🟡 Bon état général — Points à surveiller'; etatCol = '#eab308';
  } else {
    etatTxt = '🟢 Excellent état — Tous les points sont OK'; etatCol = '#22c55e';
  }
  etatEl.textContent = etatTxt;
  etatEl.style.color = etatCol;
  etatEl.style.background = etatCol + '15';
  etatEl.style.border = `1px solid ${etatCol}40`;
  etatEl.style.borderRadius = '10px';

  // Contenu
  let html = '';
  const addBlock = (title, items, col) => {
    if(!items.length) return;
    html += `<div class="dv-sum-block"><div class="dv-sum-lbl" style="color:${col}">${title}</div>`;
    items.forEach(p => {
      const s = dvState[p.id];
      html += `<div class="dv-sum-item">
        <strong>${p.id}. ${p.name}</strong>
        ${s.action ? `<br><span style="color:var(--muted)">→ ${s.action}</span>` : ''}
        ${s.note   ? `<br><span style="color:var(--muted);font-size:12px">${s.note}</span>` : ''}
      </div>`;
    });
    html += `</div>`;
  };

  addBlock('⚠️ Points DANGER', dangers, '#a855f7');
  addBlock('🔴 Pièces à remplacer', replaces, '#ef4444');
  addBlock('🟠 Réglages nécessaires', fixes, '#f97316');
  addBlock('🟡 À surveiller', warned, '#eab308');
  if(urgents.length){
    addBlock('🚨 Priorité urgente', urgents, '#ef4444');
  }

  document.getElementById('dvSumContent').innerHTML = html;
  document.getElementById('dvSummary').style.display = 'block';
}

function dvToggleFiche(){
  const body = document.getElementById('dvFicheBody');
  const btn  = document.getElementById('dvFicheToggle');
  const open = body.style.display !== 'none';
  body.style.display = open ? 'none' : 'block';
  btn.classList.toggle('closed', open);
}

function dvGetFiche(){
  return {
    nom:     document.getElementById('dvClientNom')?.value    || '',
    prenom:  document.getElementById('dvClientPrenom')?.value || '',
    tel:     document.getElementById('dvClientTel')?.value    || '',
    mail:    document.getElementById('dvClientMail')?.value   || '',
    marque:  document.getElementById('dvVeloMarque')?.value   || '',
    modele:  document.getElementById('dvVeloModele')?.value   || '',
    annee:   document.getElementById('dvVeloAnnee')?.value    || '',
    couleur: document.getElementById('dvVeloCouleur')?.value  || '',
    serie:   document.getElementById('dvVeloSerie')?.value    || '',
    km:      document.getElementById('dvVeloKm')?.value       || '',
    type:    document.getElementById('dvVeloType')?.value     || '',
    meca:    document.getElementById('dvMeca')?.value         || '',
    date:    document.getElementById('dvDate')?.value         || new Date().toLocaleDateString('fr-FR'),
    atelier: document.getElementById('dvAtelier')?.value      || '',
  };
}

function dvExport(){
  const filled = DV_POINTS.filter(p => dvState[p.id].status !== null);
  if(!filled.length){ alert('Aucun point contrôlé — commencez le diagnostic.'); return; }

  const f = dvGetFiche();
  const comment = document.getElementById('dvSumComment')?.value || '';
  const date = f.date || new Date().toLocaleDateString('fr-FR');

  let penalite = 0;
  filled.forEach(p => { penalite += DV_STATUS[dvState[p.id].status].score; });
  const score = Math.max(0, Math.round(100-(penalite/Math.max(filled.length*80,1))*100));
  const scoreCol = score>=80?'#22c55e':score>=60?'#f97316':'#ef4444';
  const counts = {ok:0,warn:0,fix:0,replace:0,danger:0};
  filled.forEach(p => { if(counts[dvState[p.id].status]!==undefined) counts[dvState[p.id].status]++; });

  const clientLine = [f.prenom, f.nom].filter(Boolean).join(' ');
  const veloLine   = [f.marque, f.modele, f.annee, f.type].filter(Boolean).join(' · ');

  // Texte brut
  let txt = 'RAPPORT DIAGNOSTIC VÉLO — VéloID\n';
  txt += '════════════════════════════════════\n';
  txt += 'Date : ' + date + '\n';
  if(f.atelier) txt += 'Atelier : ' + f.atelier + '\n';
  if(f.meca)    txt += 'Mécanicien : ' + f.meca + '\n';
  txt += '\nCLIENT\n────────────────────\n';
  if(clientLine) txt += 'Nom : ' + clientLine + '\n';
  if(f.tel)  txt += 'Tél : ' + f.tel + '\n';
  if(f.mail) txt += 'Email : ' + f.mail + '\n';
  txt += '\nVÉLO\n────────────────────\n';
  if(veloLine) txt += veloLine + '\n';
  if(f.couleur) txt += 'Couleur : ' + f.couleur + '\n';
  if(f.serie)   txt += 'N° série : ' + f.serie + '\n';
  if(f.km)      txt += 'Kilométrage : ' + parseInt(f.km).toLocaleString('fr-FR') + ' km\n';
  txt += '\nSCORE GÉNÉRAL : ' + score + '/100\n';
  txt += 'OK:' + counts.ok + ' / Surveiller:' + counts.warn + ' / Régler:' + counts.fix + ' / Remplacer:' + counts.replace + ' / Danger:' + counts.danger + '\n';
  txt += '\n════════════════════════════════════\nDÉTAIL DES POINTS\n════════════════════════════════════\n\n';

  ['danger','replace','fix','warn','ok'].forEach(st => {
    const pts = DV_POINTS.filter(p => dvState[p.id].status===st);
    if(!pts.length) return;
    txt += DV_STATUS[st].label.toUpperCase() + '\n';
    pts.forEach(p => {
      const s = dvState[p.id];
      const prio = s.priority==='urg'?' [URGENT]':s.priority==='med'?' [Moyen]':'';
      txt += '  ' + p.id + '. ' + p.name + prio + '\n';
      if(s.action) txt += '     → ' + s.action + '\n';
      if(s.note)   txt += '     ' + s.note + '\n';
    });
    txt += '\n';
  });

  if(comment) txt += 'COMMENTAIRE\n────────────────────\n' + comment + '\n\n';
  txt += '────────────────────────────────────\nGénéré par VéloID · ' + new Date().toLocaleString('fr-FR');

  dvShowExportModal(txt, f, score, scoreCol, counts, comment);
}

function dvShowExportModal(txt, f, score, scoreCol, counts, comment){
  const old = document.getElementById('dvExportModal');
  if(old) old.remove();

  const clientLine = [f.prenom, f.nom].filter(Boolean).join(' ');
  const veloLine   = [f.marque, f.modele, f.annee].filter(Boolean).join(' ');
  const date = f.date || new Date().toLocaleDateString('fr-FR');

  // Bouton mail — Web Share API ou fallback copie
  const mailHref = f.mail ? 'mailto:' + f.mail + '?subject=' + encodeURIComponent('Rapport diagnostic vélo' + (clientLine?' — '+clientLine:'') + (veloLine?' — '+veloLine:'')) : null;

  // Schéma SVG
  function buildSvg(){
    const el = document.getElementById('dvBikeSvg');
    if(!el) return '';
    const c = el.cloneNode(true);
    c.querySelectorAll('.dv-zone').forEach(z => z.remove());
    c.setAttribute('style','display:block;width:100%;max-width:460px;margin:0 auto');
    return c.outerHTML;
  }

  // Points HTML
  let pointsHtml = '';
  ['danger','replace','fix','warn','ok'].forEach(st => {
    const pts = DV_POINTS.filter(p => dvState[p.id].status===st);
    if(!pts.length) return;
    const S = DV_STATUS[st];
    pointsHtml += '<div style="margin-bottom:12px"><div style="font-size:10px;font-family:monospace;color:'+S.col+';letter-spacing:.1em;text-transform:uppercase;padding:5px 0;border-bottom:1px solid #1e1e1e;margin-bottom:6px">'+S.label+' ('+pts.length+')</div>';
    pts.forEach(p => {
      const s = dvState[p.id];
      const prio = s.priority==='urg'?'<span style="color:#ef4444;font-size:10px;margin-left:6px">🚨 URGENT</span>':s.priority==='med'?'<span style="color:#f97316;font-size:10px;margin-left:6px">⚠️ Moyen</span>':'';
      pointsHtml += '<div style="background:#141414;border-left:3px solid '+S.col+';border-radius:0 8px 8px 0;padding:8px 11px;margin-bottom:5px">';
      pointsHtml += '<div style="font-size:13px;color:#f0ede8;font-weight:600">'+p.id+'. '+p.name+prio+'</div>';
      if(s.action) pointsHtml += '<div style="font-size:12px;color:#9a9590;margin-top:3px">→ '+s.action+'</div>';
      if(s.note)   pointsHtml += '<div style="font-size:11px;color:#5a5550;margin-top:2px">'+s.note+'</div>';
      pointsHtml += '</div>';
    });
    pointsHtml += '</div>';
  });

  const modal = document.createElement('div');
  modal.id = 'dvExportModal';
  modal.style.cssText = 'position:fixed;inset:0;z-index:9999;background:rgba(0,0,0,.93);display:flex;flex-direction:column;padding:10px;';
  modal.innerHTML = `
    <div style="background:#111;border:1px solid #2a2a2a;border-radius:16px;display:flex;flex-direction:column;height:100%;overflow:hidden">
      <div style="padding:12px 14px;border-bottom:1px solid #1e1e1e;display:flex;align-items:center;gap:8px;flex-shrink:0;background:#0d0d0d">
        <div style="flex:1">
          <div style="font-family:'Syne',sans-serif;font-size:15px;font-weight:800;color:#f0ede8">📋 Rapport Diagnostic</div>
          <div style="font-size:11px;color:#5a5550;font-family:monospace">${date}${f.atelier?' · '+f.atelier:''}</div>
        </div>
        <button onclick="document.getElementById('dvExportModal').remove()"
          style="width:32px;height:32px;background:#1e1e1e;border:1px solid #333;border-radius:8px;color:#9a9590;font-size:16px;cursor:pointer">✕</button>
      </div>
      <div style="padding:10px 14px;border-bottom:1px solid #1e1e1e;display:flex;gap:8px;flex-shrink:0">
        <button onclick="dvCopyExport()" id="dvCopyExportBtn"
          style="flex:1;height:42px;background:#2d7ff9;border:none;border-radius:9px;font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:#fff;cursor:pointer">📋 Copier</button>
        <button onclick="dvShareReport()" id="dvShareBtn"
          style="flex:1;height:42px;background:#22c55e;border:none;border-radius:9px;font-family:'Syne',sans-serif;font-size:13px;font-weight:700;color:#fff;cursor:pointer">✉️ Partager</button>
      </div>
      <div style="flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:14px">

        <div style="background:#0d0d0d;border:1px solid #1e1e1e;border-radius:12px;padding:14px;margin-bottom:12px">
          <div style="font-family:'Syne',sans-serif;font-size:17px;font-weight:800;color:#f0ede8;margin-bottom:10px">VéloID — Rapport Diagnostic</div>
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
            <div style="background:#141414;border-radius:8px;padding:10px">
              <div style="font-size:9px;font-family:monospace;color:#2d7ff9;letter-spacing:.1em;text-transform:uppercase;margin-bottom:5px">CLIENT</div>
              ${clientLine?'<div style="font-size:13px;color:#f0ede8;font-weight:600">'+clientLine+'</div>':'<div style="font-size:12px;color:#5a5550">—</div>'}
              ${f.tel?'<div style="font-size:12px;color:#9a9590;margin-top:2px">📞 '+f.tel+'</div>':''}
              ${f.mail?'<div style="font-size:12px;color:#9a9590">✉️ '+f.mail+'</div>':''}
            </div>
            <div style="background:#141414;border-radius:8px;padding:10px">
              <div style="font-size:9px;font-family:monospace;color:#2d7ff9;letter-spacing:.1em;text-transform:uppercase;margin-bottom:5px">VÉLO</div>
              ${veloLine?'<div style="font-size:13px;color:#f0ede8;font-weight:600">'+veloLine+'</div>':'<div style="font-size:12px;color:#5a5550">—</div>'}
              ${f.type?'<div style="font-size:12px;color:#9a9590;margin-top:2px">'+f.type+'</div>':''}
              ${f.couleur?'<div style="font-size:12px;color:#9a9590">'+f.couleur+'</div>':''}
              ${f.km?'<div style="font-size:12px;color:#9a9590">'+parseInt(f.km).toLocaleString('fr-FR')+' km</div>':''}
              ${f.serie?'<div style="font-size:11px;color:#5a5550;font-family:monospace">'+f.serie+'</div>':''}
            </div>
          </div>
          ${f.meca?'<div style="font-size:11px;color:#5a5550;margin-top:8px;text-align:right;font-family:monospace">Mécanicien : '+f.meca+(f.atelier?' · '+f.atelier:'')+'</div>':''}
        </div>

        <div style="background:linear-gradient(135deg,#0d1f14,#0d0d0d);border:1px solid ${scoreCol}44;border-radius:12px;padding:14px;margin-bottom:12px;display:flex;align-items:center;gap:14px">
          <div style="text-align:center;flex-shrink:0">
            <div style="font-family:'Syne',sans-serif;font-size:44px;font-weight:800;color:${scoreCol};line-height:1">${score}</div>
            <div style="font-size:10px;color:#5a5550;font-family:monospace">/100</div>
          </div>
          <div style="flex:1;display:grid;grid-template-columns:repeat(5,1fr);gap:3px">
            <div style="background:#141414;border-radius:6px;padding:5px 2px;text-align:center"><div style="font-size:16px;font-weight:800;color:#22c55e">${counts.ok}</div><div style="font-size:8px;color:#5a5550">OK</div></div>
            <div style="background:#141414;border-radius:6px;padding:5px 2px;text-align:center"><div style="font-size:16px;font-weight:800;color:#eab308">${counts.warn}</div><div style="font-size:8px;color:#5a5550">Surv.</div></div>
            <div style="background:#141414;border-radius:6px;padding:5px 2px;text-align:center"><div style="font-size:16px;font-weight:800;color:#f97316">${counts.fix}</div><div style="font-size:8px;color:#5a5550">Régl.</div></div>
            <div style="background:#141414;border-radius:6px;padding:5px 2px;text-align:center"><div style="font-size:16px;font-weight:800;color:#ef4444">${counts.replace}</div><div style="font-size:8px;color:#5a5550">Rempl.</div></div>
            <div style="background:#141414;border-radius:6px;padding:5px 2px;text-align:center"><div style="font-size:16px;font-weight:800;color:#a855f7">${counts.danger}</div><div style="font-size:8px;color:#5a5550">Dang.</div></div>
          </div>
        </div>

        <div style="background:#0d0d0d;border:1px solid #1e1e1e;border-radius:12px;padding:10px;margin-bottom:12px">
          <div style="font-size:9px;font-family:monospace;color:#5a5550;letter-spacing:.1em;text-transform:uppercase;margin-bottom:8px;text-align:center">Localisation des anomalies</div>
          ${buildSvg()}
        </div>

        <div style="margin-bottom:12px">
          <div style="font-size:9px;font-family:monospace;color:#5a5550;letter-spacing:.1em;text-transform:uppercase;margin-bottom:8px">Détail des points</div>
          ${pointsHtml}
        </div>

        ${comment?'<div style="background:#141414;border:1px solid #1e1e1e;border-radius:10px;padding:12px;margin-bottom:12px"><div style="font-size:9px;font-family:monospace;color:#5a5550;letter-spacing:.1em;text-transform:uppercase;margin-bottom:6px">Commentaire mécanicien</div><div style="font-size:13px;color:#9a9590;line-height:1.7">'+comment+'</div></div>':''}

        <div style="text-align:center;font-family:monospace;font-size:10px;color:#2a2a2a;padding-top:8px">Généré par VéloID · ${new Date().toLocaleString('fr-FR')}</div>
      </div>
      <textarea id="dvExportTxt" style="position:absolute;left:-9999px;opacity:0" readonly>${txt}</textarea>
    </div>`;
  document.body.appendChild(modal);
}

function dvCopyExport(){
  const ta  = document.getElementById('dvExportTxt');
  const btn = document.getElementById('dvCopyExportBtn');
  if(!ta) return;
  const done = () => { btn.textContent='✓ Copié !'; setTimeout(()=>{ btn.textContent='📋 Copier'; },2500); };
  if(navigator.clipboard){ navigator.clipboard.writeText(ta.value).then(done).catch(()=>{ ta.select(); document.execCommand('copy'); done(); }); }
  else { ta.select(); ta.setSelectionRange(0,99999); document.execCommand('copy'); done(); }
}

function dvShareReport(){
  const ta  = document.getElementById('dvExportTxt');
  const btn = document.getElementById('dvShareBtn');
  if(!ta) return;
  const txt = ta.value;
  const f = dvGetFiche();
  const clientLine = [f.prenom, f.nom].filter(Boolean).join(' ');
  const veloLine   = [f.marque, f.modele].filter(Boolean).join(' ');
  const title = 'Rapport diagnostic vélo' + (clientLine?' — '+clientLine:'') + (veloLine?' — '+veloLine:'');

  // Web Share API — fonctionne sur Android Chrome et iOS Safari
  if(navigator.share){
    navigator.share({ title: title, text: txt })
      .then(() => { btn.textContent='✓ Partagé !'; setTimeout(()=>{ btn.textContent='✉️ Partager'; },2000); })
      .catch(() => {}); // Annulé par l'utilisateur
    return;
  }

  // Fallback : copie + ouvre mail vide avec sujet seulement
  const copied = () => {
    btn.textContent='✓ Copié !';
    setTimeout(()=>{ btn.textContent='✉️ Partager'; },2500);
    // Mail avec sujet pré-rempli, coller le rapport dans le corps
    const subject = encodeURIComponent(title);
    const mailTo  = f.mail ? 'mailto:'+f.mail+'?subject='+subject : 'mailto:?subject='+subject;
    setTimeout(() => { window.location.href = mailTo; }, 400);
  };

  if(navigator.clipboard){
    navigator.clipboard.writeText(txt).then(()=>{
      alert('Rapport copié !\n\nVotre application Mail va s\'ouvrir.\nCollez le rapport dans le corps du message (maintien du doigt → Coller).');
      copied();
    });
  } else {
    ta.select(); ta.setSelectionRange(0,99999); document.execCommand('copy');
    alert('Rapport copié !\n\nVotre application Mail va s\'ouvrir.\nCollez le rapport dans le corps du message.');
    copied();
  }
}

function dvReset(){
  if(!confirm('Réinitialiser tout le diagnostic ?')) return;
  dvInit();
  document.getElementById('dvSummary').style.display = 'none';
  DV_POINTS.forEach(p => {
    if(!p.zone) return;
    const m = document.getElementById(p.zone);
    if(m){
      m.setAttribute('fill','#1e1e1e');
      m.setAttribute('stroke','#555');
      m.setAttribute('r','10');
      const mId = p.zone.replace('dvM','dvMt');
      const t = document.getElementById(mId);
      if(t) t.setAttribute('fill','#9a9590');
    }
  });
  document.getElementById('dvScroll').scrollTo({ top:0, behavior:'smooth' });
}

// Init au chargement
document.addEventListener('DOMContentLoaded', () => { dvInit(); });


// SEARCH
// ═══════════════════════════════
function filterCards(){
  const q=document.getElementById('searchInput').value.toLowerCase();
  document.querySelectorAll('.diag-card').forEach(c=>{
    const t=(c.dataset.tags||'').toLowerCase()+c.querySelector('.dc-title').textContent.toLowerCase();
    c.style.display=(!q||t.includes(q))?'flex':'none';
  });
}


// ═══════════════════════════════
// INFO PANELS WITH SVG SCHEMAS
// ═══════════════════════════════
const INFO = {

erd:{
  title:'ERD — Diamètre efficace de la jante',
  svg:`<svg viewBox="0 0 320 200" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Jante vue de face -->
    <circle cx="160" cy="100" r="88" stroke="#333" stroke-width="8"/>
    <!-- Fond de jante (creux) -->
    <circle cx="160" cy="100" r="80" stroke="#22c55e" stroke-width="2.5" stroke-dasharray="6,4"/>
    <!-- Rayon exemple -->
    <line x1="160" y1="20" x2="160" y2="100" stroke="#e8400c" stroke-width="1.5" stroke-dasharray="4,3" opacity=".7"/>
    <circle cx="160" cy="20" r="4" fill="#e8400c"/>
    <text x="168" y="18" font-size="9" fill="#e8400c" font-family="monospace">Tête de rayon</text>
    <!-- Flèche ERD (diamètre complet) -->
    <line x1="80" y1="100" x2="240" y2="100" stroke="#22c55e" stroke-width="2"/>
    <line x1="80" y1="93" x2="80" y2="107" stroke="#22c55e" stroke-width="2"/>
    <line x1="240" y1="93" x2="240" y2="107" stroke="#22c55e" stroke-width="2"/>
    <text x="160" y="118" text-anchor="middle" font-size="11" fill="#22c55e" font-family="monospace" font-weight="bold">ERD</text>
    <!-- BSD (extérieur, pour comparaison) -->
    <line x1="72" y1="100" x2="72" y2="152" stroke="#555" stroke-width="1" stroke-dasharray="3,3"/>
    <line x1="248" y1="100" x2="248" y2="152" stroke="#555" stroke-width="1" stroke-dasharray="3,3"/>
    <line x1="72" y1="150" x2="248" y2="150" stroke="#555" stroke-width="1.5"/>
    <text x="160" y="165" text-anchor="middle" font-size="9" fill="#5a5550" font-family="monospace">BSD/ETRTO (≠ ERD)</text>
    <!-- Note -->
    <text x="160" y="186" text-anchor="middle" font-size="9" fill="#5a5550" font-family="monospace">ERD &lt; BSD de 10 à 20 mm</text>
  </svg>`,
  steps:[
    {t:'L\'ERD est le Ø intérieur <strong>au niveau où les têtes de rayon se posent</strong> (fond de jante), pas le BSD ETRTO.'},
    {t:'<strong>Méthode pratique :</strong> insérez 2 rayons connus de longueur L dans 2 trous opposés. ERD = 2L + espace entre les 2 têtes.'},
    {t:'Consultez le site du fabricant de jante — DT Swiss, Mavic et Sapim publient l\'ERD.'},
    {t:'<strong>Valeurs typiques :</strong> 700C ≈ 590–608mm · 29" ≈ 565–580mm · 27,5" ≈ 540–555mm'},
  ]
},

trous:{
  title:'Nombre de trous — Jante & Moyeu',
  svg:`<svg viewBox="0 0 320 180" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Jante -->
    <circle cx="160" cy="90" r="78" stroke="#333" stroke-width="6"/>
    <!-- 32 trous représentés (8 symboliques) -->
    <circle cx="160" cy="12" r="4" fill="#22c55e"/>
    <circle cx="215" cy="28" r="4" fill="#22c55e"/>
    <circle cx="248" cy="75" r="4" fill="#22c55e"/>
    <circle cx="238" cy="140" r="4" fill="#22c55e"/>
    <circle cx="190" cy="168" r="4" fill="#22c55e"/>
    <circle cx="130" cy="168" r="4" fill="#22c55e"/>
    <circle cx="82" cy="140" r="4" fill="#22c55e"/>
    <circle cx="72" cy="75" r="4" fill="#22c55e"/>
    <circle cx="105" cy="28" r="4" fill="#22c55e"/>
    <text x="160" y="90" text-anchor="middle" font-size="13" fill="#22c55e" font-family="monospace" font-weight="bold" dominant-baseline="middle">32 T</text>
    <!-- Moyeu -->
    <rect x="130" y="118" width="60" height="20" rx="4" fill="#1e1e1e" stroke="#e8400c" stroke-width="2"/>
    <circle cx="140" cy="128" r="3" fill="#e8400c"/>
    <circle cx="148" cy="128" r="3" fill="#e8400c"/>
    <circle cx="156" cy="128" r="3" fill="#e8400c"/>
    <circle cx="164" cy="128" r="3" fill="#2d7ff9"/>
    <circle cx="172" cy="128" r="3" fill="#2d7ff9"/>
    <circle cx="180" cy="128" r="3" fill="#2d7ff9"/>
    <text x="140" y="148" font-size="8" fill="#e8400c" font-family="monospace">G: 16T</text>
    <text x="170" y="148" font-size="8" fill="#2d7ff9" font-family="monospace">D: 16T</text>
    <text x="160" y="170" text-anchor="middle" font-size="9" fill="#5a5550" font-family="monospace">Jante = G + D du moyeu</text>
  </svg>`,
  steps:[
    {t:'Comptez les trous dans la jante. <strong>Standards courants : 24 · 28 · 32 · 36</strong> trous.'},
    {t:'Le moyeu a des trous de chaque côté — la somme G+D doit égaler le nombre de trous jante.'},
    {t:'⚠️ Jante et moyeu <strong>doivent avoir le même nombre</strong> de trous.'},
  ]
},

osb:{
  title:'OSB — Offset du lit de rayon (jante asymétrique)',
  svg:`<svg viewBox="0 0 320 170" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Jante symétrique (gauche) -->
    <text x="70" y="18" text-anchor="middle" font-size="10" fill="#9a9590" font-family="monospace">STANDARD</text>
    <rect x="20" y="25" width="100" height="100" rx="4" fill="#1e1e1e" stroke="#333" stroke-width="1.5"/>
    <!-- Centre jante -->
    <line x1="70" y1="25" x2="70" y2="125" stroke="#555" stroke-width="1" stroke-dasharray="4,3"/>
    <!-- Fond de jante centré -->
    <rect x="30" y="70" width="80" height="8" rx="2" fill="#22c55e" opacity=".6"/>
    <!-- Trous symétriques -->
    <circle cx="70" cy="74" r="3" fill="#22c55e"/>
    <text x="45" y="100" font-size="8" fill="#9a9590" font-family="monospace">OSB = 0</text>
    <!-- Flancs cassette -->
    <text x="22" y="140" font-size="8" fill="#e8400c" font-family="monospace">G</text>
    <text x="112" y="140" font-size="8" fill="#2d7ff9" font-family="monospace">D</text>

    <!-- Jante asymétrique (droite) -->
    <text x="250" y="18" text-anchor="middle" font-size="10" fill="#f59e0b" font-family="monospace">ASYMÉTRIQUE</text>
    <rect x="200" y="25" width="100" height="100" rx="4" fill="#1e1e1e" stroke="#333" stroke-width="1.5"/>
    <!-- Centre jante -->
    <line x1="250" y1="25" x2="250" y2="125" stroke="#555" stroke-width="1" stroke-dasharray="4,3"/>
    <!-- Fond de jante décalé -->
    <rect x="210" y="70" width="80" height="8" rx="2" fill="#f59e0b" opacity=".5"/>
    <!-- Trous décalés -->
    <circle cx="258" cy="74" r="3" fill="#f59e0b"/>
    <!-- Flèche offset -->
    <line x1="250" y1="74" x2="258" y2="74" stroke="#f59e0b" stroke-width="1.5"/>
    <line x1="255" y1="70" x2="258" y2="74" stroke="#f59e0b" stroke-width="1.5"/>
    <text x="262" y="72" font-size="8" fill="#f59e0b" font-family="monospace">OSB</text>
    <text x="215" y="100" font-size="8" fill="#f59e0b" font-family="monospace">OSB ≠ 0</text>
    <text x="202" y="140" font-size="8" fill="#e8400c" font-family="monospace">G</text>
    <text x="292" y="140" font-size="8" fill="#2d7ff9" font-family="monospace">D</text>
    <!-- Légende bas -->
    <text x="160" y="162" text-anchor="middle" font-size="9" fill="#5a5550" font-family="monospace">Compense le décentrage de la cassette</text>
  </svg>`,
  steps:[
    {t:'Une jante standard a l\'OSB = 0 (trous au centre). <strong>Entrez 0 si standard.</strong>'},
    {t:'Une jante asymétrique a les trous décalés vers le côté gauche pour équilibrer la tension avec la cassette.'},
    {t:'L\'OSB est indiqué par le fabricant (ex : Mavic Ksyrium : +3,5mm). Consultez les specs de votre jante.'},
  ]
},

old:{
  title:'OLD — Over Locknut Dimension',
  svg:`<svg viewBox="0 0 320 160" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Moyeu complet vue de face -->
    <rect x="60" y="55" width="200" height="50" rx="5" fill="#1e1e1e" stroke="#333" stroke-width="2"/>
    <!-- Corps moyeu -->
    <rect x="110" y="60" width="100" height="40" rx="3" fill="#242424"/>
    <!-- Flasque G -->
    <rect x="80" y="62" width="30" height="36" rx="3" fill="#e8400c" opacity=".5" stroke="#e8400c" stroke-width="1.5"/>
    <!-- Flasque D -->
    <rect x="210" y="62" width="30" height="36" rx="3" fill="#2d7ff9" opacity=".5" stroke="#2d7ff9" stroke-width="1.5"/>
    <!-- Écrous de blocage -->
    <rect x="60" y="63" width="20" height="34" rx="2" fill="#555" stroke="#777" stroke-width="1"/>
    <rect x="240" y="63" width="20" height="34" rx="2" fill="#555" stroke="#777" stroke-width="1"/>
    <!-- Labels -->
    <text x="70" y="55" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">Écrou</text>
    <text x="250" y="55" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">Écrou</text>
    <!-- Flèche OLD (faces externes des écrous) -->
    <line x1="60" y1="118" x2="260" y2="118" stroke="#f59e0b" stroke-width="2"/>
    <line x1="60" y1="111" x2="60" y2="125" stroke="#f59e0b" stroke-width="2.5"/>
    <line x1="260" y1="111" x2="260" y2="125" stroke="#f59e0b" stroke-width="2.5"/>
    <text x="160" y="138" text-anchor="middle" font-size="12" fill="#f59e0b" font-family="monospace" font-weight="bold">OLD</text>
    <!-- Flèche OLD patte cadre (différente) -->
    <text x="160" y="153" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">Face ext. écrou → face ext. écrou</text>
    <!-- Labels flancs -->
    <text x="96" y="85" text-anchor="middle" font-size="9" fill="#e8400c" font-family="monospace">G</text>
    <text x="224" y="85" text-anchor="middle" font-size="9" fill="#2d7ff9" font-family="monospace">D</text>
  </svg>`,
  steps:[
    {t:'L\'OLD se mesure entre les <strong>faces externes</strong> des deux écrous de blocage du moyeu.'},
    {t:'C\'est la dimension du <strong>moyeu lui-même</strong> — pas la distance entre les pattes du cadre.'},
    {t:'Utilisez un pied à coulisse ou une règle rigide.'},
    {t:'<strong>Standards courants :</strong> Avant 100mm · Avant Boost 110mm · Arrière route 130mm · Arrière VTT 142mm · Boost 148mm'},
  ]
},

pcdg:{
  title:'PCD Gauche — Cercle de perçage flanc gauche',
  svg:`<svg viewBox="0 0 320 190" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Vue de face du flanc du moyeu -->
    <circle cx="160" cy="95" r="75" stroke="#333" stroke-width="3" fill="#1e1e1e"/>
    <!-- Corps moyeu (centre) -->
    <circle cx="160" cy="95" r="22" fill="#242424" stroke="#444" stroke-width="2"/>
    <!-- Cercle PCD gauche -->
    <circle cx="160" cy="95" r="48" stroke="#e8400c" stroke-width="2" stroke-dasharray="6,4"/>
    <!-- Trous du flanc gauche (6 trous) -->
    <circle cx="160" cy="47" r="5" fill="#e8400c"/>
    <circle cx="202" cy="71" r="5" fill="#e8400c"/>
    <circle cx="202" cy="119" r="5" fill="#e8400c"/>
    <circle cx="160" cy="143" r="5" fill="#e8400c"/>
    <circle cx="118" cy="119" r="5" fill="#e8400c"/>
    <circle cx="118" cy="71" r="5" fill="#e8400c"/>
    <!-- Flèche PCD (rayon du cercle × 2) -->
    <line x1="160" y1="95" x2="160" y2="47" stroke="#e8400c" stroke-width="1.5" stroke-dasharray="4,3"/>
    <text x="164" y="75" font-size="9" fill="#e8400c" font-family="monospace">PCD/2</text>
    <!-- Mesure centre-à-centre -->
    <line x1="160" y1="47" x2="202" y2="71" stroke="#f59e0b" stroke-width="2"/>
    <text x="188" y="52" font-size="9" fill="#f59e0b" font-family="monospace">mesurer ici</text>
    <!-- Label -->
    <text x="160" y="178" text-anchor="middle" font-size="10" fill="#e8400c" font-family="monospace" font-weight="bold">PCD GAUCHE</text>
    <text x="160" y="190" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">Ø du cercle passant par les trous</text>
  </svg>`,
  steps:[
    {t:'Le PCD gauche est le <strong>diamètre du cercle</strong> passant par tous les trous du flanc gauche.'},
    {t:'<strong>Méthode :</strong> mesurez de centre à centre entre 2 trous opposés (si nombre pair). C\'est directement le PCD.'},
    {t:'Si nombre impair de trous : mesurez entre deux trous adjacents et divisez par sin(π/n).'},
    {t:'Valeurs typiques : 38–48mm pour VTT · 44–60mm pour route.'},
  ]
},

pcdd:{
  title:'PCD Droit — Cercle de perçage flanc droit',
  svg:`<svg viewBox="0 0 320 190" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Vue de face du flanc D -->
    <circle cx="160" cy="95" r="75" stroke="#333" stroke-width="3" fill="#1e1e1e"/>
    <circle cx="160" cy="95" r="22" fill="#242424" stroke="#444" stroke-width="2"/>
    <!-- Cercle PCD droit (souvent plus petit côté cassette) -->
    <circle cx="160" cy="95" r="38" stroke="#2d7ff9" stroke-width="2" stroke-dasharray="6,4"/>
    <!-- Trous flanc D -->
    <circle cx="160" cy="57" r="5" fill="#2d7ff9"/>
    <circle cx="195" cy="76" r="5" fill="#2d7ff9"/>
    <circle cx="195" cy="114" r="5" fill="#2d7ff9"/>
    <circle cx="160" cy="133" r="5" fill="#2d7ff9"/>
    <circle cx="125" cy="114" r="5" fill="#2d7ff9"/>
    <circle cx="125" cy="76" r="5" fill="#2d7ff9"/>
    <!-- Rayon PCD -->
    <line x1="160" y1="95" x2="160" y2="57" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="4,3"/>
    <text x="164" y="78" font-size="9" fill="#2d7ff9" font-family="monospace">PCD/2</text>
    <!-- Cassette -->
    <rect x="140" y="150" width="40" height="12" rx="2" fill="#333" stroke="#555" stroke-width="1"/>
    <text x="160" y="159" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace">cassette</text>
    <text x="160" y="178" text-anchor="middle" font-size="10" fill="#2d7ff9" font-family="monospace" font-weight="bold">PCD DROIT</text>
    <text x="160" y="190" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">Souvent ≠ PCD gauche (cassette)</text>
  </svg>`,
  steps:[
    {t:'Le PCD droit est souvent <strong>différent du PCD gauche</strong> sur les moyeux arrière à cause de la cassette.'},
    {t:'Même méthode : mesurez centre-à-centre entre deux trous opposés sur le flanc droit uniquement.'},
    {t:'Sur un moyeu arrière Boost, la différence PCD G / PCD D peut être de 10–15mm.'},
  ]
},

dg:{
  title:'Distance flanc G → plan central du moyeu',
  svg:`<svg viewBox="0 0 320 170" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Moyeu vue de côté (coupe) -->
    <rect x="40" y="55" width="240" height="60" rx="5" fill="#1e1e1e" stroke="#333" stroke-width="2"/>
    <!-- Corps intérieur -->
    <rect x="110" y="60" width="100" height="50" rx="3" fill="#242424"/>
    <!-- Flanc G -->
    <rect x="68" y="60" width="18" height="50" rx="3" fill="#e8400c" opacity=".6" stroke="#e8400c" stroke-width="1.5"/>
    <!-- Flanc D -->
    <rect x="234" y="60" width="18" height="50" rx="3" fill="#2d7ff9" opacity=".6" stroke="#2d7ff9" stroke-width="1.5"/>
    <!-- Écrous -->
    <rect x="40" y="62" width="28" height="46" rx="2" fill="#444" stroke="#666" stroke-width="1"/>
    <rect x="252" y="62" width="28" height="46" rx="2" fill="#444" stroke="#666" stroke-width="1"/>
    <!-- Plan central -->
    <line x1="160" y1="40" x2="160" y2="130" stroke="#22c55e" stroke-width="1.5" stroke-dasharray="5,3"/>
    <text x="160" y="36" text-anchor="middle" font-size="9" fill="#22c55e" font-family="monospace">Plan central</text>
    <!-- Flèche distance G -->
    <line x1="77" y1="140" x2="160" y2="140" stroke="#e8400c" stroke-width="2"/>
    <line x1="77" y1="134" x2="77" y2="146" stroke="#e8400c" stroke-width="2"/>
    <line x1="160" y1="134" x2="160" y2="146" stroke="#e8400c" stroke-width="2"/>
    <text x="118" y="157" text-anchor="middle" font-size="10" fill="#e8400c" font-family="monospace" font-weight="bold">Dist. G</text>
    <!-- OLD pour référence -->
    <line x1="40" y1="120" x2="280" y2="120" stroke="#f59e0b" stroke-width="1" stroke-dasharray="4,3"/>
    <text x="280" y="118" font-size="8" fill="#f59e0b" font-family="monospace">OLD/2</text>
  </svg>`,
  steps:[
    {t:'C\'est la distance entre le <strong>plan médian du flanc gauche</strong> et le <strong>plan central du moyeu</strong>.'},
    {t:'Plan central = à égale distance des deux faces externes d\'écrou (OLD ÷ 2 depuis chaque face).'},
    {t:'<strong>Calcul :</strong> Dist G = (OLD ÷ 2) − distance de la face externe G au flanc G.'},
    {t:'Sur un moyeu arrière : Dist G est toujours <strong>plus grande</strong> que Dist D (cassette pousse le plan à droite).'},
  ]
},

dd:{
  title:'Distance flanc D → plan central du moyeu',
  svg:`<svg viewBox="0 0 320 170" fill="none" xmlns="http://www.w3.org/2000/svg">
    <rect x="40" y="55" width="240" height="60" rx="5" fill="#1e1e1e" stroke="#333" stroke-width="2"/>
    <rect x="110" y="60" width="100" height="50" rx="3" fill="#242424"/>
    <rect x="68" y="60" width="18" height="50" rx="3" fill="#e8400c" opacity=".6" stroke="#e8400c" stroke-width="1.5"/>
    <rect x="234" y="60" width="18" height="50" rx="3" fill="#2d7ff9" opacity=".6" stroke="#2d7ff9" stroke-width="1.5"/>
    <rect x="40" y="62" width="28" height="46" rx="2" fill="#444" stroke="#666" stroke-width="1"/>
    <rect x="252" y="62" width="28" height="46" rx="2" fill="#444" stroke="#666" stroke-width="1"/>
    <!-- Cassette symbole -->
    <rect x="250" y="35" width="28" height="22" rx="2" fill="#333" stroke="#555" stroke-width="1"/>
    <text x="264" y="50" text-anchor="middle" font-size="7" fill="#9a9590" font-family="monospace">cassette</text>
    <!-- Plan central -->
    <line x1="160" y1="40" x2="160" y2="130" stroke="#22c55e" stroke-width="1.5" stroke-dasharray="5,3"/>
    <text x="160" y="36" text-anchor="middle" font-size="9" fill="#22c55e" font-family="monospace">Plan central</text>
    <!-- Flèche distance D (plus petite) -->
    <line x1="160" y1="140" x2="243" y2="140" stroke="#2d7ff9" stroke-width="2"/>
    <line x1="160" y1="134" x2="160" y2="146" stroke="#2d7ff9" stroke-width="2"/>
    <line x1="243" y1="134" x2="243" y2="146" stroke="#2d7ff9" stroke-width="2"/>
    <text x="201" y="157" text-anchor="middle" font-size="10" fill="#2d7ff9" font-family="monospace" font-weight="bold">Dist. D</text>
    <text x="160" y="170" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">Dist. D &lt; Dist. G (moyeu arrière asymétrique)</text>
  </svg>`,
  steps:[
    {t:'Distance entre le <strong>plan médian du flanc droit</strong> et le <strong>plan central du moyeu</strong>.'},
    {t:'Sur un moyeu arrière, la cassette réduit cette distance — Dist D est <strong>plus petite</strong> que Dist G.'},
    {t:'<strong>Calcul :</strong> Dist D = (OLD ÷ 2) − distance de la face externe D au flanc D.'},
    {t:'Conséquence : les rayons côté droit sont plus courts et plus tendus → jante décentrée si compensation insuffisante.'},
  ]
},

crois:{
  title:'Nombre de croisements',
  svg:`<svg viewBox="0 0 320 190" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Radial -->
    <circle cx="60" cy="85" r="50" stroke="#333" stroke-width="2"/>
    <circle cx="60" cy="85" r="10" fill="#242424" stroke="#555" stroke-width="2"/>
    <line x1="60" y1="35" x2="60" y2="75" stroke="#e8400c" stroke-width="1.5"/>
    <line x1="60" y1="95" x2="60" y2="135" stroke="#e8400c" stroke-width="1.5"/>
    <line x1="10" y1="85" x2="50" y2="85" stroke="#e8400c" stroke-width="1.5"/>
    <line x1="70" y1="85" x2="110" y2="85" stroke="#e8400c" stroke-width="1.5"/>
    <line x1="24" y1="49" x2="53" y2="78" stroke="#e8400c" stroke-width="1.5"/>
    <line x1="67" y1="92" x2="96" y2="121" stroke="#e8400c" stroke-width="1.5"/>
    <text x="60" y="148" text-anchor="middle" font-size="10" fill="#e8400c" font-family="monospace">0× Radial</text>
    <text x="60" y="160" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">CLM · léger</text>

    <!-- 2 croisements -->
    <circle cx="180" cy="85" r="50" stroke="#333" stroke-width="2"/>
    <circle cx="180" cy="85" r="10" fill="#242424" stroke="#555" stroke-width="2"/>
    <line x1="180" y1="35" x2="209" y2="112" stroke="#f59e0b" stroke-width="1.5"/>
    <line x1="180" y1="35" x2="151" y2="112" stroke="#f59e0b" stroke-width="1.5" opacity=".6"/>
    <circle cx="196" cy="80" r="3" fill="#f59e0b"/>
    <text x="180" y="148" text-anchor="middle" font-size="10" fill="#f59e0b" font-family="monospace">2× Croisements</text>
    <text x="180" y="160" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">XC · léger</text>

    <!-- 3 croisements -->
    <circle cx="300" cy="85" r="50" stroke="#333" stroke-width="2"/>
    <circle cx="300" cy="85" r="10" fill="#242424" stroke="#555" stroke-width="2"/>
    <line x1="300" y1="35" x2="268" y2="130" stroke="#22c55e" stroke-width="2"/>
    <line x1="300" y1="35" x2="332" y2="112" stroke="#22c55e" stroke-width="2" opacity=".6"/>
    <circle cx="284" cy="77" r="3" fill="#22c55e"/>
    <circle cx="276" cy="100" r="3" fill="#22c55e"/>
    <text x="300" y="148" text-anchor="middle" font-size="10" fill="#22c55e" font-family="monospace">3× Croisements</text>
    <text x="300" y="160" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">Standard · robuste</text>
    <!-- Note bas -->
    <text x="160" y="178" text-anchor="middle" font-size="9" fill="#5a5550" font-family="monospace">Plus de croisements = plus robuste, légèrement plus lourd</text>
  </svg>`,
  steps:[
    {t:'<strong>Radial (0×)</strong> : rayons droits du moyeu à la jante. Très léger mais fragile — jamais côté cassette.'},
    {t:'<strong>2 croisements</strong> : bon compromis légèreté/rigidité. VTT XC performance.'},
    {t:'<strong>3 croisements</strong> : standard universel, recommandé pour la plupart des usages.'},
    {t:'<strong>4 croisements</strong> : DH, cargo, roues motorisées. Très robuste.'},
  ]
},

trou:{
  title:'Trou de départ du rayon',
  svg:`<svg viewBox="0 0 320 160" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Vue moyeu + jante simplifiée -->
    <circle cx="160" cy="80" r="70" stroke="#333" stroke-width="2"/>
    <circle cx="160" cy="80" r="18" fill="#242424" stroke="#555" stroke-width="2"/>
    <!-- Valve -->
    <rect x="226" y="72" width="8" height="16" rx="2" fill="#f59e0b"/>
    <text x="243" y="83" font-size="9" fill="#f59e0b" font-family="monospace">Valve</text>
    <!-- Trou 1 (juste à droite de valve) -->
    <circle cx="224" cy="48" r="5" fill="#22c55e"/>
    <text x="232" y="45" font-size="9" fill="#22c55e" font-family="monospace">Trou 1 → ici</text>
    <!-- Trou 2 -->
    <circle cx="204" cy="28" r="5" fill="#e8400c" opacity=".6"/>
    <text x="210" y="25" font-size="9" fill="#e8400c" font-family="monospace">Trou 2</text>
    <!-- Rayon depuis trou 1 -->
    <line x1="224" y1="48" x2="170" y2="72" stroke="#22c55e" stroke-width="1.5" stroke-dasharray="4,3"/>
    <text x="160" y="145" text-anchor="middle" font-size="9" fill="#5a5550" font-family="monospace">Le premier rayon part du trou à droite de la valve</text>
  </svg>`,
  steps:[
    {t:'Définit si le premier rayon part du <strong>1er trou à droite de la valve</strong> ou du 2e.'},
    {t:'En pratique : laissez <strong>"Premier trou"</strong> pour la quasi-totalité des montages.'},
    {t:'L\'influence sur la longueur calculée est inférieure à 0,5mm.'},
  ]
},

};

function showTip(id){
  const info=INFO[id];
  if(!info) return;
  document.getElementById('infoTitle').textContent=info.title;
  document.getElementById('infoSvg').innerHTML=info.svg||'';
  let body='';
  (info.steps||[]).forEach((s,i)=>{
    body+=`<div class="step-row"><div class="step-num">${i+1}</div><div>${s.t}</div></div>`;
  });
  document.getElementById('infoBody').innerHTML=body;
  document.getElementById('infoOverlay').classList.add('show');
}
function closeInfo(){ document.getElementById('infoOverlay').classList.remove('show'); }
// legacy alias
function closeTip(){ closeInfo(); }

// ═══════════════════════════════
// RAYONNAGE CALCULATOR
// ═══════════════════════════════
function calcRayonnage(){
  const erd=parseFloat(document.getElementById('r_erd').value);
  const n=parseInt(document.getElementById('r_trous').value);
  const osb=parseFloat(document.getElementById('r_osb').value)||0;
  const old=parseFloat(document.getElementById('r_old').value);
  const pcdG=parseFloat(document.getElementById('r_pcdg').value);
  const pcdD=parseFloat(document.getElementById('r_pcdd').value);
  const dg=parseFloat(document.getElementById('r_dg').value);
  const dd=parseFloat(document.getElementById('r_dd').value);
  const cross=parseInt(document.getElementById('r_cross').value);
  const hole=parseInt(document.getElementById('r_hole').value);

  if([erd,n,old,pcdG,pcdD,dg,dd].some(isNaN)){
    alert('⚠️ Veuillez remplir tous les champs obligatoires.');
    return;
  }

  // Rayon ERD
  const R = erd / 2;

  // Calcul pour chaque flanc
  // Formule : L = √( R² + (PCD/2)² - 2·R·(PCD/2)·cos(2π·cross/n·hole) ) + correction_latérale
  // correction latérale : distance du flanc au plan central de la jante tenant compte offset
  // Plan central jante = 0, plan central moyeu = 0
  // Distance flanc G au centre jante = dg (positif côté G)
  // Offset OSB déplace le centre jante

  function calcLen(pcd, dist_flanc, sideSign){
    const r_flange = pcd / 2;
    // Angle entre 2 trous jante côté = 2π/n * croisements (avec correction trou)
    const angle = 2 * Math.PI * cross * hole / (n / 2);
    // Distance latérale : dist flanc + décalage OSB
    const lat = dist_flanc + sideSign * osb / 2;
    // Longueur rayon en 3D
    const L = Math.sqrt(
      Math.pow(R, 2) +
      Math.pow(r_flange, 2) -
      2 * R * r_flange * Math.cos(angle) +
      Math.pow(lat, 2)
    );
    return L;
  }

  const lenG = calcLen(pcdG, dg, -1);
  const lenD = calcLen(pcdD, dd, +1);

  // Arrondi au 0.5 inférieur
  const roundDown = v => Math.floor(v * 2) / 2;
  const lgR = roundDown(lenG);
  const ldR = roundDown(lenD);

  // Tension théorique relative (ratio PCD / dist)
  const tensRatio = (pcdD * dg) / (pcdG * dd);

  const res = document.getElementById('rayResult');
  const lens = document.getElementById('rayLengths');
  const dets = document.getElementById('rayDetails');

  lens.innerHTML = `
    <div class="ray-len-card" style="border-left:3px solid #e8400c">
      <div class="ray-len-label">◀ Côté Gauche</div>
      <div class="ray-len-val">${lgR.toFixed(1)}<span class="ray-len-unit"> mm</span></div>
      <div class="ray-len-sub">Brut : ${lenG.toFixed(2)} mm</div>
    </div>
    <div class="ray-len-card" style="border-left:3px solid #2d7ff9">
      <div class="ray-len-label">Côté Droit ▶</div>
      <div class="ray-len-val">${ldR.toFixed(1)}<span class="ray-len-unit"> mm</span></div>
      <div class="ray-len-sub">Brut : ${lenD.toFixed(2)} mm</div>
    </div>`;

  dets.innerHTML = `
    <div class="ray-dg-item"><div class="ray-dg-label">ERD jante</div><div class="ray-dg-val">${erd} mm</div></div>
    <div class="ray-dg-item"><div class="ray-dg-label">Trous / Croisements</div><div class="ray-dg-val">${n}T · ${cross}×</div></div>
    <div class="ray-dg-item"><div class="ray-dg-label">PCD gauche</div><div class="ray-dg-val">${pcdG} mm</div></div>
    <div class="ray-dg-item"><div class="ray-dg-label">PCD droit</div><div class="ray-dg-val">${pcdD} mm</div></div>
    <div class="ray-dg-item"><div class="ray-dg-label">Offset OSB</div><div class="ray-dg-val">${osb} mm</div></div>
    <div class="ray-dg-item"><div class="ray-dg-label">Ratio tension D/G</div><div class="ray-dg-val">${tensRatio.toFixed(2)}×${tensRatio<0.7?' ⚠️':tensRatio>1.5?' ⚠️':' ✓'}</div></div>`;

  res.classList.add('show');
  document.getElementById('rayResult').scrollIntoView({behavior:'smooth',block:'nearest'});
  // Calcul tension
  if(typeof calcTensionRayons==='function') calcTensionRayons(lenG,lenD,dg,dd,pcdG,pcdD,old);
}

// ═══════════════════════════════
// DIAG DATA
// ═══════════════════════════════
const DIAGS = {

bb:{title:'Boîtier Pédalier',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#e8400c" stroke-width="2"/><circle cx="14" cy="14" r="5" stroke="#9a9590" stroke-width="1.5"/><line x1="4" y1="14" x2="9" y2="14" stroke="#e8400c" stroke-width="2"/><line x1="19" y1="14" x2="24" y2="14" stroke="#e8400c" stroke-width="2"/><rect x="1" y="12" width="3" height="4" rx="1" fill="#9a9590"/><rect x="24" y="12" width="3" height="4" rx="1" fill="#9a9590"/></svg>`,
intro:{
  title:'Boîtier Pédalier — Vue éclatée',
  svg:`<svg viewBox="0 0 320 380" fill="none" xmlns="http://www.w3.org/2000/svg">
    <text x="160" y="16" text-anchor="middle" font-size="11" fill="#9a9590" font-family="monospace" font-weight="bold">BOTTOM BRACKET / BOÎTIER DE PÉDALIER</text>

    <!-- Manivelle droite / Right crank -->
    <rect x="200" y="30" width="100" height="20" rx="4" fill="#242424" stroke="#555" stroke-width="1.5"/>
    <circle cx="308" cy="40" r="8" fill="#333" stroke="#555" stroke-width="1.5"/>
    <line x1="195" y1="40" x2="155" y2="40" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="10" y="37" font-size="9" fill="#22c55e" font-family="monospace">Manivelle D</text>
    <text x="10" y="48" font-size="8" fill="#5a5550" font-family="monospace">Right Crank Arm</text>

    <!-- Axe pédalier / Spindle -->
    <rect x="100" y="65" width="120" height="14" rx="5" fill="#e8400c" opacity=".7" stroke="#e8400c" stroke-width="1.5"/>
    <line x1="225" y1="72" x2="270" y2="72" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="273" y="69" font-size="9" fill="#e8400c" font-family="monospace">Axe pédalier</text>
    <text x="273" y="80" font-size="8" fill="#5a5550" font-family="monospace">Spindle / Axle</text>

    <!-- Roulement D / Right bearing -->
    <ellipse cx="210" cy="100" rx="28" ry="14" fill="#1a1a1a" stroke="#f59e0b" stroke-width="2"/>
    <ellipse cx="210" cy="100" rx="14" ry="7" fill="none" stroke="#f59e0b" stroke-width="1" stroke-dasharray="3,2"/>
    <line x1="240" y1="100" x2="270" y2="100" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="273" y="97" font-size="9" fill="#f59e0b" font-family="monospace">Roulement D</text>
    <text x="273" y="108" font-size="8" fill="#5a5550" font-family="monospace">Right Bearing</text>

    <!-- Boîtier (coque D) / Right shell -->
    <rect x="180" y="125" width="60" height="34" rx="4" fill="#2d7ff9" opacity=".5" stroke="#2d7ff9" stroke-width="2"/>
    <line x1="245" y1="142" x2="270" y2="142" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="273" y="139" font-size="9" fill="#2d7ff9" font-family="monospace">Coque D (filetée G)</text>
    <text x="273" y="150" font-size="8" fill="#5a5550" font-family="monospace">Right Cup / Shell</text>

    <!-- Tube pédalier cadre / BB Shell (frame) -->
    <rect x="100" y="165" width="120" height="50" rx="3" fill="#161616" stroke="#444" stroke-width="2.5"/>
    <!-- Filetage symbolisé -->
    <line x1="100" y1="175" x2="220" y2="175" stroke="#333" stroke-width="1"/>
    <line x1="100" y1="182" x2="220" y2="182" stroke="#333" stroke-width="1"/>
    <line x1="100" y1="189" x2="220" y2="189" stroke="#333" stroke-width="1"/>
    <line x1="100" y1="196" x2="220" y2="196" stroke="#333" stroke-width="1"/>
    <line x1="40" y1="190" x2="97" y2="190" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="10" y="182" font-size="9" fill="#9a9590" font-family="monospace">Tube</text>
    <text x="10" y="193" font-size="9" fill="#9a9590" font-family="monospace">pédalier</text>
    <text x="10" y="205" font-size="8" fill="#5a5550" font-family="monospace">BB Shell (frame)</text>

    <!-- Coque G / Left cup -->
    <rect x="80" y="225" width="60" height="34" rx="4" fill="#e8400c" opacity=".5" stroke="#e8400c" stroke-width="2"/>
    <line x1="75" y1="242" x2="40" y2="242" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="10" y="239" font-size="9" fill="#e8400c" font-family="monospace">Coque G</text>
    <text x="10" y="250" font-size="8" fill="#5a5550" font-family="monospace">Left Cup / Shell</text>

    <!-- Roulement G / Left bearing -->
    <ellipse cx="110" cy="275" rx="28" ry="14" fill="#1a1a1a" stroke="#f59e0b" stroke-width="2"/>
    <ellipse cx="110" cy="275" rx="14" ry="7" fill="none" stroke="#f59e0b" stroke-width="1" stroke-dasharray="3,2"/>
    <line x1="80" y1="275" x2="40" y2="275" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="10" y="272" font-size="9" fill="#f59e0b" font-family="monospace">Roulement G</text>
    <text x="10" y="283" font-size="8" fill="#5a5550" font-family="monospace">Left Bearing</text>

    <!-- Manivelle G / Left crank -->
    <rect x="20" y="305" width="100" height="20" rx="4" fill="#242424" stroke="#555" stroke-width="1.5"/>
    <circle cx="12" cy="315" r="8" fill="#333" stroke="#555" stroke-width="1.5"/>
    <line x1="125" y1="315" x2="155" y2="315" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="160" y="312" font-size="9" fill="#22c55e" font-family="monospace">Manivelle G</text>
    <text x="160" y="323" font-size="8" fill="#5a5550" font-family="monospace">Left Crank Arm</text>

    <!-- Flèche Ø BB Shell -->
    <line x1="235" y1="165" x2="235" y2="215" stroke="#22c55e" stroke-width="1.5"/>
    <line x1="229" y1="165" x2="241" y2="165" stroke="#22c55e" stroke-width="1.5"/>
    <line x1="229" y1="215" x2="241" y2="215" stroke="#22c55e" stroke-width="1.5"/>
    <text x="250" y="192" font-size="9" fill="#22c55e" font-family="monospace">Ø+L</text>
    <text x="245" y="204" font-size="8" fill="#22c55e" font-family="monospace">à mesurer</text>

    <!-- Légende -->
    <rect x="10" y="348" width="10" height="8" rx="1" fill="#e8400c" opacity=".7"/>
    <text x="24" y="356" font-size="8" fill="#9a9590" font-family="monospace">Coupelles / Cups</text>
    <rect x="120" y="348" width="10" height="8" rx="1" fill="#f59e0b"/>
    <text x="134" y="356" font-size="8" fill="#9a9590" font-family="monospace">Roulements / Bearings</text>
    <rect x="240" y="348" width="10" height="8" rx="1" fill="#e8400c"/>
    <text x="254" y="356" font-size="8" fill="#9a9590" font-family="monospace">Axe / Spindle</text>

    <!-- Largeur BB -->
    <line x1="100" y1="360" x2="220" y2="360" stroke="#2d7ff9" stroke-width="1.5"/>
    <line x1="100" y1="355" x2="100" y2="365" stroke="#2d7ff9" stroke-width="1.5"/>
    <line x1="220" y1="355" x2="220" y2="365" stroke="#2d7ff9" stroke-width="1.5"/>
    <text x="160" y="374" text-anchor="middle" font-size="9" fill="#2d7ff9" font-family="monospace">Largeur BB (68/73/86,5mm)</text>
  </svg>`
},
steps:[
  {id:'fix',q:'Type de fixation du boîtier ?',type:'choice',
   guide:{title:'Comment identifier le type ?',
   svg:`<svg viewBox="0 0 300 160" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- Tube section transverse -->
     <rect x="40" y="60" width="220" height="40" rx="4" fill="#1e1e1e" stroke="#2a2a2a" stroke-width="2"/>
     <!-- Fileté: trait hélicoïdal stylisé -->
     <g opacity=".9">
       <path d="M60 62 Q70 55 80 62 Q90 69 100 62 Q110 55 120 62 Q130 69 140 62 Q150 55 160 62" stroke="#e8400c" stroke-width="2" fill="none"/>
       <path d="M60 98 Q70 91 80 98 Q90 105 100 98 Q110 91 120 98 Q130 105 140 98 Q150 91 160 98" stroke="#e8400c" stroke-width="2" fill="none"/>
     </g>
     <text x="100" y="130" text-anchor="middle" font-size="11" fill="#e8400c" font-family="monospace">FILETÉ</text>
     <!-- Press-fit: lisse -->
     <rect x="170" y="62" width="90" height="36" rx="3" fill="#161616" stroke="#2d7ff9" stroke-width="2"/>
     <text x="215" y="130" text-anchor="middle" font-size="11" fill="#2d7ff9" font-family="monospace">PRESS-FIT</text>
     <!-- Flèche -->
     <text x="100" y="50" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">Filetage visible</text>
     <text x="215" y="50" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">Tube lisse</text>
   </svg>`,
   steps:[
     {t:'Lampe torche dans le tube de pédalier depuis le dessous.'},
     {t:'<strong>Fileté</strong> : filetage hélicoïdal visible — résiste au doigt.'},
     {t:'<strong>Press-Fit</strong> : intérieur parfaitement lisse et poli.'},
   ]},
   choices:[
     {val:'filété',icon:'🔧',lbl:'<strong>Fileté</strong>',desc:'filetage hélicoïdal'},
     {val:'pressfit',icon:'🔨',lbl:'<strong>Press-Fit</strong>',desc:'tube lisse, emboîtement'},
   ]},
  {id:'diam',q:'Diamètre intérieur du tube pédalier ?',type:'number',unit:'mm',ph:'34.8',
   guide:{title:'Mesurer le Ø intérieur',
   svg:`<svg viewBox="0 0 300 170" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- Vue de face du tube -->
     <circle cx="150" cy="85" r="68" stroke="#2a2a2a" stroke-width="3"/>
     <circle cx="150" cy="85" r="52" stroke="#e8400c" stroke-width="2" stroke-dasharray="6,3"/>
     <!-- Pied à coulisse stylisé -->
     <rect x="82" y="82" width="136" height="6" rx="3" fill="#2d7ff9" opacity=".8"/>
     <rect x="82" y="78" width="4" height="14" rx="1" fill="#2d7ff9"/>
     <rect x="214" y="78" width="4" height="14" rx="1" fill="#2d7ff9"/>
     <!-- Flèche Ø -->
     <line x1="98" y1="58" x2="202" y2="58" stroke="#22c55e" stroke-width="1.5" stroke-dasharray="4,3"/>
     <line x1="98" y1="53" x2="98" y2="63" stroke="#22c55e" stroke-width="2"/>
     <line x1="202" y1="53" x2="202" y2="63" stroke="#22c55e" stroke-width="2"/>
     <text x="150" y="50" text-anchor="middle" font-size="10" fill="#22c55e" font-family="monospace">Ø intérieur</text>
     <!-- Valeurs -->
     <text x="150" y="138" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">BSA=34,8 · PF30=46 · BB86=41</text>
   </svg>`,
   steps:[
     {t:'Pied à coulisse en mode <strong>mesure intérieure</strong> (becs inversés).'},
     {t:'<strong>BSA :</strong> <code>34,8 mm</code> · <strong>Italian :</strong> <code>35 mm</code>'},
     {t:'<strong>BB30 :</strong> <code>42 mm</code> · <strong>PF30/T47 :</strong> <code>46 mm</code> · <strong>BB86 :</strong> <code>41 mm</code>'},
   ]}},
  {id:'larg',q:'Largeur du tube pédalier (entre les faces internes) ?',type:'number',unit:'mm',ph:'68',
   guide:{title:'Mesurer la largeur — comme l\'OLD',
   svg:`<svg viewBox="0 0 300 130" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- Tube pédalier vue de dessous -->
     <rect x="20" y="40" width="260" height="50" rx="5" fill="#1e1e1e" stroke="#2a2a2a" stroke-width="2"/>
     <rect x="20" y="40" width="14" height="50" rx="3" fill="#e8400c" opacity=".8"/>
     <rect x="266" y="40" width="14" height="50" rx="3" fill="#e8400c" opacity=".8"/>
     <!-- Flèche largeur intérieure -->
     <line x1="34" y1="105" x2="266" y2="105" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="5,3"/>
     <line x1="34" y1="99" x2="34" y2="111" stroke="#2d7ff9" stroke-width="2"/>
     <line x1="266" y1="99" x2="266" y2="111" stroke="#2d7ff9" stroke-width="2"/>
     <text x="150" y="122" text-anchor="middle" font-size="10" fill="#2d7ff9" font-family="monospace">68 · 73 · 86,5 mm</text>
     <text x="150" y="30" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">Face interne → face interne</text>
   </svg>`,
   steps:[
     {t:'Mesurez la <strong>distance entre faces internes</strong> des deux pattes.'},
     {t:'<code>68 mm</code> = route standard · <code>73 mm</code> = VTT · <code>86,5 mm</code> = BB86'},
   ]}},
],
identify(a){
  const fix=a.fix,d=parseFloat(a.diam),l=parseFloat(a.larg);
  if(fix==='filété'){
    if(d>=34.5&&d<=35.2&&l>=67&&l<=69) return{std:'BSA / English',detail:'34,8mm · 68mm · 1,37"×24tpi',compat:['Shimano Hollowtech II 24mm','SRAM DUB 28,99mm','SRAM GXP 24/22mm','Campagnolo Ultra/Power Torque','Pédaliers 30mm via adaptateur'],note:'Standard le plus universel au monde. Côté droit fileté à gauche — serrage normal.',warn:''};
    if(d>=34.5&&d<=35.2&&l>=72&&l<=74) return{std:'BSA 73mm (VTT)',detail:'34,8mm · 73mm · 1,37"×24tpi',compat:['Shimano MTB 24mm','SRAM DUB MTB','Adaptateur 30mm'],note:'Version VTT du BSA. +5mm pour dégagement plateau.',warn:''};
    if(d>=34.6&&d<=35.4&&l>=69&&l<=71) return{std:'Italian / ITA',detail:'35mm · 70mm · 36×24tpi',compat:['Shimano 24mm','Campagnolo'],note:'',warn:'⚠️ LES DEUX CÔTÉS sont filetés à droite — opposé au BSA !'};
    if(d>=45.5&&d<=47&&l>=67&&l<=69) return{std:'T47 · 68mm',detail:'46mm · 68mm · 47×1mm fileté',compat:['Shimano 24mm','SRAM DUB','30mm + adaptateur'],note:'Meilleur des deux mondes : grand Ø + filetage. Standard en pleine expansion.',warn:''};
    if(d>=45.5&&d<=47&&l>=72&&l<=74) return{std:'T47 · 73mm (VTT)',detail:'46mm · 73mm · 47×1mm fileté',compat:['Shimano MTB','SRAM DUB MTB'],note:'Version VTT du T47.',warn:''};
    return{std:'Fileté non identifié',detail:`Ø${d}mm · L${l}mm`,compat:[],note:'Peut-être French/Swiss (vintage). Mesurez à nouveau.',warn:'Vérifiez la mesure'};
  } else {
    if(d>=40.5&&d<=42.5&&l>=67&&l<=69) return{std:'BB30',detail:'42mm · 68mm · Press-Fit',compat:['30mm natif','24mm via adaptateur BSA'],note:'Standard Cannondale alu.',warn:'Insert fileté T47 recommandé contre les craquements'};
    if(d>=45.5&&d<=47&&l>=67&&l<=69) return{std:'PF30',detail:'46mm · 68mm · Press-Fit',compat:['30mm natif','24mm via adaptateur'],note:'Répandu sur carbone haut de gamme.',warn:'Insert fileté recommandé si craquements'};
    if(d>=40&&d<=42&&l>=85&&l<=88) return{std:'BB86 (PF41)',detail:'41mm · 86,5mm · Press-Fit',compat:['Shimano 24mm natif','SRAM via adapt.'],note:'Standard Shimano route. Très stable.',warn:''};
    if(d>=40&&d<=42&&l>=91&&l<=93) return{std:'BB92 (VTT)',detail:'41mm · 92mm · Press-Fit',compat:['Shimano MTB 24mm'],note:'Version VTT du BB86. Standard Trek.',warn:''};
    if(d>=45.5&&d<=47&&l>=85&&l<=88) return{std:'BB386EVO',detail:'46mm · 86,5mm · Press-Fit',compat:['30mm','24mm via adapt.'],note:'Standard universel 3T. Très polyvalent.',warn:''};
    return{std:'Press-Fit non identifié',detail:`Ø${d}mm · L${l}mm`,compat:[],note:'Vérifiez BB90 Trek (37mm) ou contactez le fabricant.',warn:'Vérifiez les mesures'};
  }
}},

shis:{title:'Jeu de Direction SHIS',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><rect x="11" y="2" width="6" height="24" rx="2" stroke="#e8400c" stroke-width="1.5"/><ellipse cx="14" cy="8" rx="7" ry="4" stroke="#9a9590" stroke-width="1.5"/><ellipse cx="14" cy="20" rx="9" ry="4.5" stroke="#9a9590" stroke-width="1.5"/></svg>`,
// Vue d'accueil du diagnostic SHIS — schéma éclaté
intro:{
  title:'Jeu de Direction — Vue éclatée',
  svg:`<svg viewBox="0 0 320 520" fill="none" xmlns="http://www.w3.org/2000/svg">
    <!-- Titre -->
    <text x="160" y="18" text-anchor="middle" font-size="11" fill="#9a9590" font-family="monospace" font-weight="bold">HEADSET / JEU DE DIRECTION</text>

    <!-- ── BOUCHON DE POTENCE / TOP CAP ── -->
    <ellipse cx="160" cy="48" rx="28" ry="9" fill="#242424" stroke="#555" stroke-width="1.5"/>
    <rect x="148" y="38" width="24" height="10" rx="3" fill="#333" stroke="#555" stroke-width="1"/>
    <line x1="195" y1="44" x2="240" y2="44" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="243" y="41" font-size="9" fill="#22c55e" font-family="monospace">Bouchon de potence</text>
    <text x="243" y="52" font-size="8" fill="#5a5550" font-family="monospace">Top Cap</text>

    <!-- ── ENTRETOISES / SPACERS ── -->
    <ellipse cx="160" cy="80" rx="22" ry="6" fill="#1e1e1e" stroke="#444" stroke-width="1.5"/>
    <ellipse cx="160" cy="92" rx="22" ry="6" fill="#1e1e1e" stroke="#444" stroke-width="1.5"/>
    <line x1="185" y1="86" x2="240" y2="86" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="243" y="83" font-size="9" fill="#22c55e" font-family="monospace">Entretoises</text>
    <text x="243" y="94" font-size="8" fill="#5a5550" font-family="monospace">Spacers</text>

    <!-- ── POTENCE / STEM ── -->
    <rect x="130" y="105" width="60" height="20" rx="4" fill="#2a2a2a" stroke="#555" stroke-width="1.5"/>
    <line x1="195" y1="115" x2="240" y2="115" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="243" y="112" font-size="9" fill="#22c55e" font-family="monospace">Potence</text>
    <text x="243" y="123" font-size="8" fill="#5a5550" font-family="monospace">Stem</text>

    <!-- ── COLONNE DE FOURCHE / STEERER ── -->
    <rect x="148" y="130" width="24" height="90" rx="3" fill="#1e1e1e" stroke="#e8400c" stroke-width="2"/>
    <line x1="80" y1="175" x2="145" y2="175" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="3" y="167" font-size="9" fill="#e8400c" font-family="monospace">Colonne</text>
    <text x="3" y="178" font-size="9" fill="#e8400c" font-family="monospace">fourche</text>
    <text x="3" y="189" font-size="8" fill="#5a5550" font-family="monospace">Steerer</text>

    <!-- ── CAPOT SUPÉRIEUR / TOP COVER ── -->
    <ellipse cx="160" cy="232" rx="32" ry="9" fill="#2d2d2d" stroke="#555" stroke-width="1.5"/>
    <line x1="195" y1="232" x2="240" y2="232" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="243" y="229" font-size="9" fill="#22c55e" font-family="monospace">Capot sup.</text>
    <text x="243" y="240" font-size="8" fill="#5a5550" font-family="monospace">Top Cover</text>

    <!-- ── ROULEMENT SUPÉRIEUR / UPPER BEARING ── -->
    <ellipse cx="160" cy="252" rx="36" ry="10" fill="#1a1a1a" stroke="#f59e0b" stroke-width="2"/>
    <ellipse cx="160" cy="252" rx="18" ry="5" fill="none" stroke="#f59e0b" stroke-width="1" stroke-dasharray="3,2"/>
    <line x1="80" y1="252" x2="121" y2="252" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="3" y="249" font-size="9" fill="#f59e0b" font-family="monospace">Roulement</text>
    <text x="3" y="260" font-size="9" fill="#f59e0b" font-family="monospace">supérieur</text>
    <text x="3" y="271" font-size="8" fill="#5a5550" font-family="monospace">Upper Bearing</text>

    <!-- ── COUPELLE SUPÉRIEURE / UPPER CUP ── -->
    <ellipse cx="160" cy="280" rx="44" ry="12" fill="#242424" stroke="#e8400c" stroke-width="2"/>
    <line x1="207" y1="280" x2="240" y2="280" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="243" y="277" font-size="9" fill="#e8400c" font-family="monospace">Coupelle sup.</text>
    <text x="243" y="288" font-size="8" fill="#5a5550" font-family="monospace">Upper Cup / EC·ZS·IS</text>

    <!-- ── TUBE DE DIRECTION / HEAD TUBE ── -->
    <rect x="136" y="295" width="48" height="80" rx="2" fill="#161616" stroke="#333" stroke-width="2"/>
    <!-- Filetage intérieur symbolisé -->
    <line x1="136" y1="305" x2="184" y2="305" stroke="#444" stroke-width="1"/>
    <line x1="136" y1="315" x2="184" y2="315" stroke="#444" stroke-width="1"/>
    <line x1="136" y1="325" x2="184" y2="325" stroke="#444" stroke-width="1"/>
    <line x1="80" y1="335" x2="133" y2="335" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="3" y="328" font-size="9" fill="#9a9590" font-family="monospace">Tube de</text>
    <text x="3" y="340" font-size="9" fill="#9a9590" font-family="monospace">direction</text>
    <text x="3" y="352" font-size="8" fill="#5a5550" font-family="monospace">Head Tube</text>

    <!-- ── COUPELLE INFÉRIEURE / LOWER CUP ── -->
    <ellipse cx="160" cy="378" rx="44" ry="12" fill="#242424" stroke="#2d7ff9" stroke-width="2"/>
    <line x1="207" y1="378" x2="240" y2="378" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="243" y="375" font-size="9" fill="#2d7ff9" font-family="monospace">Coupelle inf.</text>
    <text x="243" y="386" font-size="8" fill="#5a5550" font-family="monospace">Lower Cup / EC·ZS·IS</text>

    <!-- ── ROULEMENT INFÉRIEUR / LOWER BEARING ── -->
    <ellipse cx="160" cy="403" rx="36" ry="10" fill="#1a1a1a" stroke="#f59e0b" stroke-width="2"/>
    <ellipse cx="160" cy="403" rx="18" ry="5" fill="none" stroke="#f59e0b" stroke-width="1" stroke-dasharray="3,2"/>
    <line x1="80" y1="403" x2="121" y2="403" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="3" y="400" font-size="9" fill="#f59e0b" font-family="monospace">Roulement</text>
    <text x="3" y="411" font-size="9" fill="#f59e0b" font-family="monospace">inférieur</text>
    <text x="3" y="422" font-size="8" fill="#5a5550" font-family="monospace">Lower Bearing</text>

    <!-- ── COUPELLE DE FOURCHE / CROWN RACE ── -->
    <ellipse cx="160" cy="430" rx="38" ry="10" fill="#333" stroke="#22c55e" stroke-width="2"/>
    <line x1="200" y1="430" x2="240" y2="430" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="243" y="427" font-size="9" fill="#22c55e" font-family="monospace">Coupelle fourche</text>
    <text x="243" y="438" font-size="8" fill="#5a5550" font-family="monospace">Crown Race</text>

    <!-- ── COURONNE FOURCHE / FORK CROWN ── -->
    <path d="M130 450 Q160 445 190 450 L200 470 Q160 465 120 470 Z" fill="#222" stroke="#444" stroke-width="1.5"/>
    <line x1="80" y1="460" x2="127" y2="460" stroke="#555" stroke-width="1" stroke-dasharray="3,2"/>
    <text x="3" y="457" font-size="9" fill="#9a9590" font-family="monospace">Couronne</text>
    <text x="3" y="468" font-size="9" fill="#9a9590" font-family="monospace">de fourche</text>
    <text x="3" y="480" font-size="8" fill="#5a5550" font-family="monospace">Fork Crown</text>

    <!-- Trait de rappel vertical -->
    <line x1="160" y1="57" x2="160" y2="225" stroke="#333" stroke-width="1" stroke-dasharray="2,4"/>
    <line x1="160" y1="265" x2="160" y2="290" stroke="#333" stroke-width="1" stroke-dasharray="2,4"/>
    <line x1="160" y1="393" x2="160" y2="418" stroke="#333" stroke-width="1" stroke-dasharray="2,4"/>
    <line x1="160" y1="443" x2="160" y2="448" stroke="#333" stroke-width="1" stroke-dasharray="2,4"/>

    <!-- Légende couleurs -->
    <rect x="10" y="490" width="10" height="10" rx="2" fill="#e8400c"/>
    <text x="24" y="499" font-size="8" fill="#9a9590" font-family="monospace">Coupelle sup./inf.</text>
    <rect x="130" y="490" width="10" height="10" rx="2" fill="#f59e0b"/>
    <text x="144" y="499" font-size="8" fill="#9a9590" font-family="monospace">Roulements</text>
    <rect x="230" y="490" width="10" height="10" rx="2" fill="#22c55e"/>
    <text x="244" y="499" font-size="8" fill="#9a9590" font-family="monospace">Fixes</text>
  </svg>`
},
steps:[
  {id:'type',q:'Type de coupe visible sur le cadre ?',type:'choice',
   guide:{title:'Identifier le type SHIS',
   svg:`<svg viewBox="0 0 320 200" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- 3 tubes côte à côte avec leurs coupelles -->
     <!-- EC -->
     <text x="53" y="15" text-anchor="middle" font-size="10" fill="#e8400c" font-family="monospace" font-weight="bold">EC</text>
     <text x="53" y="26" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">External Cup</text>
     <rect x="38" y="30" width="30" height="120" rx="3" fill="#1e1e1e" stroke="#333" stroke-width="1.5"/>
     <!-- Coupelle EC saillante -->
     <ellipse cx="53" cy="42" rx="24" ry="8" fill="#e8400c" opacity=".85"/>
     <ellipse cx="53" cy="145" rx="24" ry="8" fill="#e8400c" opacity=".85"/>
     <text x="53" y="170" text-anchor="middle" font-size="8" fill="#e8400c" font-family="monospace">Dépasse</text>
     <text x="53" y="181" text-anchor="middle" font-size="7" fill="#5a5550" font-family="monospace">Stands out</text>

     <!-- ZS -->
     <text x="160" y="15" text-anchor="middle" font-size="10" fill="#2d7ff9" font-family="monospace" font-weight="bold">ZS</text>
     <text x="160" y="26" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">Zero Stack</text>
     <rect x="145" y="30" width="30" height="120" rx="3" fill="#1e1e1e" stroke="#333" stroke-width="1.5"/>
     <!-- Logement usiné ZS -->
     <rect x="138" y="32" width="44" height="14" rx="2" fill="#2d7ff9" opacity=".7"/>
     <rect x="138" y="134" width="44" height="14" rx="2" fill="#2d7ff9" opacity=".7"/>
     <text x="160" y="170" text-anchor="middle" font-size="8" fill="#2d7ff9" font-family="monospace">Affleure</text>
     <text x="160" y="181" text-anchor="middle" font-size="7" fill="#5a5550" font-family="monospace">Flush</text>

     <!-- IS -->
     <text x="267" y="15" text-anchor="middle" font-size="10" fill="#22c55e" font-family="monospace" font-weight="bold">IS</text>
     <text x="267" y="26" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">Integrated</text>
     <rect x="252" y="30" width="30" height="120" rx="3" fill="#1e1e1e" stroke="#333" stroke-width="1.5"/>
     <!-- Roulement IS interne -->
     <ellipse cx="267" cy="46" rx="14" ry="10" fill="#22c55e" opacity=".7"/>
     <ellipse cx="267" cy="140" rx="14" ry="10" fill="#22c55e" opacity=".7"/>
     <text x="267" y="170" text-anchor="middle" font-size="8" fill="#22c55e" font-family="monospace">Interne</text>
     <text x="267" y="181" text-anchor="middle" font-size="7" fill="#5a5550" font-family="monospace">Internal</text>

     <!-- Note bas -->
     <text x="160" y="197" text-anchor="middle" font-size="8" fill="#5a5550" font-family="monospace">Vue de côté — Tube de direction cadre</text>
   </svg>`,
   steps:[
     {t:'<strong>EC (External Cup)</strong> : coupelle saillante qui dépasse EN DEHORS du tube de direction.'},
     {t:'<strong>ZS (Zero Stack)</strong> : coupelle dans un logement usiné — affleure parfaitement le tube.'},
     {t:'<strong>IS (Integrated)</strong> : roulement inséré directement dans le tube — rien ne dépasse.'},
   ]},
   choices:[
     {val:'EC',icon:'⬆️',lbl:'<strong>EC</strong> — External Cup',desc:'coupelle saillante visible'},
     {val:'ZS',icon:'⬛',lbl:'<strong>ZS</strong> — Zero Stack',desc:'affleure le tube'},
     {val:'IS',icon:'🔲',lbl:'<strong>IS</strong> — Integrated',desc:'rien ne dépasse'},
   ]},
  {id:'log',q:'Ø intérieur du logement dans le cadre ?',type:'number',unit:'mm',ph:'44',
   guide:{title:'Mesurer le Ø du logement',
   svg:`<svg viewBox="0 0 300 160" fill="none" xmlns="http://www.w3.org/2000/svg">
     <rect x="100" y="20" width="100" height="120" rx="5" fill="#1e1e1e" stroke="#2a2a2a" stroke-width="2"/>
     <ellipse cx="150" cy="40" rx="42" ry="14" fill="none" stroke="#e8400c" stroke-width="2" stroke-dasharray="5,3"/>
     <line x1="108" y1="40" x2="192" y2="40" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="4,3"/>
     <line x1="108" y1="34" x2="108" y2="46" stroke="#2d7ff9" stroke-width="2"/>
     <line x1="192" y1="34" x2="192" y2="46" stroke="#2d7ff9" stroke-width="2"/>
     <text x="150" y="30" text-anchor="middle" font-size="10" fill="#2d7ff9" font-family="monospace">Ø logement</text>
     <text x="150" y="145" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">34 · 42 · 44 · 49 · 52 · 56 mm</text>
   </svg>`,
   steps:[
     {t:'Retirez le jeu de direction. Mesurez le <strong>Ø intérieur</strong> du logement.'},
     {t:'<code>34mm</code> · <code>42mm</code> · <code>44mm</code> · <code>49mm</code> · <code>52mm</code> · <code>56mm</code>'},
   ]}},
  {id:'col',q:'Ø extérieur de la colonne de fourche ?',type:'number',unit:'mm',ph:'28.6',
   guide:{title:'Mesurer la colonne de fourche',
   svg:`<svg viewBox="0 0 300 160" fill="none" xmlns="http://www.w3.org/2000/svg">
     <rect x="130" y="10" width="40" height="140" rx="5" fill="#e8400c" opacity=".5"/>
     <line x1="70" y1="80" x2="230" y2="80" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="5,3"/>
     <line x1="130" y1="74" x2="130" y2="86" stroke="#2d7ff9" stroke-width="2"/>
     <line x1="170" y1="74" x2="170" y2="86" stroke="#2d7ff9" stroke-width="2"/>
     <text x="150" y="70" text-anchor="middle" font-size="11" fill="#2d7ff9" font-family="monospace">Ø colonne</text>
     <text x="150" y="148" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">25,4 (1") · 28,6 (1⅛") · 38,1 (1½")</text>
   </svg>`,
   steps:[
     {t:'Mesurez le <strong>Ø extérieur</strong> de la colonne (tube de la fourche).'},
     {t:'<code>25,4mm</code> = 1" · <code>28,6mm</code> = 1⅛" · <code>38,1mm</code> = 1½"'},
     {t:'Fourche tapered : Ø différent en haut et en bas — mesurez les deux.'},
   ]}},
  {id:'pos',q:'Position de cette coupe ?',type:'choice',
   choices:[
     {val:'haut',icon:'⬆️',lbl:'Coupe <strong>supérieure</strong>',desc:'haut du tube de direction'},
     {val:'bas',icon:'⬇️',lbl:'Coupe <strong>inférieure</strong>',desc:'bas du tube de direction'},
   ]},
],
identify(a){
  const t=a.type,l=parseFloat(a.log),c=parseFloat(a.col);
  const cC=c<=26?'26':c<=29?'28.6':c<=33?'31.8':'40';
  const code=`${t}${Math.round(l)}|${cC}`;
  const db={'ZS44|28.6':{d:'Zero Stack · 44mm · colonne 1⅛"',u:'Route, gravel, VTT XC — standard actuel'},'ZS44|30':{d:'Zero Stack · 44mm · colonne 1⅛" (bas)',u:'Route bas'},'ZS56|40':{d:'Zero Stack · 56mm · colonne 1½"',u:'VTT tapered bas — enduro/trail'},'EC34|28.6':{d:'External Cup · 34mm · colonne 1⅛"',u:'Route, gravel, city'},'EC44|28.6':{d:'External Cup · 44mm · colonne 1⅛"',u:'Route grands tubes'},'EC44|40':{d:'External Cup · 44mm · colonne 1½"',u:'VTT tapered bas'},'IS42|28.6':{d:'Integrated · 42mm · 1⅛"',u:'Route/VTT IS'},'IS52|40':{d:'Integrated · 52mm · 1½"',u:'VTT tapered bas IS (enduro)'},'IS52|28.6':{d:'Integrated · 52mm · 1⅛"',u:'VTT tapered haut IS'},'ZS49|28.6':{d:'Zero Stack · 49mm · 1⅛"',u:'Certains cadres carbone route'}};
  const f=db[code];
  if(f) return{std:code,detail:f.d,compat:[f.u,`Position : ${a.pos}`,'Vérifiez haut ET bas pour jeu complet'],note:`Combinaison courante. ${f.u}.`,warn:''};
  return{std:code,detail:`Type ${t} · ${l}mm · col ${c}mm`,compat:[],note:'Combinaison peu courante. Vérifiez les mesures.',warn:'Peu courant — vérifiez'};
}},

axe:{title:'Axe de Roue',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#9a9590" stroke-width="1.5"/><line x1="2" y1="14" x2="26" y2="14" stroke="#e8400c" stroke-width="2.5" stroke-linecap="round"/><circle cx="14" cy="14" r="3" fill="#e8400c"/><rect x="0" y="12" width="4" height="4" rx="1" fill="#2d7ff9"/><rect x="24" y="12" width="4" height="4" rx="1" fill="#2d7ff9"/></svg>`,
steps:[
  {id:'roue',q:'Quelle roue ?',type:'choice',choices:[{val:'avant',icon:'🔵',lbl:'Roue <strong>Avant</strong>',desc:'fourche'},{val:'arriere',icon:'🔴',lbl:'Roue <strong>Arrière</strong>',desc:'cadre'}]},
  {id:'old',q:'Mesure OLD — entre les faces internes des pattes ?',type:'number',unit:'mm',ph:'100',
   guide:{title:'Mesurer l\'OLD',
   svg:`<svg viewBox="0 0 300 120" fill="none" xmlns="http://www.w3.org/2000/svg">
     <rect x="10" y="40" width="18" height="40" rx="3" fill="#e8400c" opacity=".8"/>
     <rect x="272" y="40" width="18" height="40" rx="3" fill="#e8400c" opacity=".8"/>
     <line x1="28" y1="60" x2="272" y2="60" stroke="#2a2a2a" stroke-width="5"/>
     <circle cx="150" cy="60" r="18" fill="#1e1e1e" stroke="#9a9590" stroke-width="2"/>
     <!-- Flèche OLD -->
     <line x1="28" y1="98" x2="272" y2="98" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="6,3"/>
     <line x1="28" y1="92" x2="28" y2="104" stroke="#2d7ff9" stroke-width="2"/>
     <line x1="272" y1="92" x2="272" y2="104" stroke="#2d7ff9" stroke-width="2"/>
     <text x="150" y="114" text-anchor="middle" font-size="10" fill="#2d7ff9" font-family="monospace">OLD = face int. → face int.</text>
     <text x="150" y="30" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">Roue retirée · Pied à coulisse</text>
   </svg>`,
   steps:[
     {t:'Roue retirée. Mesurez entre les <strong>faces internes</strong> des pattes.'},
     {t:'<strong>Avant :</strong> <code>100mm</code> (QR/TA) · <code>110mm</code> (Boost) · <code>150mm</code> (Fat)'},
     {t:'<strong>Arrière :</strong> <code>130mm</code> (route) · <code>135mm</code> (VTT/ville) · <code>142mm</code> (TA) · <code>148mm</code> (Boost)'},
   ]}},
  {id:'fix',q:'Type de fixation de l\'axe ?',type:'choice',
   guide:{title:'QR vs Traversant (TA)',
   svg:`<svg viewBox="0 0 300 140" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- QR -->
     <line x1="20" y1="40" x2="200" y2="40" stroke="#9a9590" stroke-width="5" stroke-linecap="round"/>
     <circle cx="208" cy="40" r="10" fill="none" stroke="#e8400c" stroke-width="2"/>
     <path d="M208 30 Q220 35 220 40 Q220 50 208 50" stroke="#e8400c" stroke-width="1.5" fill="none"/>
     <text x="150" y="28" text-anchor="middle" font-size="10" fill="#e8400c" font-family="monospace">QR — levier excentrique</text>
     <!-- TA -->
     <line x1="20" y1="100" x2="270" y2="100" stroke="#2d7ff9" stroke-width="6" stroke-linecap="round"/>
     <circle cx="270" cy="100" r="8" fill="#2d7ff9"/>
     <text x="150" y="125" text-anchor="middle" font-size="10" fill="#2d7ff9" font-family="monospace">TA — axe plein fileté M12</text>
     <text x="150" y="87" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">Vissage depuis un côté</text>
   </svg>`,
   steps:[
     {t:'<strong>QR</strong> : petit levier excentrique, axe creux 9mm (AV) ou 10mm (AR).'},
     {t:'<strong>TA</strong> : axe plein fileté qui se visse, Ø 12mm ou 15mm.'},
     {t:'Sur TA : pas de levier — vous vissez/dévissez l\'axe avec une clé Allen.'},
   ]},
   choices:[
     {val:'qr',icon:'⚡',lbl:'<strong>QR</strong> — Quick Release',desc:'levier excentrique'},
     {val:'ta12',icon:'🔩',lbl:'<strong>TA 12mm</strong>',desc:'traversant fileté M12'},
     {val:'ta15',icon:'🔩',lbl:'<strong>TA 15mm</strong>',desc:'traversant M15 (fourche)'},
     {val:'ta20',icon:'💪',lbl:'<strong>TA 20mm</strong>',desc:'traversant DH M20'},
   ]},
],
identify(a){
  const r=a.roue,o=Math.round(parseFloat(a.old)),f=a.fix;
  const db={'avant-100-qr':{std:'QR 9×100mm',detail:'Avant · 100mm · QR 9mm',compat:['Route classique','VTT XC ancien','Ville/trekking'],note:'Standard classique universel. Freehub HG compatible.'},'avant-100-ta12':{std:'TA 12×100mm',detail:'Avant · 100mm · Traversant M12',compat:['Route moderne','Gravel','VTT XC/trail'],note:'Standard traversant moderne dominant.'},'avant-100-ta15':{std:'TA 15×100mm',detail:'Avant · 100mm · Traversant M15',compat:['VTT XC/trail 2010–2018'],note:'En cours de remplacement par 12×100.'},'avant-110-ta12':{std:'TA 12×110mm Boost',detail:'Avant · 110mm · Boost',compat:['VTT Boost trail/enduro'],note:'Standard Boost actuel VTT.'},'avant-110-ta15':{std:'TA 15×110mm Boost',detail:'Avant · 110mm · M15 Boost',compat:['Certains VTT Boost'],note:'Moins courant que 12×110.'},'avant-110-ta20':{std:'TA 20×110mm',detail:'Avant · 110mm · M20 DH',compat:['DH','Freeride'],note:'Standard descente.'},'arriere-130-qr':{std:'QR 10×130mm',detail:'Arrière · 130mm · QR 10mm',compat:['Route classique 8–11v'],note:'Standard route depuis 1985.'},'arriere-135-qr':{std:'QR 10×135mm',detail:'Arrière · 135mm · QR 10mm',compat:['VTT classique','Ville','Trekking'],note:'Standard VTT/ville classique.'},'arriere-130-ta12':{std:'TA 12×130mm',detail:'Arrière · 130mm · Traversant',compat:['Route moderne'],note:'Même OLD que QR 130 mais plus rigide.'},'arriere-135-ta12':{std:'TA 12×135mm',detail:'Arrière · 135mm · Traversant',compat:['Gravel','Certains VTT'],note:'Courant sur gravel.'},'arriere-142-ta12':{std:'TA 12×142mm',detail:'Arrière · 142mm · Traversant',compat:['VTT XC/trail standard'],note:'Standard traversant VTT classique.'},'arriere-148-ta12':{std:'TA 12×148mm Boost',detail:'Arrière · 148mm · Boost',compat:['VTT Boost (standard actuel)'],note:'+6mm vs 142mm pour meilleur angle rayon.'},'arriere-157-ta12':{std:'TA 12×157mm SuperBoost',detail:'Arrière · 157mm',compat:['DH','E-MTB','Fat Bike'],note:'DH et e-MTB lourds.'}};
  const key=`${r}-${o}-${f}`;
  const found=db[key];
  if(found) return{...found,warn:''};
  return{std:`${r}·${o}mm·${f}`,detail:'Combinaison non répertoriée',compat:[],note:'Fat Bike? Piste? Vérifiez.',warn:'Vérifiez les mesures'};
}},

pneu:{title:'Pneu / Jante ETRTO',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="11" stroke="#e8400c" stroke-width="2.5"/><circle cx="14" cy="14" r="6" stroke="#9a9590" stroke-width="1.5"/><circle cx="14" cy="14" r="2" fill="#e8400c"/></svg>`,
steps:[
  {id:'l',q:'Largeur du pneu — 1er chiffre ETRTO ?',type:'number',unit:'mm',ph:'37',
   guide:{title:'Lire le code sur le flanc',
   svg:`<svg viewBox="0 0 300 160" fill="none" xmlns="http://www.w3.org/2000/svg">
     <ellipse cx="150" cy="80" rx="120" ry="65" fill="none" stroke="#2a2a2a" stroke-width="3"/>
     <ellipse cx="150" cy="80" rx="80" ry="40" fill="none" stroke="#333" stroke-width="2"/>
     <!-- Code ETRTO sur flanc -->
     <rect x="80" y="65" width="140" height="30" rx="5" fill="#1e1e1e" stroke="#e8400c" stroke-width="1.5"/>
     <text x="150" y="84" text-anchor="middle" font-size="14" fill="#e8400c" font-family="monospace" font-weight="bold">37-622</text>
     <!-- Flèche largeur -->
     <line x1="150" y1="15" x2="150" y2="40" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="3,2"/>
     <text x="193" y="25" font-size="10" fill="#2d7ff9" font-family="monospace">Largeur</text>
     <!-- Flèche BSD -->
     <line x1="30" y1="80" x2="70" y2="80" stroke="#22c55e" stroke-width="1.5" stroke-dasharray="3,2"/>
     <text x="18" y="75" font-size="9" fill="#22c55e" font-family="monospace">BSD</text>
     <text x="150" y="148" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">37-622 → largeur=37 · BSD=622</text>
   </svg>`,
   steps:[
     {t:'Le code ETRTO est sur le <strong>flanc du pneu</strong> : ex <code>37-622</code>.'},
     {t:'Le 1er chiffre est la <strong>largeur</strong> en mm.'},
     {t:'Si le code est en pouces (ex <code>2.35"</code>), multipliez par 25,4.'},
   ]}},
  {id:'b',q:'BSD — Diamètre siège de tringle (2e chiffre) ?',type:'number',unit:'mm',ph:'622',
   guide:{title:'Identifier le BSD',
   steps:[
     {t:'Le 2e chiffre du code ETRTO : <code>37-622</code> → BSD = <code>622</code>.'},
     {t:'<code>622</code> = 700C/29" · <code>584</code> = 27,5" · <code>559</code> = 26" · <code>406</code> = 20"'},
     {t:'<code>451</code> = 20" route · <code>349</code> = 16" Brompton · <code>507</code> = 24"'},
   ]}},
],
identify(a){
  const l=parseFloat(a.l),b=Math.round(parseFloat(a.b));
  const nm={203:'12"',305:'16"BMX',349:'16"Brompton',406:'20"BMX/Pliant',451:'20"Route/Pliant',507:'24"VTT',559:'26"MTB',571:'26"route/650B anc.',584:'27,5"/650B',622:'700C/29"',630:'27"route',635:'28"hollandais'};
  const name=nm[b]||`BSD ${b}mm`;
  const cat=b===622?(l<28?'Route compétition':l<35?'Route/Endurance':l<45?'Gravel':l<55?'Bikepacking':'Gros gravel'):b===584?(l<50?'VTT XC/Trail':l<62?'Trail/Enduro':'Plus'):b===559?'VTT 26"':b===406?'BMX/Pliant':b===451?'Pliant route':'Spécifique';
  const pres=b===622&&l<25?'7–9 bar':b===622&&l<32?'5,5–8 bar':b===622&&l<45?'3–5,5 bar':b===622?'1,5–3 bar':b===584?'1,2–2,5 bar':'1,5–3,5 bar';
  return{std:`ETRTO ${Math.round(l)}-${b}`,detail:`${name} · Largeur ${l}mm`,compat:[cat,`Pression : ${pres}`,l>=28?'Compatible tubeless ready':'Non tubeless standard'],note:`Code ETRTO complet : ${Math.round(l)}-${b}. ⚠️ Ne jamais monter sur BSD différent.`,warn:''};
}},

guidon:{title:'Guidon & Potence',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><path d="M2 14h24" stroke="#e8400c" stroke-width="2.5" stroke-linecap="round"/><rect x="6" y="13" width="4" height="3" rx="1" fill="#9a9590"/><rect x="18" y="13" width="4" height="3" rx="1" fill="#9a9590"/></svg>`,
steps:[
  {id:'elem',q:'Que voulez-vous identifier ?',type:'choice',choices:[{val:'cintre',icon:'↔️',lbl:'<strong>Cintre</strong>',desc:'Ø de serrage dans la potence'},{val:'colonne',icon:'↕️',lbl:'<strong>Colonne fourche</strong>',desc:'Ø dans la potence'}]},
  {id:'diam',q:'Diamètre extérieur mesuré ?',type:'number',unit:'mm',ph:'31.8',
   guide:{title:'Mesurer le Ø du cintre ou colonne',
   svg:`<svg viewBox="0 0 300 150" fill="none" xmlns="http://www.w3.org/2000/svg">
     <rect x="10" y="67" width="280" height="16" rx="8" fill="#e8400c" opacity=".5"/>
     <circle cx="150" cy="75" r="28" fill="none" stroke="#2d7ff9" stroke-width="2" stroke-dasharray="6,4"/>
     <line x1="122" y1="75" x2="178" y2="75" stroke="#2d7ff9" stroke-width="1.5"/>
     <line x1="122" y1="69" x2="122" y2="81" stroke="#2d7ff9" stroke-width="2"/>
     <line x1="178" y1="69" x2="178" y2="81" stroke="#2d7ff9" stroke-width="2"/>
     <text x="150" y="62" text-anchor="middle" font-size="11" fill="#2d7ff9" font-family="monospace">Ø ext. zone centrale</text>
     <text x="150" y="130" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">22,2 · 25,4 · 31,8 · 35 mm</text>
   </svg>`,
   steps:[
     {t:'Pied à coulisse en <strong>mode extérieur</strong>.'},
     {t:'Cintre : mesurez au <strong>centre</strong> (zone de serrage de la potence).'},
     {t:'<code>22,2mm</code> · <code>25,4mm</code> · <code>31,8mm</code> (dominant route) · <code>35mm</code> (enduro)'},
   ]}},
],
identify(a){
  const el=a.elem,d=parseFloat(a.diam);
  if(el==='cintre'){
    if(d>=22&&d<=22.5) return{std:'22,2 mm',detail:'Cintre vintage/enfant/BMX',compat:['Potences 22,2mm','Vélo enfant','BMX'],note:'Standard très ancien.',warn:'Rare en neuf — standard obsolète'};
    if(d>=25.2&&d<=25.6) return{std:'25,4 mm (1")',detail:'Cintre oversized années 90',compat:['Potences 25,4mm','VTT 90s'],note:'Standard des années 90.',warn:'Peu de potences neuves disponibles'};
    if(d>=31.5&&d<=32.1) return{std:'31,8 mm',detail:'Oversize — standard route/gravel/XC actuel',compat:['Route','Gravel','VTT XC','City'],note:'Standard dominant actuel. Choix immense de potences.',warn:''};
    if(d>=34.8&&d<=35.3) return{std:'35 mm',detail:'Oversize+ enduro/DH',compat:['VTT enduro','DH','E-MTB'],note:'Plus rigide que 31,8mm. Standard enduro depuis 2016.',warn:''};
    return{std:`${d}mm`,detail:'Non standard',compat:[],note:'Vérifiez la mesure.',warn:'Vérifiez'};
  } else {
    if(d>=25.2&&d<=25.6) return{std:'1" (25,4mm)',detail:'Colonne vintage fileté ou A-Head',compat:['JDD TH33/36','Potences 1" vintage'],note:'Standard vintage.',warn:'Rare en neuf'};
    if(d>=28.4&&d<=28.8) return{std:'1⅛" (28,6mm)',detail:'Colonne oversized — standard universel',compat:['Tous JDD modernes 28.6','Très nombreuses potences'],note:'Standard universel actuel. Compatibilité maximale.',warn:''};
    if(d>=38&&d<=38.4) return{std:'1½" (38,1mm)',detail:'Colonne tapered bas ou DH',compat:['JDD IS52|40 / ZS56|40'],note:'Uniquement bas de fourche tapered ou DH.',warn:'Bas de fourche uniquement'};
    return{std:`${d}mm`,detail:'Non standard',compat:[],note:'Fourche tapered ? Mesurez haut et bas.',warn:'Vérifiez'};
  }
}},

selle:{title:'Tige de Selle',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><rect x="12" y="10" width="4" height="16" rx="2" fill="#e8400c" opacity=".8"/><path d="M6 10 Q14 6 22 10" stroke="#e8400c" stroke-width="2" fill="none" stroke-linecap="round"/></svg>`,
steps:[
  {id:'diam',q:'Diamètre extérieur de la tige de selle ?',type:'number',unit:'mm',ph:'31.6',
   guide:{title:'Mesurer le Ø de la tige',
   svg:`<svg viewBox="0 0 300 180" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- Tige de selle schéma DT Swiss style -->
     <rect x="132" y="15" width="36" height="130" rx="8" fill="#e8400c" opacity=".4" stroke="#e8400c" stroke-width="2"/>
     <!-- Selle stylisée en haut -->
     <path d="M80 18 Q130 8 150 12 Q170 8 220 18" stroke="#9a9590" stroke-width="2.5" fill="none" stroke-linecap="round"/>
     <line x1="150" y1="12" x2="150" y2="20" stroke="#9a9590" stroke-width="2"/>
     <!-- Pied à coulisse -->
     <line x1="90" y1="80" x2="210" y2="80" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="5,3"/>
     <line x1="132" y1="74" x2="132" y2="86" stroke="#2d7ff9" stroke-width="2"/>
     <line x1="168" y1="74" x2="168" y2="86" stroke="#2d7ff9" stroke-width="2"/>
     <text x="150" y="68" text-anchor="middle" font-size="11" fill="#2d7ff9" font-family="monospace">Ø tige</text>
     <!-- Tube de selle cadre -->
     <rect x="120" y="145" width="60" height="25" rx="3" fill="#1e1e1e" stroke="#333" stroke-width="1.5"/>
     <text x="150" y="162" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">Tube de selle cadre</text>
     <text x="150" y="175" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">Ø souvent gravé sur la tige</text>
   </svg>`,
   steps:[
     {t:'<strong>Sortez la tige du cadre.</strong> Le Ø est souvent <strong>gravé ou imprimé</strong> sur la tige.'},
     {t:'Pied à coulisse mode extérieur, au <strong>milieu de la tige</strong>.'},
     {t:'<strong>Courant :</strong> <code>27,2</code> · <code>30,9</code> · <code>31,6</code> mm'},
     {t:'Tolérance très faible — précision à <code>0,1mm</code> nécessaire.'},
   ],
   tip:'⚠️ Ne jamais forcer une tige légèrement trop large — risque de fissure du tube de selle cadre.'}},
],
identify(a){
  const d=parseFloat(a.diam);
  const map=[[22.2,'22,2mm','Enfant/BMX/ville bas de gamme'],[25.4,'25,4mm','VTT ancien, ville'],[26.8,'26,8mm','Certains cadres alu VTT'],[27.0,'27,0mm','Certains cadres acier'],[27.2,'27,2mm','Route, gravel, acier haut de gamme — standard classique'],[28.6,'28,6mm','Certains cadres acier/alu'],[30.0,'30,0mm','Cadres alu VTT (transitoire)'],[30.9,'30,9mm','VTT courant — dropper post'],[31.6,'31,6mm','VTT moderne, gravel carbone — standard actuel dominant'],[34.9,'34,9mm','E-bike, VTT oversize']];
  for(const[v,s,u] of map){
    if(Math.abs(d-v)<0.3) return{std:s,detail:`Ø ${v}mm`,compat:[u],note:`Usage : ${u}. La tige doit correspondre exactement au tube de selle (tolérance ±0,1mm).`,warn:''};
  }
  return{std:`${d}mm`,detail:'Non standard',compat:[],note:'Vérifiez la mesure. Certains cadres spéciaux ont des Ø intermédiaires.',warn:'Non standard — vérifiez'};
}},

frein:{title:'Frein à Disque',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="10" stroke="#e8400c" stroke-width="2"/><circle cx="14" cy="14" r="4" fill="none" stroke="#9a9590" stroke-width="1.5"/><circle cx="14" cy="4.5" r="1.5" fill="#9a9590"/><circle cx="22.2" cy="9.5" r="1.5" fill="#9a9590"/><circle cx="22.2" cy="18.5" r="1.5" fill="#9a9590"/><circle cx="14" cy="23.5" r="1.5" fill="#9a9590"/><circle cx="5.8" cy="18.5" r="1.5" fill="#9a9590"/><circle cx="5.8" cy="9.5" r="1.5" fill="#9a9590"/></svg>`,
steps:[
  {id:'mont',q:'Type de montage de l\'étrier ?',type:'choice',
   guide:{title:'Identifier le montage',
   svg:`<svg viewBox="0 0 300 170" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- IS -->
     <rect x="20" y="30" width="60" height="50" rx="3" fill="#1e1e1e" stroke="#2a2a2a" stroke-width="1.5"/>
     <rect x="10" y="45" width="18" height="6" rx="2" fill="#e8400c" opacity=".8"/>
     <rect x="10" y="59" width="18" height="6" rx="2" fill="#e8400c" opacity=".8"/>
     <text x="50" y="100" text-anchor="middle" font-size="9" fill="#e8400c" font-family="monospace">IS</text>
     <text x="50" y="112" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">vis horiz.</text>
     <!-- PM -->
     <rect x="120" y="30" width="60" height="50" rx="3" fill="#1e1e1e" stroke="#2a2a2a" stroke-width="1.5"/>
     <rect x="133" y="10" width="6" height="22" rx="2" fill="#2d7ff9" opacity=".8"/>
     <rect x="151" y="10" width="6" height="22" rx="2" fill="#2d7ff9" opacity=".8"/>
     <text x="150" y="100" text-anchor="middle" font-size="9" fill="#2d7ff9" font-family="monospace">PM</text>
     <text x="150" y="112" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">pattes vert.</text>
     <!-- FM -->
     <rect x="220" y="50" width="60" height="20" rx="3" fill="#22c55e" opacity=".4" stroke="#22c55e" stroke-width="1.5"/>
     <rect x="233" y="70" width="6" height="18" rx="2" fill="#22c55e" opacity=".8"/>
     <rect x="251" y="70" width="6" height="18" rx="2" fill="#22c55e" opacity=".8"/>
     <text x="250" y="100" text-anchor="middle" font-size="9" fill="#22c55e" font-family="monospace">FM</text>
     <text x="250" y="112" text-anchor="middle" font-size="8" fill="#9a9590" font-family="monospace">plat dessous</text>
   </svg>`,
   steps:[
     {t:'<strong>IS</strong> : vis dans l\'axe de la roue — horizontales.'},
     {t:'<strong>PM</strong> : deux pattes verticales parallèles au disque.'},
     {t:'<strong>FM</strong> : étrier très plat vissé par dessous (route/gravel).'},
   ]},
   choices:[
     {val:'IS',icon:'↔️',lbl:'<strong>IS</strong> — International Std',desc:'vis horizontales'},
     {val:'PM',icon:'↕️',lbl:'<strong>Post Mount</strong>',desc:'pattes verticales'},
     {val:'FM',icon:'⬇️',lbl:'<strong>Flat Mount</strong>',desc:'plat par dessous (route)'},
   ]},
  {id:'rfx',q:'Fixation du rotor sur le moyeu ?',type:'choice',
   guide:{title:'6 trous vs Center Lock',
   svg:`<svg viewBox="0 0 300 130" fill="none" xmlns="http://www.w3.org/2000/svg">
     <!-- 6 bolt -->
     <circle cx="80" cy="65" r="45" stroke="#9a9590" stroke-width="2"/>
     <circle cx="80" cy="21" r="5" fill="#e8400c"/><circle cx="119" cy="43" r="5" fill="#e8400c"/><circle cx="119" cy="87" r="5" fill="#e8400c"/><circle cx="80" cy="109" r="5" fill="#e8400c"/><circle cx="41" cy="87" r="5" fill="#e8400c"/><circle cx="41" cy="43" r="5" fill="#e8400c"/>
     <text x="80" y="65" text-anchor="middle" font-size="9" fill="#e8400c" font-family="monospace" dominant-baseline="middle">6×M5</text>
     <text x="80" y="120" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">6 TROUS</text>
     <!-- CL -->
     <circle cx="220" cy="65" r="45" stroke="#9a9590" stroke-width="2"/>
     <circle cx="220" cy="65" r="22" stroke="#2d7ff9" stroke-width="3"/>
     <path d="M208 55 l10 10 10-10" stroke="#2d7ff9" stroke-width="2" fill="none"/>
     <text x="220" y="120" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">CENTER LOCK</text>
   </svg>`,
   steps:[
     {t:'<strong>6 trous</strong> : 6 vis M5 Torx T25, cercle 44mm — universel.'},
     {t:'<strong>Center Lock</strong> : cannelures centrales Shimano + bague de serrage.'},
     {t:'Des adaptateurs 6T → CL et CL → 6T existent.'},
   ]},
   choices:[
     {val:'6bolt',icon:'⭐',lbl:'<strong>6 trous</strong>',desc:'6 vis Torx T25 · universel'},
     {val:'cl',icon:'🔒',lbl:'<strong>Center Lock</strong>',desc:'cannelures Shimano'},
   ]},
  {id:'dr',q:'Diamètre du rotor ?',type:'number',unit:'mm',ph:'160',
   guide:{title:'Mesurer le Ø du rotor',
   steps:[
     {t:'Mesurez le <strong>Ø extérieur</strong> — souvent <strong>gravé sur le rotor</strong>.'},
     {t:'<code>140</code> · <code>160</code> · <code>180</code> · <code>200</code> · <code>203</code> mm'},
   ]}},
],
identify(a){
  const m=a.mont,rf=a.rfx,d=Math.round(parseFloat(a.dr));
  const adapt={IS:{140:'Adaptateur IS→PM req.',160:'IS natif env. 160',180:'IS→PM +20mm',200:'IS→PM +40mm',203:'IS→PM +40mm'},PM:{140:'PM→adapt. -20mm ou FM',160:'PM natif ✓',180:'PM +20mm adaptateur',200:'PM +40mm adaptateur',203:'PM +40mm'},FM:{140:'FM natif ✓',160:'FM +20mm adaptateur',180:'FM spécifique requis'}};
  const ad=(adapt[m]&&adapt[m][d])||'Adaptateur spécifique requis';
  const pwr=d<=140?'Modérée (route)':d<=160?'Bonne':d<=180?'Haute':d<=200?'Très haute':'Extrême (DH/e-bike)';
  return{std:`${m} · ${rf==='6bolt'?'6 trous':'Center Lock'} · Ø${d}mm`,detail:`Montage ${m} · Fixation rotor ${rf} · Ø${d}mm`,compat:[pwr,ad,rf==='cl'?'Outil cassette pour serrage':'Clé Torx T25 · 6×M5'],note:`${ad}. Puissance : ${pwr}.`,warn:d>=200?'Vérifiez contraintes thermiques à haute vitesse':''};
}},

chaine:{title:'Chaîne & Vitesses',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><rect x="3" y="11" width="5" height="6" rx="1.5" stroke="#e8400c" stroke-width="1.5"/><rect x="11" y="11" width="5" height="6" rx="1.5" stroke="#9a9590" stroke-width="1.5"/><rect x="19" y="11" width="5" height="6" rx="1.5" stroke="#e8400c" stroke-width="1.5"/><line x1="8" y1="14" x2="11" y2="14" stroke="#9a9590" stroke-width="1.5"/><line x1="16" y1="14" x2="19" y2="14" stroke="#9a9590" stroke-width="1.5"/></svg>`,
steps:[
  {id:'vit',q:'Nombre de pignons sur la cassette ?',type:'choice',guide:{title:'Compter les vitesses',steps:[{t:'Comptez les <strong>pignons sur la cassette arrière</strong>.'},{t:'Ou lisez les chiffres sur votre <strong>levier de vitesse</strong>.'}]},choices:[{val:'678',icon:'6️⃣',lbl:'<strong>6 / 7 / 8</strong> vitesses',desc:''},{val:'9',icon:'9️⃣',lbl:'<strong>9</strong> vitesses',desc:''},{val:'10',icon:'🔟',lbl:'<strong>10</strong> vitesses',desc:''},{val:'11',icon:'1️⃣',lbl:'<strong>11</strong> vitesses',desc:''},{val:'12',icon:'2️⃣',lbl:'<strong>12</strong> vitesses',desc:''}]},
  {id:'marq',q:'Fabricant du groupe ?',type:'choice',choices:[{val:'shimano',icon:'⚙️',lbl:'<strong>Shimano</strong>',desc:''},{val:'sram',icon:'🔴',lbl:'<strong>SRAM</strong>',desc:''},{val:'campa',icon:'🟡',lbl:'<strong>Campagnolo</strong>',desc:''},{val:'autre',icon:'❓',lbl:'<strong>Autre / Inconnu</strong>',desc:''}]},
],
identify(a){
  const v=a.vit,m=a.marq;
  const db={'678':{l:'7,1–7,3 mm',r:'Shimano CN-HG/KMC Z7/SRAM PC830',fh:'HG standard',n:'6/7/8v interchangeables entre marques.'},'9':{l:'6,7–6,8 mm',r:'Shimano CN-HG53/KMC X9/SRAM PC951',fh:'HG',n:'Compatible Shimano et SRAM.'},'10':{l:'6,1–6,3 mm',r:'Shimano CN-HG54/KMC X10/SRAM PC1031',fh:'HG',n:'Shimano/SRAM compatibles.'},'11':{l:'5,5–5,65 mm',r:'Shimano CN-HG601/KMC X11/SRAM PC1130',fh:'HG ou Campa',n:'Shimano/SRAM compat. Campagnolo légèrement différent.'},'12':{l:'5,2–5,3 mm',r:m==='shimano'?'CN-M8100/CN-R9200':m==='sram'?'SRAM Eagle/KMC e12':'KMC e12/Connex',fh:m==='shimano'?'Micro Spline':m==='sram'?'XD (MTB) / XDR (route)':'Micro Spline ou XD',n:m==='shimano'?'12v Shimano spécifique — ne pas utiliser Eagle SRAM.':m==='sram'?'Eagle SRAM. Freehub XD (MTB) ou XDR (route) requis.':'Vérifiez compatibilité.'}};
  const c=db[v]||db['678'];
  return{std:`Chaîne ${v}v ${m}`,detail:`Largeur ext. : ${c.l}`,compat:[`Références : ${c.r}`,`Freehub : ${c.fh}`],note:c.n+' Pas de chaîne toujours 1/2" (12,7mm).',warn:v==='12'&&m==='shimano'?'Ne pas mixer avec Eagle SRAM':''};
}},

freehub:{title:'Corps de Roue Libre',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="9" stroke="#9a9590" stroke-width="1.5"/><circle cx="14" cy="14" r="5" stroke="#e8400c" stroke-width="2"/><path d="M14 5v4M14 19v4M5 14h4M19 14h4" stroke="#9a9590" stroke-width="1.5" stroke-linecap="round"/></svg>`,
steps:[
  {id:'vit',q:'Nombre de vitesses ?',type:'choice',choices:[{val:'678',icon:'6️⃣',lbl:'<strong>6/7/8</strong>',desc:''},{val:'9',icon:'9️⃣',lbl:'<strong>9</strong>',desc:''},{val:'10',icon:'🔟',lbl:'<strong>10</strong>',desc:''},{val:'11',icon:'1️⃣',lbl:'<strong>11</strong>',desc:''},{val:'12',icon:'2️⃣',lbl:'<strong>12</strong>',desc:''}]},
  {id:'marq',q:'Fabricant ?',type:'choice',choices:[{val:'shimano',icon:'⚙️',lbl:'<strong>Shimano</strong>',desc:''},{val:'sram',icon:'🔴',lbl:'<strong>SRAM</strong>',desc:''},{val:'campa',icon:'🟡',lbl:'<strong>Campagnolo</strong>',desc:''}]},
  {id:'usage',q:'Type de vélo ?',type:'choice',choices:[{val:'route',icon:'🚴',lbl:'<strong>Route / Gravel</strong>',desc:''},{val:'vtt',icon:'🚵',lbl:'<strong>VTT</strong>',desc:''},{val:'ville',icon:'🏙️',lbl:'<strong>Ville / Trekking</strong>',desc:''}]},
],
identify(a){
  const v=a.vit,m=a.marq,u=a.usage;
  let std='HG',note='',warn='';
  if(m==='shimano'&&v!=='12'){std='HG (Hyperglide)';note='Standard Shimano classique. Compatible SRAM ≤11v. Splines classiques.';}
  else if(m==='shimano'&&v==='12'){std='Micro Spline';note='Nouveau standard Shimano 12v. Incompatible HG.';warn='Incompatible corps HG existant';}
  else if(m==='sram'&&v!=='12'){std='HG compatible';note='SRAM ≤11v = compatible corps HG standard Shimano.';}
  else if(m==='sram'&&v==='12'&&u==='route'){std='XDR';note='SRAM route 12v. XDR = version route de XD, 2mm plus long.';warn='Incompatible XD et HG';}
  else if(m==='sram'&&v==='12'){std='XD Driver';note='SRAM Eagle 12v MTB. Permet pignon 10t minimum. Filetage externe.';warn='Incompatible HG et XDR';}
  else if(m==='campa'&&v==='12'){std='N3W (Campagnolo)';note='Nouveau standard Campagnolo 9–13v. Incompatible HG et XD.';warn='Propriétaire Campagnolo';}
  else{std='Campagnolo (splines Campa)';note='Campagnolo ≤11v. Splines Campa spécifiques — incompatible Shimano/SRAM.';warn='Propriétaire Campagnolo';}
  return{std,detail:`${v}v ${m} ${u}`,compat:[`Cassette : ${m} ${v}v`,`Pignon mini : ${std==='XD Driver'||std==='Micro Spline'?'10t':'11t'}`],note,warn};
}},

bcd:{title:'Plateau / BCD',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><circle cx="14" cy="14" r="4" fill="#e8400c"/><circle cx="14" cy="6" r="2" fill="#9a9590"/><circle cx="21" cy="10" r="2" fill="#9a9590"/><circle cx="21" cy="18" r="2" fill="#9a9590"/><circle cx="14" cy="22" r="2" fill="#9a9590"/><circle cx="7" cy="18" r="2" fill="#9a9590"/><circle cx="7" cy="10" r="2" fill="#9a9590"/></svg>`,
steps:[
  {id:'bras',q:'Nombre de bras (araignée) ?',type:'choice',guide:{title:'Compter les bras',steps:[{t:'Comptez les <strong>bras de l\'araignée</strong> du pédalier.'},{t:'<strong>Sans araignée</strong> = Direct Mount — plateau vissé directement.'}]},choices:[{val:'4',icon:'4️⃣',lbl:'<strong>4 bras</strong>',desc:''},{val:'5',icon:'5️⃣',lbl:'<strong>5 bras</strong>',desc:''},{val:'dm',icon:'🔩',lbl:'<strong>Direct Mount</strong>',desc:'sans araignée'}]},
  {id:'bcd',q:'BCD mesuré (centre de trou à centre de trou) ?',type:'number',unit:'mm',ph:'104',
   guide:{title:'Mesurer le BCD',
   svg:`<svg viewBox="0 0 300 180" fill="none" xmlns="http://www.w3.org/2000/svg">
     <circle cx="150" cy="90" r="70" stroke="#2a2a2a" stroke-width="2"/>
     <circle cx="150" cy="90" r="52" stroke="#e8400c" stroke-width="1.5" stroke-dasharray="6,4"/>
     <!-- 5 vis -->
     <circle cx="150" cy="38" r="6" fill="#e8400c"/>
     <circle cx="199" cy="56" r="6" fill="#e8400c"/>
     <circle cx="180" cy="133" r="6" fill="#e8400c"/>
     <circle cx="120" cy="133" r="6" fill="#e8400c"/>
     <circle cx="101" cy="56" r="6" fill="#e8400c"/>
     <!-- Mesure entre non-adjacents -->
     <line x1="150" y1="38" x2="199" y2="56" stroke="#2d7ff9" stroke-width="1.5" stroke-dasharray="4,3"/>
     <text x="188" y="40" font-size="9" fill="#2d7ff9" font-family="monospace">Centre→Centre</text>
     <!-- BCD cercle -->
     <line x1="150" y1="38" x2="150" y2="90" stroke="#22c55e" stroke-width="1" stroke-dasharray="3,2"/>
     <text x="156" y="68" font-size="9" fill="#22c55e" font-family="monospace">BCD/2</text>
     <text x="150" y="165" text-anchor="middle" font-size="9" fill="#9a9590" font-family="monospace">5 bras : mesure × 1,051</text>
   </svg>`,
   steps:[
     {t:'Mesurez centre-à-centre entre deux trous <strong>non adjacents</strong>.'},
     {t:'<strong>4 bras :</strong> mesure directe. <strong>5 bras :</strong> mesure × 1,051.'},
     {t:'<code>104mm</code> (VTT) · <code>110mm</code> (route compact) · <code>130mm</code> (route std)'},
   ]}},
],
identify(a){
  const br=a.bras,bcd=parseFloat(a.bcd);
  if(br==='dm') return{std:'Direct Mount',detail:'Plateau sans araignée',compat:['SRAM Force/Red DM','Shimano HT2 XTR/XT/SLX','Rotor DM'],note:'Vérifiez la marque — Direct Mount est propriétaire selon fabricant.',warn:'Propriétaire — vérifiez'};
  const db={'4-104':{std:'BCD 104mm · 4 bras',u:'VTT 1× et 2× — standard dominant MTB'},'5-110':{std:'BCD 110mm · 5 bras',u:'Route compact (34/50t) — Shimano/SRAM moderne'},'5-130':{std:'BCD 130mm · 5 bras',u:'Route standard ancien (39/53t)'},'5-112':{std:'BCD 112mm · 5 bras',u:'Campagnolo compact'},'5-135':{std:'BCD 135mm · 5 bras',u:'Campagnolo standard ancien'},'4-96':{std:'BCD 96mm · 4 bras',u:'VTT Shimano ancien'},'4-64':{std:'BCD 64mm · 4 bras',u:'Petit plateau intérieur VTT 2×'}};
  const key=`${br}-${Math.round(bcd)}`;
  const f=db[key];
  if(f) return{std:f.std,detail:`${br} bras · ${bcd}mm`,compat:[f.u],note:`Usage : ${f.u}. Précision ±1mm peut changer le standard.`,warn:''};
  return{std:`BCD ${bcd}mm · ${br} bras`,detail:'BCD non standard courant',compat:[],note:'BCD peu courant ou vintage. Vérifiez chez le fabricant.',warn:'Vérifiez la mesure'};
}},

etrto_dec:{title:'Décodeur ETRTO',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><rect x="3" y="6" width="22" height="16" rx="3" stroke="#e8400c" stroke-width="1.5"/><text x="14" y="17" text-anchor="middle" font-size="8" fill="#e8400c" font-family="monospace" font-weight="bold">37-622</text></svg>`,
steps:[
  {id:'l',q:'Largeur (1er chiffre ETRTO) ?',type:'number',unit:'mm',ph:'37',guide:{title:'Format ETRTO',steps:[{t:'Code sur le flanc : <code>37-622</code> → largeur=37, BSD=622.'},{t:'Entrez chaque valeur séparément.'}]}},
  {id:'b',q:'BSD — diamètre siège de tringle ?',type:'number',unit:'mm',ph:'622'},
],
identify(a){
  const l=parseFloat(a.l),b=Math.round(parseFloat(a.b));
  const nm={203:'12"',305:'16"BMX',349:'16"Brompton',406:'20"BMX',451:'20"route',507:'24"VTT',559:'26"MTB',584:'27,5"/650B',622:'700C/29"',630:'27"route',635:'28"hollandais'};
  const name=nm[b]||`BSD ${b}mm`;
  return{std:`${Math.round(l)}-${b}`,detail:`${name} · Largeur ${l}mm`,compat:[name,l<30?'Route':l<45?'Polyvalent':'Tout-terrain'],note:`Code ETRTO : ${Math.round(l)}-${b}. Appellation : ${name}.`,warn:''};
}},

shis_dec:{title:'Décodeur SHIS',icon:`<svg width="22" height="22" viewBox="0 0 28 28" fill="none"><rect x="3" y="6" width="22" height="16" rx="3" stroke="#9a9590" stroke-width="1.5"/><text x="14" y="17" text-anchor="middle" font-size="7" fill="#2d7ff9" font-family="monospace" font-weight="bold">ZS44|28</text></svg>`,
steps:[
  {id:'code',q:'Entrez le code SHIS à décoder',type:'text',ph:'ZS44|28.6',guide:{title:'Format SHIS',steps:[{t:'Format : <code>TYPE+Ø|colonne</code>'},{t:'Ex : <code>ZS44|28.6</code> · <code>IS52|40</code> · <code>EC34|28.6</code>'},{t:'Trouvé dans les specs cadre ou sur la boîte du jeu de direction.'}]}},
],
identify(a){
  const c=(a.code||'').toUpperCase().trim();
  const db={'ZS44|28.6':{d:'Zero Stack · 44mm · 1⅛"',u:'Route/gravel/XC — standard actuel dominant'},'ZS44|30':{d:'Zero Stack · 44mm · bas direction',u:'Route (bas)'},'ZS56|40':{d:'Zero Stack · 56mm · 1½"',u:'VTT tapered bas'},'EC34|28.6':{d:'External Cup · 34mm · 1⅛"',u:'Route, gravel, city'},'EC44|40':{d:'External Cup · 44mm · 1½"',u:'VTT tapered bas'},'IS42|28.6':{d:'Integrated · 42mm · 1⅛"',u:'Route/VTT IS'},'IS52|40':{d:'Integrated · 52mm · 1½"',u:'VTT tapered bas IS'},'IS52|28.6':{d:'Integrated · 52mm · 1⅛"',u:'VTT tapered haut IS'},'ZS49|28.6':{d:'Zero Stack · 49mm · 1⅛"',u:'Certains cadres carbone route'}};
  const f=db[c];
  if(f) return{std:c,detail:f.d,compat:[f.u,'Vérifiez haut ET bas pour jeu complet'],note:`Code SHIS : ${c}. ${f.u}.`,warn:''};
  return{std:c||'?',detail:'Code non reconnu',compat:[],note:'Format : TYPE (EC/ZS/IS) + Ø (44…) + | + colonne (28.6/40…)',warn:'Format incorrect'};
}},

};

// ═══════════════════════════════
// STATE & DIAG ENGINE
// ═══════════════════════════════
let S={diag:null,step:0,ans:{}};

function goHome(){ showTab('home'); S={diag:null,step:0,ans:{}}; }

function startDiag(id){
  S={diag:id,step:0,ans:{}};
  showScreen('diag');
  renderStep();
}

function renderStep(){
  const d=DIAGS[S.diag];
  const s=d.steps[S.step];
  const isLast=S.step===d.steps.length-1;

  document.getElementById('diagTitle').textContent=`${d.title}`;
  document.getElementById('diagStep').textContent=`Étape ${S.step+1} sur ${d.steps.length}`;
  document.getElementById('btnPrev').style.display=S.step===0?'none':'flex';
  document.getElementById('btnLbl').textContent=isLast?'🔍 Identifier':'Suivant';

  // Dots
  const dots=document.getElementById('progDots');
  dots.innerHTML=d.steps.map((_,i)=>`<div class="pdot ${i<S.step?'done':i===S.step?'active':''}"></div>`).join('');

  let html=`<div class="step-lbl">Étape ${S.step+1} / ${d.steps.length}</div>
  <div class="step-q">${s.q}</div>`;

  // Guide
  if(s.guide){
    html+=`<div class="guide-card">
      <div class="guide-head" onclick="toggleGuide(this)">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="#9a9590" stroke-width="2"/><path d="M12 8v4M12 16h.01" stroke="#9a9590" stroke-width="2" stroke-linecap="round"/></svg>
        <span class="guide-head-title">${s.guide.title}</span>
        <span class="gtog" id="gtog">▾</span>
      </div>
      <div class="guide-body" id="gbody">`;
    if(s.guide.svg) html+=`<div class="diag-svg-wrap">${s.guide.svg}</div>`;
    html+=`<div class="guide-steps">`;
    (s.guide.steps||[]).forEach((gs,i)=>{
      html+=`<div class="gstep"><div class="gstep-num">${i+1}</div><div class="gstep-text">${gs.t}</div></div>`;
    });
    html+=`</div></div></div>`;
  }

  if(s.tip) html+=`<div class="tip-bar">${s.tip}</div>`;

  // Choice
  if(s.type==='choice'){
    html+=`<div class="choice-list">`;
    s.choices.forEach(c=>{
      const sel=S.ans[s.id]===c.val;
      html+=`<div class="choice-item ${sel?'selected':''}" onclick="pick('${s.id}','${c.val}',this)">
        <div class="ci-icon">${c.icon||''}</div>
        <div class="ci-body">
          <div class="ci-val">${c.val}</div>
          <div class="ci-label">${c.lbl}${c.desc?` <span style="color:var(--muted)">— ${c.desc}</span>`:''}</div>
        </div>
        <div class="ci-check">${sel?'✓':''}</div>
      </div>`;
    });
    html+=`</div>`;
  }

  // Number
  if(s.type==='number'){
    html+=`<div class="num-wrap">
      <div class="num-lbl">${s.q}</div>
      <div class="num-row">
        <input class="num-inp" type="number" inputmode="decimal" id="inp_m"
          placeholder="${s.ph}" value="${S.ans[s.id]||''}" step="0.1" autocomplete="off"
          onkeydown="if(event.key==='Enter')nextStep()">
        <span class="num-unit">${s.unit}</span>
      </div>
    </div>`;
  }

  // Text
  if(s.type==='text'){
    html+=`<div class="text-wrap">
      <div class="num-lbl">${s.q}</div>
      <input class="text-inp" type="text" inputmode="text" id="inp_m"
        placeholder="${s.ph}" value="${S.ans[s.id]||''}"
        autocomplete="off" autocorrect="off" spellcheck="false"
        onkeydown="if(event.key==='Enter')nextStep()">
    </div>`;
  }

  document.getElementById('stepScroll').innerHTML=html;
  document.getElementById('stepScroll').scrollTop=0;
}

function toggleGuide(btn){
  const b=document.getElementById('gbody');
  const t=document.getElementById('gtog');
  const o=b.classList.contains('open');
  b.classList.toggle('open',!o);
  t.classList.toggle('open',!o);
}

function pick(sid,val,el){
  S.ans[sid]=val;
  document.querySelectorAll('.choice-item').forEach(c=>{c.classList.remove('selected');c.querySelector('.ci-check').textContent='';});
  el.classList.add('selected');
  el.querySelector('.ci-check').textContent='✓';
}

function collectInp(){
  const s=DIAGS[S.diag].steps[S.step];
  if(s.type==='number'||s.type==='text'){
    const v=document.getElementById('inp_m')?.value;
    if(v) S.ans[s.id]=v;
  }
}

function nextStep(){
  collectInp();
  const d=DIAGS[S.diag];
  if(S.step===d.steps.length-1){ showResult(); }
  else { S.step++; renderStep(); }
}

function prevStep(){ if(S.step>0){S.step--;renderStep();} }

function showResult(){
  const d=DIAGS[S.diag];
  let res;
  try{res=d.identify(S.ans);}catch(e){res={std:'Erreur',detail:'Vérifiez vos réponses',compat:[],note:'',warn:'Erreur de calcul'};}

  const cH=res.compat.filter(Boolean).map(c=>{
    const dot=c.includes('Incompat')||c.includes('attention')||c.includes('⚠')?'cdot-r':c.includes('adapt')||c.includes('vérif')?'cdot-o':'cdot-g';
    return `<div class="compat-item"><div class="cdot ${dot}"></div>${c}</div>`;
  }).join('');

  document.getElementById('resScroll').innerHTML=`
  <div class="res-hero">
    <div class="res-badge">✓ IDENTIFIÉ</div>
    <div class="res-std">${res.std}</div>
    <div class="res-detail">${res.detail}</div>
  </div>
  ${res.warn?`<div class="warn-bar">⚠️ ${res.warn}</div>`:''}
  ${cH?`<div class="res-sec"><div class="rs-lbl">Compatibilités</div><div class="compat-list">${cH}</div></div>`:''}
  ${res.note?`<div class="res-sec"><div class="rs-lbl">Informations</div><div class="rs-note">${res.note}</div></div>`:''}
  <div class="res-foot">
    <button class="btn-restart" onclick="startDiag('${S.diag}')">↺ Recommencer</button>
    <button class="btn-home" onclick="goHome()">🏠 Accueil</button>
  </div>`;

  showScreen('result');
}

// ═══════════════════════════════
// SPLASH
// ═══════════════════════════════

// ══════════════════════════════════════════
// TOAST NOTIFICATION
// ══════════════════════════════════════════
function showToast(msg, isError=false){
  const t = document.getElementById('veloToast');
  if(!t) return;
  t.textContent = msg;
  t.className = 'velo-toast' + (isError?' error':'');
  void t.offsetWidth;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 2600);
}

// ══════════════════════════════════════════
// HISTORIQUE — localStorage
// ══════════════════════════════════════════
function histSave(){
  const filled = DV_POINTS.filter(p => dvState[p.id].status !== null);
  if(!filled.length){ showToast('Aucun point contrôlé', true); return; }
  const f = dvGetFiche();
  let penalite = 0;
  filled.forEach(p => { penalite += DV_STATUS[dvState[p.id].status].score; });
  const score = Math.max(0, Math.round(100-(penalite/Math.max(filled.length*80,1))*100));
  const counts = {ok:0,warn:0,fix:0,replace:0,danger:0};
  filled.forEach(p => { if(counts[dvState[p.id].status]!==undefined) counts[dvState[p.id].status]++; });

  const entry = {
    id: Date.now(),
    date: new Date().toLocaleDateString('fr-FR'),
    ts: Date.now(),
    client: { nom:f.nom, prenom:f.prenom, tel:f.tel, mail:f.mail },
    velo: { marque:f.marque, modele:f.modele, annee:f.annee, type:f.type, couleur:f.couleur, serie:f.serie, km:f.km },
    atelier: { meca:f.meca, atelier:f.atelier },
    score, counts,
    points: DV_POINTS.reduce((acc,p)=>{ acc[p.id]=dvState[p.id]; return acc; }, {}),
    comment: document.getElementById('dvSumComment')?.value || ''
  };

  const hist = histLoad();
  hist.unshift(entry);
  if(hist.length > 50) hist.pop(); // max 50
  try { localStorage.setItem('veloid_hist', JSON.stringify(hist)); } catch(e){}
  showToast('✓ Diagnostic sauvegardé !');
}

function histLoad(){
  try { return JSON.parse(localStorage.getItem('veloid_hist')||'[]'); } catch(e){ return []; }
}

function histRender(){
  const hist = histLoad();
  const el = document.getElementById('histList');
  const cnt = document.getElementById('histCount');
  if(!el) return;
  if(cnt) cnt.textContent = hist.length + ' diagnostic(s) sauvegardé(s)';
  if(!hist.length){
    el.innerHTML = '<div class="hist-empty">📋 Aucun diagnostic sauvegardé<br>Réalisez un diagnostic et appuyez sur<br><strong>💾 Sauvegarder</strong></div>';
    return;
  }
  el.innerHTML = hist.map((h,idx) => {
    const clientLine = [h.client.prenom, h.client.nom].filter(Boolean).join(' ') || '— Client inconnu —';
    const veloLine   = [h.velo.marque, h.velo.modele, h.velo.annee].filter(Boolean).join(' ') || '— Vélo non renseigné —';
    const scoreCol   = h.score>=80?'#22c55e':h.score>=60?'#f97316':'#ef4444';
    const tags = [
      h.counts.danger  ? `<span class="hist-tag" style="background:rgba(168,85,247,.15);color:#a855f7">⚠️ ${h.counts.danger} danger</span>` : '',
      h.counts.replace ? `<span class="hist-tag" style="background:rgba(239,68,68,.12);color:#ef4444">🔴 ${h.counts.replace} remplacer</span>` : '',
      h.counts.fix     ? `<span class="hist-tag" style="background:rgba(249,115,22,.12);color:#f97316">🔧 ${h.counts.fix} régler</span>` : '',
      h.counts.ok      ? `<span class="hist-tag" style="background:rgba(34,197,94,.12);color:#22c55e">✓ ${h.counts.ok} OK</span>` : '',
    ].filter(Boolean).join('');
    return `<div class="hist-card">
      <div class="hist-card-hdr">
        <div class="hist-score-dot" style="background:${scoreCol}">${h.score}</div>
        <div class="hist-info">
          <div class="hist-client">${clientLine}</div>
          <div class="hist-velo">${veloLine}</div>
          <div class="hist-date">${h.date}${h.atelier.atelier?' · '+h.atelier.atelier:''}</div>
        </div>
      </div>
      <div class="hist-tags">${tags}</div>
      <div class="hist-card-btns">
        <button class="hist-open-btn" onclick="histRestore(${idx})">↩ Rouvrir</button>
        <button class="hist-devis-btn" onclick="histToDevis(${idx})">€ Devis</button>
        <button class="hist-del-btn" onclick="histDelete(${idx})">🗑</button>
      </div>
    </div>`;
  }).join('');
}

function histDelete(idx){
  const hist = histLoad();
  hist.splice(idx,1);
  try { localStorage.setItem('veloid_hist', JSON.stringify(hist)); } catch(e){}
  histRender();
  showToast('Diagnostic supprimé');
}

function histClearAll(){
  if(!confirm("Effacer tout l'historique ?")) return;
  try { localStorage.removeItem('veloid_hist'); } catch(e){}
  histRender();
  showToast('Historique effacé');
}

function histRestore(idx){
  const hist = histLoad();
  const h = hist[idx];
  if(!h) return;

  // Remplir fiche client
  const set = (id,v) => { const e=document.getElementById(id); if(e) e.value=v||''; };
  set('dvClientNom', h.client.nom); set('dvClientPrenom', h.client.prenom);
  set('dvClientTel', h.client.tel); set('dvClientMail', h.client.mail);
  set('dvVeloMarque', h.velo.marque); set('dvVeloModele', h.velo.modele);
  set('dvVeloAnnee', h.velo.annee); set('dvVeloCouleur', h.velo.couleur);
  set('dvVeloSerie', h.velo.serie); set('dvVeloKm', h.velo.km);
  set('dvMeca', h.atelier.meca); set('dvAtelier', h.atelier.atelier);
  const typeEl = document.getElementById('dvVeloType');
  if(typeEl) typeEl.value = h.velo.type||'';

  // Réinitialiser et remplir état
  dvInit();
  DV_POINTS.forEach(p => {
    const s = h.points[p.id];
    if(!s || !s.status) return;
    dvState[p.id] = { ...s };
    // Mettre à jour l'UI carte
    const card = document.getElementById('dvCard'+p.id);
    if(!card) return;
    const st = DV_STATUS[s.status];
    card.className = 'dv-card open st-'+s.status;
    card.querySelectorAll('.dv-st-btn').forEach(b => {
      if(b.onclick?.toString().includes("'"+s.status+"'")) b.classList.add(st.cls);
    });
    if(s.priority){
      const pClass = 'p-'+s.priority;
      card.querySelectorAll('.dv-prio-btn').forEach(b => { if(b.classList.contains(pClass)) b.classList.add(pClass); });
    }
    const noteEl = document.getElementById('dvNote'+p.id);
    if(noteEl) noteEl.value = s.note||'';
    const actEl = document.getElementById('dvAction'+p.id);
    if(actEl) actEl.value = s.action||'';
    // Badge
    const badge = document.getElementById('dvBadge'+p.id);
    if(badge){ badge.style.display='flex'; badge.style.background=st.col+'22'; badge.style.color=st.col; badge.textContent=st.label; }
    // Marqueur
    if(p.zone){
      const m=document.getElementById(p.zone);
      if(m){ m.setAttribute('fill',st.col); m.setAttribute('stroke',st.col); m.setAttribute('r','10'); }
      const mt=document.getElementById(p.zone.replace('dvM','dvMt'));
      if(mt) mt.setAttribute('fill','#fff');
    }
  });
  const cmtEl = document.getElementById('dvSumComment');
  if(cmtEl) cmtEl.value = h.comment||'';

  dvUpdateScore();
  dvUpdateSummary();
  showScreen('diagvelo');
  showToast('✓ Diagnostic restauré');
}

function histToDevis(idx){
  const hist = histLoad();
  const h = hist[idx];
  if(!h) return;
  const clientLine = [h.client.prenom, h.client.nom].filter(Boolean).join(' ');
  const veloLine   = [h.velo.marque, h.velo.modele].filter(Boolean).join(' ');
  const nameEl = document.getElementById('devisClientName');
  if(nameEl) nameEl.textContent = (clientLine||'Client') + (veloLine?' — '+veloLine:'');

  // Pré-remplir lignes depuis les points non-OK
  devisReset(true);
  DV_POINTS.forEach(p => {
    const s = h.points[p.id];
    if(!s || s.status==='ok' || !s.status) return;
    const st = DV_STATUS[s.status];
    devisAddLine(p.name + (s.action?' — '+s.action:''), 1, 0);
  });
  devisCalc();
  showScreen('devis');
  showToast('✓ Lignes importées depuis le diagnostic');
}

// ══════════════════════════════════════════
// DEVIS RAPIDE
// ══════════════════════════════════════════
let devisLines = [];

function devisInit(){
  devisLines = [];
  devisAddLine('', 1, 0);
  devisAddLine('', 1, 0);
  devisCalc();
}

function devisAddLine(desc='', qty=1, prix=0){
  const id = Date.now() + Math.random();
  devisLines.push({ id, desc, qty, prix });
  devisRenderLines();
  devisCalc();
}

function devisRenderLines(){
  const container = document.getElementById('devisLines');
  if(!container) return;
  container.innerHTML = devisLines.map((l,i) => `
    <div class="devis-line" id="dline_${i}">
      <input class="devis-line-desc-inp" type="text" placeholder="Description prestation ou pièce…"
        value="${l.desc.replace(/"/g,'&quot;')}"
        oninput="devisLines[${i}].desc=this.value">
      <div class="devis-line-row">
        <div class="devis-num-wrap">
          <div class="devis-num-lbl">Qté</div>
          <input class="devis-num-inp" type="number" value="${l.qty}" min="0" step="1" inputmode="numeric"
            oninput="devisLines[${i}].qty=parseFloat(this.value)||0;devisCalc()">
        </div>
        <div class="devis-num-wrap">
          <div class="devis-num-lbl">Prix HT (€)</div>
          <input class="devis-num-inp" type="number" value="${l.prix||''}" min="0" step="0.01" inputmode="decimal"
            placeholder="0,00"
            oninput="devisLines[${i}].prix=parseFloat(this.value)||0;devisCalc()">
        </div>
        <div class="devis-num-wrap">
          <div class="devis-num-lbl">Total HT</div>
          <div class="devis-line-total">${((l.qty||0)*(l.prix||0)).toFixed(2)} €</div>
        </div>
        <button class="devis-del" onclick="devisDelLine(${i})">✕</button>
      </div>
    </div>`).join('');
}

function devisDelLine(i){
  devisLines.splice(i,1);
  devisRenderLines();
  devisCalc();
}

function devisCalc(){
  const tvaRate = parseFloat(document.getElementById('devisTva')?.value||'20')/100;
  const ht = devisLines.reduce((s,l)=>s+(l.qty||0)*(l.prix||0), 0);
  const tva = ht * tvaRate;
  const ttc = ht + tva;

  const linesEl = document.getElementById('devisTotalLines');
  if(linesEl){
    const rows = devisLines.filter(l=>(l.qty||0)*(l.prix||0)>0)
      .map(l=>`<div class="devis-total-line"><span>${l.desc||'—'} ×${l.qty}</span><span>${((l.qty||0)*(l.prix||0)).toFixed(2)} €</span></div>`)
      .join('');
    linesEl.innerHTML = rows + `
      <div class="devis-total-line"><span>Total HT</span><span>${ht.toFixed(2)} €</span></div>
      <div class="devis-total-line"><span>TVA ${(tvaRate*100).toFixed(0)}%</span><span>${tva.toFixed(2)} €</span></div>`;
  }
  const gtEl = document.getElementById('devisGrandTotal');
  if(gtEl) gtEl.textContent = ttc.toFixed(2).replace('.',',') + ' €';

  // Update totals in lines render
  devisRenderLines();
}

function devisFromDiag(){
  const f = dvGetFiche();
  const clientLine = [f.prenom, f.nom].filter(Boolean).join(' ');
  const veloLine   = [f.marque, f.modele].filter(Boolean).join(' ');
  const nameEl = document.getElementById('devisClientName');
  if(nameEl) nameEl.textContent = (clientLine||'Client') + (veloLine?' — '+veloLine:'');

  const filled = DV_POINTS.filter(p => dvState[p.id].status && dvState[p.id].status!=='ok');
  if(filled.length){
    devisLines = [];
    filled.forEach(p => {
      const s = dvState[p.id];
      devisLines.push({ id:Date.now()+Math.random(), desc: p.name+(s.action?' — '+s.action:''), qty:1, prix:0 });
    });
    devisRenderLines(); devisCalc();
    showToast('✓ Points importés depuis le diagnostic');
  } else {
    showToast('Aucun point à importer', true);
  }
}

function devisReset(silent=false){
  devisLines = [];
  const nameEl = document.getElementById('devisClientName');
  if(nameEl) nameEl.textContent = '— Aucun client sélectionné —';
  devisAddLine('',1,0); devisAddLine('',1,0);
  devisCalc();
  if(!silent) showToast('Devis réinitialisé');
}

function devisBuildText(){
  const tvaRate = parseFloat(document.getElementById('devisTva')?.value||'20')/100;
  const ht  = devisLines.reduce((s,l)=>s+(l.qty||0)*(l.prix||0),0);
  const tva = ht*tvaRate;
  const ttc = ht+tva;
  const clientName = document.getElementById('devisClientName')?.textContent||'';
  const date = new Date().toLocaleDateString('fr-FR');

  let txt = 'DEVIS — VéloID\n';
  txt += '════════════════════════════\n';
  txt += 'Date : '+date+'\n';
  if(clientName && clientName!=='— Aucun client sélectionné —') txt += 'Client : '+clientName+'\n';
  txt += '\n';
  devisLines.forEach(l => {
    if(!(l.qty&&l.prix)) return;
    txt += `${l.desc||'Prestation'} × ${l.qty}  →  ${((l.qty||0)*(l.prix||0)).toFixed(2)} €\n`;
  });
  txt += '\n────────────────────────────\n';
  txt += `Total HT  :  ${ht.toFixed(2)} €\n`;
  txt += `TVA ${(tvaRate*100).toFixed(0)}%   :  ${tva.toFixed(2)} €\n`;
  txt += `TOTAL TTC :  ${ttc.toFixed(2)} €\n`;
  txt += '────────────────────────────\nGénéré par VéloID';
  return txt;
}

function devisCopy(){
  const txt = devisBuildText();
  const btn = document.getElementById('devisCopyBtn');
  const done = ()=>{ btn.textContent='✓ Copié !'; setTimeout(()=>{ btn.textContent='📋 Copier'; },2200); };
  if(navigator.clipboard) navigator.clipboard.writeText(txt).then(done);
  else{ const ta=document.createElement('textarea'); ta.value=txt; document.body.appendChild(ta); ta.select(); document.execCommand('copy'); document.body.removeChild(ta); done(); }
}

function devisShare(){
  const txt = devisBuildText();
  const clientName = document.getElementById('devisClientName')?.textContent||'';
  const title = 'Devis VéloID' + (clientName&&clientName!=='— Aucun client sélectionné —'?' — '+clientName:'');
  if(navigator.share){ navigator.share({title, text:txt}).catch(()=>{}); return; }
  if(navigator.clipboard){ navigator.clipboard.writeText(txt).then(()=>showToast('✓ Devis copié !')); }
}

// ══════════════════════════════════════════
// BIBLIOTHÈQUE CLIENTS — localStorage
// ══════════════════════════════════════════
let biblioEditId = null;

function biblioLoad(){
  try { return JSON.parse(localStorage.getItem('veloid_clients')||'[]'); } catch(e){ return []; }
}

function biblioSaveAll(data){
  try { localStorage.setItem('veloid_clients', JSON.stringify(data)); } catch(e){ showToast('Erreur sauvegarde',true); }
}

function biblioRender(){
  const q    = (document.getElementById('biblioSearch')?.value||'').toLowerCase();
  const data = biblioLoad().filter(c => {
    const s = [c.prenom,c.nom,c.tel,c.mail,c.velos?.map(v=>v.marque+' '+v.modele).join(' ')].join(' ').toLowerCase();
    return !q || s.includes(q);
  });
  const el = document.getElementById('biblioList');
  if(!el) return;
  if(!data.length){
    el.innerHTML = q
      ? '<div class="biblio-empty">🔍 Aucun résultat pour « '+q+' »</div>'
      : '<div class="biblio-empty">📋 Aucun client enregistré<br>Appuyez sur <strong>Nouveau client</strong></div>';
    return;
  }
  el.innerHTML = data.map((c,i) => {
    const initials = ((c.prenom||'?')[0]+(c.nom||'?')[0]).toUpperCase();
    const veloTags = (c.velos||[]).map(v=>`<span class="biblio-velo-tag">${[v.marque,v.modele,v.annee].filter(Boolean).join(' ')}</span>`).join('');
    return `<div class="biblio-card">
      <div class="biblio-card-top">
        <div class="biblio-avatar">${initials}</div>
        <div class="biblio-info">
          <div class="biblio-name">${[c.prenom,c.nom].filter(Boolean).join(' ')||'—'}</div>
          <div class="biblio-contact">${c.tel?'📞 '+c.tel+'<br>':''} ${c.mail?'✉️ '+c.mail:''}</div>
        </div>
      </div>
      ${veloTags?`<div class="biblio-velos">${veloTags}</div>`:''}
      <div class="biblio-btns" style="margin-top:9px">
        <button class="biblio-use-btn" onclick="biblioUse(${i})">↩ Importer</button>
        <button class="biblio-diag-btn" onclick="biblioDiag(${i})">🔍 Diagnostic</button>
        <button class="biblio-del-btn" onclick="biblioDelete(${i})" title="Supprimer">🗑</button>
      </div>
    </div>`;
  }).join('');
}

function biblioOpenModal(editIdx=null){
  biblioEditId = editIdx;
  const modal = document.getElementById('biblioModal');
  if(!modal) return;
  const title = document.getElementById('biblioModalTitle');
  if(title) title.textContent = editIdx!==null ? 'Modifier client' : 'Nouveau client';

  const fields = ['bmPrenom','bmNom','bmTel','bmMail','bmMarque','bmModele','bmAnnee','bmType','bmNotes'];
  fields.forEach(id=>{ const e=document.getElementById(id); if(e) e.value=''; });

  if(editIdx!==null){
    const c = biblioLoad()[editIdx];
    if(c){
      const set=(id,v)=>{const e=document.getElementById(id);if(e)e.value=v||'';};
      set('bmPrenom',c.prenom); set('bmNom',c.nom); set('bmTel',c.tel); set('bmMail',c.mail);
      set('bmNotes',c.notes);
      if(c.velos&&c.velos[0]){
        set('bmMarque',c.velos[0].marque); set('bmModele',c.velos[0].modele);
        set('bmAnnee',c.velos[0].annee); set('bmType',c.velos[0].type);
      }
    }
  }
  modal.style.display='flex';
  setTimeout(()=>document.getElementById('bmPrenom')?.focus(), 200);
}

function biblioCloseModal(){
  const modal = document.getElementById('biblioModal');
  if(modal) modal.style.display='none';
  biblioEditId = null;
}

function biblioSave(){
  const get=(id)=>document.getElementById(id)?.value?.trim()||'';
  const prenom=get('bmPrenom'), nom=get('bmNom');
  if(!prenom&&!nom){ showToast('Entrez au moins un nom',true); return; }

  const client = {
    id: Date.now(),
    prenom, nom,
    tel: get('bmTel'), mail: get('bmMail'), notes: get('bmNotes'),
    velos: [{marque:get('bmMarque'),modele:get('bmModele'),annee:get('bmAnnee'),type:get('bmType')}].filter(v=>v.marque||v.modele)
  };

  const data = biblioLoad();
  if(biblioEditId!==null){ data[biblioEditId]={...data[biblioEditId],...client}; }
  else { data.unshift(client); }
  biblioSaveAll(data);
  biblioCloseModal();
  biblioRender();
  showToast('✓ Client enregistré !');
}

function biblioDelete(idx){
  if(!confirm('Supprimer ce client ?')) return;
  const data = biblioLoad();
  data.splice(idx,1);
  biblioSaveAll(data);
  biblioRender();
  showToast('Client supprimé');
}

function biblioUse(idx){
  // Remplir la fiche client du diagnostic
  const c = biblioLoad()[idx];
  if(!c) return;
  const set=(id,v)=>{const e=document.getElementById(id);if(e)e.value=v||'';};
  set('dvClientNom',c.nom); set('dvClientPrenom',c.prenom);
  set('dvClientTel',c.tel); set('dvClientMail',c.mail);
  if(c.velos&&c.velos[0]){
    set('dvVeloMarque',c.velos[0].marque); set('dvVeloModele',c.velos[0].modele);
    set('dvVeloAnnee',c.velos[0].annee);
    const t=document.getElementById('dvVeloType'); if(t) t.value=c.velos[0].type||'';
  }
  showToast('✓ Client importé dans le diagnostic');
}

function biblioDiag(idx){
  biblioUse(idx);
  showScreen('diagvelo');
}

// ══════════════════════════════════════════
// SAVE BUTTON dans diagnostic
// ══════════════════════════════════════════
// Ajout d'un bouton sauvegarder dans le header du diag
(function addSaveBtn(){
  document.addEventListener('DOMContentLoaded', ()=>{
    const hdr = document.querySelector('#diagvelo .diag-hdr');
    if(!hdr) return;
    const btn = document.createElement('button');
    btn.textContent = '💾';
    btn.title = 'Sauvegarder le diagnostic';
    btn.style.cssText = 'height:32px;padding:0 10px;background:rgba(34,197,94,.15);border:1px solid rgba(34,197,94,.3);border-radius:8px;color:#22c55e;font-size:16px;cursor:pointer;flex-shrink:0;margin-right:6px';
    btn.onclick = ()=>{ histSave(); };
    // Insert before export button
    const expBtn = hdr.querySelector('.dv-export-btn');
    if(expBtn) hdr.insertBefore(btn, expBtn);
    else hdr.appendChild(btn);
  });
})();

// ══════════════════════════════════════════
// PWA — Service Worker
// ══════════════════════════════════════════
if('serviceWorker' in navigator){
  window.addEventListener('load', ()=>{
    // Inline service worker as blob
    const swCode = `
      const CACHE = 'veloid-v1';
      const URLS  = [self.location.href];
      self.addEventListener('install', e => {
        e.waitUntil(caches.open(CACHE).then(c => c.addAll(URLS)));
        self.skipWaiting();
      });
      self.addEventListener('fetch', e => {
        e.respondWith(caches.match(e.request).then(r => r || fetch(e.request).catch(()=>caches.match(e.request))));
      });
    `;
    const blob = new Blob([swCode], {type:'application/javascript'});
    const url  = URL.createObjectURL(blob);
    navigator.serviceWorker.register(url).catch(()=>{});
  });
}


window.addEventListener('load',()=>{
  setTimeout(()=>{
    document.getElementById('splash').classList.remove('active');
    document.getElementById('home').classList.add('active');
  },1600);
});
</script>

<!-- ══ HISTORIQUE DES DIAGNOSTICS ══ -->
<div class="screen" id="historique">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Historique</div>
      <div class="dh-step">Diagnostics sauvegardés localement</div>
    </div>
  </div>
  <div class="hist-scroll" id="histScroll">
    <div class="hist-top-bar">
      <div class="hist-count" id="histCount">0 diagnostic(s)</div>
      <button class="hist-clear-btn" onclick="histClearAll()">Tout effacer</button>
    </div>
    <div id="histList"></div>
  </div>
</div>

<!-- ══ DEVIS RAPIDE ══ -->
<div class="screen" id="devis">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Devis Rapide</div>
      <div class="dh-step">Prestations + pièces · total TTC</div>
    </div>
  </div>
  <div class="devis-scroll" id="devisScroll">

    <!-- Client bar -->
    <div class="devis-client-bar">
      <div style="flex:1">
        <div class="devis-client-txt">Client</div>
        <div class="devis-client-name" id="devisClientName">— Aucun client sélectionné —</div>
      </div>
      <button class="devis-from-diag-btn" onclick="devisFromDiag()">↙ Importer diag.</button>
    </div>

    <!-- Lignes de prestation -->
    <div class="devis-section-lbl">Prestations &amp; pièces</div>
    <div class="devis-lines" id="devisLines"></div>
    <button class="devis-add-btn" onclick="devisAddLine()">+ Ajouter une ligne</button>

    <!-- TVA -->
    <div class="devis-tva-row">
      <span class="devis-tva-lbl">TVA</span>
      <select class="devis-tva-sel" id="devisTva" onchange="devisCalc()">
        <option value="0">Sans TVA (HT)</option>
        <option value="5.5">5,5%</option>
        <option value="10">10%</option>
        <option value="20" selected>20%</option>
      </select>
    </div>

    <!-- Total -->
    <div class="devis-total-card" id="devisTotalCard">
      <div class="devis-section-lbl" style="margin-bottom:10px">Récapitulatif</div>
      <div id="devisTotalLines"></div>
      <div class="devis-grand">
        <div class="devis-grand-lbl">TOTAL TTC</div>
        <div class="devis-grand-val" id="devisGrandTotal">0,00 €</div>
      </div>
    </div>

    <!-- Actions -->
    <div class="devis-btns">
      <button class="devis-copy-btn" onclick="devisCopy()" id="devisCopyBtn">📋 Copier</button>
      <button class="devis-share-btn" onclick="devisShare()">↗ Partager</button>
      <button class="devis-reset-btn" onclick="devisReset()">↺</button>
    </div>

    <div style="height:20px"></div>
  </div>
</div>

<!-- ══ BIBLIOTHÈQUE CLIENTS ══ -->
<div class="screen" id="biblio">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Clients &amp; Vélos</div>
      <div class="dh-step">Bibliothèque · fiches réutilisables</div>
    </div>
  </div>
  <div class="biblio-scroll">
    <div class="biblio-searchbar">
      <svg width="16" height="16" fill="none" viewBox="0 0 24 24"><circle cx="11" cy="11" r="7" stroke="#5a5550" stroke-width="2"/><path d="m16 16 4 4" stroke="#5a5550" stroke-width="2" stroke-linecap="round"/></svg>
      <input type="search" id="biblioSearch" placeholder="Chercher un client…" oninput="biblioRender()">
    </div>
    <button class="biblio-new-btn" onclick="biblioOpenModal()">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><line x1="12" y1="5" x2="12" y2="19" stroke="#fff" stroke-width="2.5" stroke-linecap="round"/><line x1="5" y1="12" x2="19" y2="12" stroke="#fff" stroke-width="2.5" stroke-linecap="round"/></svg>
      Nouveau client
    </button>
    <div id="biblioList"></div>
  </div>
</div>

<!-- Modale ajout/edit client -->
<div class="velo-modal-overlay" id="biblioModal" style="display:none" onclick="biblioCloseModal()">
  <div class="velo-modal-sheet" onclick="event.stopPropagation()">
    <div class="velo-modal-handle"></div>
    <div class="velo-modal-title" id="biblioModalTitle">Nouveau client</div>
    <div class="velo-modal-body">
      <div class="velo-modal-row">
        <div class="velo-modal-field">
          <div class="velo-modal-lbl">Prénom</div>
          <input class="velo-modal-inp" type="text" id="bmPrenom" placeholder="Jean" autocomplete="given-name">
        </div>
        <div class="velo-modal-field">
          <div class="velo-modal-lbl">Nom</div>
          <input class="velo-modal-inp" type="text" id="bmNom" placeholder="Dupont" autocomplete="family-name">
        </div>
      </div>
      <div class="velo-modal-field">
        <div class="velo-modal-lbl">Téléphone</div>
        <input class="velo-modal-inp" type="tel" id="bmTel" placeholder="06 12 34 56 78" autocomplete="tel">
      </div>
      <div class="velo-modal-field">
        <div class="velo-modal-lbl">Email</div>
        <input class="velo-modal-inp" type="email" id="bmMail" placeholder="jean@mail.com" autocomplete="email">
      </div>
      <div class="velo-modal-row">
        <div class="velo-modal-field">
          <div class="velo-modal-lbl">Marque vélo</div>
          <input class="velo-modal-inp" type="text" id="bmMarque" placeholder="Trek, Specialized…">
        </div>
        <div class="velo-modal-field">
          <div class="velo-modal-lbl">Modèle</div>
          <input class="velo-modal-inp" type="text" id="bmModele" placeholder="Domane, Tarmac…">
        </div>
      </div>
      <div class="velo-modal-row">
        <div class="velo-modal-field">
          <div class="velo-modal-lbl">Année</div>
          <input class="velo-modal-inp" type="number" id="bmAnnee" placeholder="2023" inputmode="numeric">
        </div>
        <div class="velo-modal-field">
          <div class="velo-modal-lbl">Type</div>
          <select class="velo-modal-inp" id="bmType">
            <option value="">—</option>
            <option>Route</option><option>Gravel</option><option>VTT XC</option>
            <option>VTT Trail</option><option>VTT Enduro</option><option>E-Bike</option>
            <option>Ville</option><option>BMX</option><option>Autre</option>
          </select>
        </div>
      </div>
      <div class="velo-modal-field">
        <div class="velo-modal-lbl">Notes</div>
        <input class="velo-modal-inp" type="text" id="bmNotes" placeholder="Taille cadre, particularités…">
      </div>
    </div>
    <button class="velo-modal-save" onclick="biblioSave()">💾 Enregistrer</button>
    <button class="velo-modal-cancel" onclick="biblioCloseModal()">Annuler</button>
  </div>
</div>

<!-- Toast global -->
<div class="velo-toast" id="veloToast"></div>

<!-- ══ COMPATIBILITÉ CHAÎNE/CASSETTE ══ -->
<div class="screen" id="compat">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Compatibilité Chaîne/Cassette</div>
      <div class="dh-step">Références · alertes croisées · largeurs</div>
    </div>
  </div>
  <div class="compat-scroll">
    <div style="background:rgba(34,197,94,.08);border:1px solid rgba(34,197,94,.2);border-radius:12px;padding:11px 14px;margin-bottom:14px;font-size:12px;color:#22c55e;line-height:1.6">
      🔗 Sélectionnez la <strong>marque</strong>, le nombre de <strong>vitesses</strong> pour obtenir les références chaînes compatibles et les alertes croisées.
    </div>
    <div class="compat-section">
      <div class="compat-section-hdr">
        <span class="compat-section-hdr-title">Configuration</span>
      </div>
      <div class="compat-section-body">
        <div class="compat-field-lbl">Fabricant</div>
        <div class="compat-chip-row" id="cmp_marq">
          <div class="compat-chip" onclick="cmpPick('marq','shimano',this)">Shimano</div>
          <div class="compat-chip" onclick="cmpPick('marq','sram',this)">SRAM</div>
          <div class="compat-chip" onclick="cmpPick('marq','campa',this)">Campagnolo</div>
          <div class="compat-chip" onclick="cmpPick('marq','kmc',this)">KMC</div>
          <div class="compat-chip" onclick="cmpPick('marq','connex',this)">Connex</div>
        </div>
        <div class="compat-field-lbl">Vitesses</div>
        <div class="compat-chip-row" id="cmp_vit">
          <div class="compat-chip" onclick="cmpPick('vit','678',this)">6/7/8v</div>
          <div class="compat-chip" onclick="cmpPick('vit','9',this)">9v</div>
          <div class="compat-chip" onclick="cmpPick('vit','10',this)">10v</div>
          <div class="compat-chip" onclick="cmpPick('vit','11',this)">11v</div>
          <div class="compat-chip" onclick="cmpPick('vit','12',this)">12v</div>
          <div class="compat-chip" onclick="cmpPick('vit','13',this)">13v</div>
        </div>
        <button class="compat-calc-btn" onclick="cmpCompute()">🔍 Trouver les compatibilités</button>
      </div>
    </div>
    <div class="compat-result" id="cmpResult">
      <div class="compat-result-title">✓ Chaînes compatibles</div>
      <div class="compat-chain-list" id="cmpChainList"></div>
      <div style="font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);text-transform:uppercase;letter-spacing:.1em;margin:12px 0 7px">Alertes &amp; compatibilités cassettes</div>
      <div class="compat-alert-list" id="cmpAlerts"></div>
      <div style="font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);text-transform:uppercase;letter-spacing:.1em;margin:12px 0 7px">Largeur de chaîne &amp; cassettes</div>
      <div class="compat-chain-list" id="cmpLargeur"></div>
    </div>
    <div style="height:20px"></div>
  </div>
</div>

<!-- ══ GLOSSAIRE ══ -->
<div class="screen" id="glossaire">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Glossaire Technique</div>
      <div class="dh-step">Lexique atelier · FR/EN · 50+ termes</div>
    </div>
  </div>
  <div style="display:flex;flex-direction:column;height:calc(100% - 54px);overflow:hidden">
    <div class="glos-search" style="flex-shrink:0">
      <svg width="16" height="16" fill="none" viewBox="0 0 24 24"><circle cx="11" cy="11" r="7" stroke="#5a5550" stroke-width="2"/><path d="m16 16 4 4" stroke="#5a5550" stroke-width="2" stroke-linecap="round"/></svg>
      <input type="search" id="glosSearch" placeholder="Chercher un terme…" oninput="glosFilter()">
    </div>
    <div class="glos-letter-bar" id="glosLetterBar" style="flex-shrink:0"></div>
    <div class="glos-scroll" id="glosContent" style="flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch"></div>
  </div>
</div>

<!-- ══ SETTINGS ══ -->
<div class="screen" id="settings">
  <div class="diag-hdr">
    <div class="btn-back" onclick="goHome()">‹</div>
    <div class="dh-info">
      <div class="dh-title">Paramètres</div>
      <div class="dh-step">Préférences · langue · thème</div>
    </div>
  </div>
  <div class="settings-scroll">
    <div class="settings-section-title">Apparence</div>
    <div class="settings-group">
      <div class="settings-row">
        <div class="settings-lbl"><strong>Mode sombre / clair</strong><span style="font-size:12px;color:var(--muted);display:block;margin-top:1px">Thème de l'interface</span></div>
        <button class="theme-toggle" id="themeToggle" onclick="toggleTheme()"></button>
      </div>
    </div>
    <div class="settings-section-title">Langue · Language</div>
    <div class="settings-group">
      <div class="settings-row">
        <div class="settings-lbl"><strong>Français / English</strong><span style="font-size:12px;color:var(--muted);display:block;margin-top:1px">Langue de l'interface</span></div>
        <div class="lang-toggle">
          <button class="lang-btn" id="settingsLangFR" onclick="setLang('fr')">FR</button>
          <button class="lang-btn" id="settingsLangEN" onclick="setLang('en')">EN</button>
        </div>
      </div>
    </div>
    <div class="settings-section-title">À propos</div>
    <div class="settings-group">
      <div class="settings-about">
        <div class="settings-app-name">Vélo<em>ID</em></div>
        <div class="settings-version">v4.1 · Mai 2026</div>
        <div style="font-size:12px;color:var(--muted);margin-top:8px;line-height:1.8">
          Diagnostic · Mesure · Rayonnage<br>
          Braquet · Couples · Compatibilité<br>
          Glossaire · Clients · Devis
        </div>
      </div>
    </div>
    <div style="height:30px"></div>
  </div>
</div>

<script>
// ══════════════════════════════════════════
// LANG & THEME — définis dans le <head> (null-safe)
// Les fonctions setLang, applyTheme, toggleTheme sont déclarées en tête de document.
// Ce bloc ne redéclare que les constantes de traduction avancées si besoin.
// ══════════════════════════════════════════
const I18N = {};  // réservé pour extensions futures


// ══════════════════════════════════════════
// TENSION RAYONS — calcul complémentaire
// ══════════════════════════════════════════
function calcTensionRayons(lenG, lenD, dg, dd, pcdG, pcdD, old) {
  const angG = Math.atan(dg / (pcdG / 2));
  const angD = Math.atan(dd / (pcdD / 2));
  const tensRatio = (Math.sin(angD) * lenG) / (Math.sin(angG) * lenD);

  let usageKey = 'Standard', plage = {min:80,max:110};
  if(old<=110){usageKey='Route / Gravel';plage={min:80,max:120};}
  else if(old===148){usageKey='VTT Boost';plage={min:70,max:110};}
  else if(old===142){usageKey='VTT XC';plage={min:80,max:120};}
  else if(old>=157){usageKey='DH / E-Bike';plage={min:100,max:140};}

  const kgfG = Math.round(plage.min + ((angG/(Math.PI/3)) * (plage.max-plage.min) * 0.9));
  const kgfD = Math.round(kgfG * Math.min(tensRatio, 1.0));

  const tensCard = document.getElementById('rayTensionCard');
  const tensGrid = document.getElementById('tensionGrid');
  const tensRatioEl = document.getElementById('tensionRatio');
  if(!tensCard) return;

  const colG = kgfG>=plage.min&&kgfG<=plage.max?'#22c55e':kgfG<plage.min?'#f59e0b':'#ef4444';
  const colD = kgfD>=plage.min&&kgfD<=plage.max?'#22c55e':kgfD<plage.min?'#f59e0b':'#ef4444';
  const pctG = Math.min(100,Math.max(10,((kgfG-plage.min)/(plage.max-plage.min+30))*100));
  const pctD = Math.min(100,Math.max(10,((kgfD-plage.min)/(plage.max-plage.min+30))*100));

  tensGrid.innerHTML = `
    <div class="tension-side" style="border-left:3px solid #e8400c">
      <div class="tension-side-lbl">◀ Côté Gauche</div>
      <div class="tension-nm" style="color:${colG}">${kgfG}<span class="tension-nm-unit"> kgf</span></div>
      <div class="tension-plage">Cible : ${plage.min}–${plage.max} kgf</div>
      <div class="tension-gauge-wrap" style="margin-top:7px"><div class="tension-gauge-bar"><div class="tension-gauge-fill" style="width:${pctG}%;background:${colG}"></div></div></div>
    </div>
    <div class="tension-side" style="border-left:3px solid #2d7ff9">
      <div class="tension-side-lbl">Côté Droit ▶</div>
      <div class="tension-nm" style="color:${colD}">${kgfD}<span class="tension-nm-unit"> kgf</span></div>
      <div class="tension-plage">Cible : ${plage.min}–${plage.max} kgf</div>
      <div class="tension-gauge-wrap" style="margin-top:7px"><div class="tension-gauge-bar"><div class="tension-gauge-fill" style="width:${pctD}%;background:${colD}"></div></div></div>
    </div>`;

  const ratioOk = tensRatio>=0.6&&tensRatio<=1.4;
  tensRatioEl.innerHTML = `
    <div class="ray-dg-item" style="margin:8px 0">
      <div class="ray-dg-label">Usage · ${usageKey}</div>
      <div class="ray-dg-val" style="color:${ratioOk?'var(--green)':'var(--gold)'}">Ratio D/G : ${tensRatio.toFixed(2)}× ${ratioOk?'✓':'⚠️ Déséquilibré'}</div>
    </div>`;

  tensCard.classList.add('show');
}

// ══════════════════════════════════════════
// COMPATIBILITÉ CHAÎNE/CASSETTE
// ══════════════════════════════════════════
const CMP_DATA = {
  shimano:{
    '678':{chains:[{ref:'CN-HG71 / CN-HG40',badge:'shimano',note:'6/7/8v — universel toutes marques'},{ref:'KMC Z8',badge:'kmc',note:'Compatible Shimano 8v HG'}],largeur:'7,1–7,3 mm ext.',cassettes:'Shimano HG · SRAM PG · Microshift · SunRace 6/7/8v',alerts:[],ok:['Compatible SRAM 6/7/8v sans adaptateur','Chaîne universelle toutes marques']},
    '9':{chains:[{ref:'CN-HG53 / CN-HG73',badge:'shimano',note:'9v MTB/route'},{ref:'KMC X9',badge:'kmc',note:'Compatible Shimano/SRAM 9v'}],largeur:'6,5–6,8 mm ext.',cassettes:'Shimano HG · SRAM PG · Microshift 9v',alerts:[],ok:['Compatible SRAM 9v PG']},
    '10':{chains:[{ref:'CN-HG54 / CN-HG95',badge:'shimano',note:'10v MTB'},{ref:'CN-6701 / CN-5701',badge:'shimano',note:'10v Route'},{ref:'KMC X10',badge:'kmc',note:'10v universel'}],largeur:'6,1–6,3 mm ext.',cassettes:'Shimano HG · SRAM PG 10v',alerts:['⚠️ Campagnolo 10v incompatible — écartement différent'],ok:['Compatible SRAM 10v']},
    '11':{chains:[{ref:'CN-HG601 / CN-HG701',badge:'shimano',note:'11v MTB'},{ref:'CN-HG601-1 / CN-HG901',badge:'shimano',note:'11v Route'},{ref:'KMC X11',badge:'kmc',note:'11v universel'},{ref:'Connex 11s0',badge:'connex',note:'11v maillon breveté'}],largeur:'5,5–5,65 mm ext.',cassettes:'Shimano HG · SRAM PG/XG 11v',alerts:['⚠️ Campagnolo 11v légèrement différent'],ok:['Compatible SRAM 11v PG/XG','Corps freehub HG (11t mini)']},
    '12':{chains:[{ref:'CN-M8100 / CN-M7100',badge:'shimano',note:'12v VTT'},{ref:'CN-R9200 / CN-R8100',badge:'shimano',note:'12v Route Dura-Ace/Ultegra'},{ref:'KMC e12 Turbo',badge:'kmc',note:'Compatible Shimano 12v'}],largeur:'5,2–5,3 mm ext.',cassettes:'Shimano 12v MTB (Micro Spline) · Shimano 12v Route (HG+)',alerts:['🚫 INCOMPATIBLE SRAM Eagle','🚫 Micro Spline (MTB) ≠ HG Route','⚠️ Cassettes MTB/route Shimano 12v non interchangeables'],ok:['KMC e12 compatible Shimano 12v']}
  },
  sram:{
    '678':{chains:[{ref:'PC-830',badge:'sram',note:'8v SRAM'},{ref:'KMC Z8',badge:'kmc',note:'Compatible SRAM 8v'}],largeur:'7,1–7,3 mm ext.',cassettes:'SRAM PG · Shimano HG',alerts:[],ok:['Compatible Shimano 6/7/8v','Universel']},
    '9':{chains:[{ref:'PC-951',badge:'sram',note:'9v SRAM'},{ref:'KMC X9',badge:'kmc',note:'Compatible SRAM 9v'}],largeur:'6,5–6,8 mm ext.',cassettes:'SRAM PG · Shimano HG 9v',alerts:[],ok:['Compatible Shimano 9v']},
    '10':{chains:[{ref:'PC-1031 / PC-1051',badge:'sram',note:'10v SRAM'},{ref:'KMC X10',badge:'kmc',note:'10v universel'}],largeur:'6,1–6,3 mm ext.',cassettes:'SRAM PG · Shimano HG 10v',alerts:[],ok:['Compatible Shimano 10v']},
    '11':{chains:[{ref:'PC-1110 / PC-1130 / PC-1170',badge:'sram',note:'11v SRAM'},{ref:'KMC X11',badge:'kmc',note:'11v universel'}],largeur:'5,5–5,65 mm ext.',cassettes:'SRAM PG-1130 / XG-1150 · Shimano HG 11v',alerts:['⚠️ XG-1150 → freehub XD requis (10t min)'],ok:['Compatible Shimano 11v sur freehub HG (PG-1130)']},
    '12':{chains:[{ref:'Eagle PC-1210 / PC-1230',badge:'sram',note:'12v Eagle MTB'},{ref:'PC-1270 Red AXS',badge:'sram',note:'12v Route'},{ref:'KMC e12',badge:'kmc',note:'Compatible Eagle'}],largeur:'5,2–5,3 mm ext.',cassettes:'SRAM Eagle XG-1275/1295 (XD) · Route XG-1290 (XDR)',alerts:['🚫 INCOMPATIBLE Shimano 12v','🚫 Eagle MTB : freehub XD obligatoire','🚫 Route AXS : freehub XDR ≠ XD MTB'],ok:['Cassette Eagle jusqu\'à 10t mini']},
    '13':{chains:[{ref:'Flattop PC-1310',badge:'sram',note:'13v route AXS Force/Red'}],largeur:'4,9–5,0 mm ext.',cassettes:'SRAM XPLR XG-1350 (XDR)',alerts:['🚫 Standard propriétaire — chaîne Flattop uniquement','🚫 Freehub XDR exclusivement','🚫 Incompatible toutes autres marques'],ok:[]}
  },
  campa:{
    '11':{chains:[{ref:'Campagnolo CN-11',badge:'campa',note:'11v route'},{ref:'KMC X11 (vérif.)',badge:'kmc',note:'Partiellement compatible'}],largeur:'5,5–5,6 mm ext.',cassettes:'Campagnolo 11v uniquement',alerts:['⚠️ Shimano/SRAM 11v non certifié Campagnolo'],ok:[]},
    '12':{chains:[{ref:'Campagnolo CN-12X',badge:'campa',note:'12v Super Record/Record'}],largeur:'5,2–5,3 mm ext.',cassettes:'Campagnolo N3W 9–13v',alerts:['🚫 Corps N3W propriétaire — incompatible HG/XD/XDR'],ok:[]},
    '13':{chains:[{ref:'Campagnolo CN-EK Ekar',badge:'campa',note:'13v gravel'}],largeur:'≈5,0 mm ext.',cassettes:'Campagnolo Ekar 13v (N3W)',alerts:['🚫 Standard exclusif — aucune alternative certifiée'],ok:[]}
  },
  kmc:{
    '678':{chains:[{ref:'KMC Z7 / Z8',badge:'kmc',note:'6/7/8v universel'}],largeur:'7,1–7,3 mm',cassettes:'Toutes marques',alerts:[],ok:['Universel HG/PG']},
    '9':{chains:[{ref:'KMC X9 / X9SL',badge:'kmc',note:'9v haute perf.'}],largeur:'6,5–6,8 mm',cassettes:'Shimano HG · SRAM PG',alerts:[],ok:['Compatible Shimano et SRAM 9v']},
    '10':{chains:[{ref:'KMC X10 / DLC10',badge:'kmc',note:'10v'}],largeur:'6,1–6,3 mm',cassettes:'Shimano HG · SRAM PG',alerts:[],ok:['Compatible Shimano et SRAM 10v']},
    '11':{chains:[{ref:'KMC X11 / X11SL / DLC11',badge:'kmc',note:'11v haute perf.'}],largeur:'5,5–5,65 mm',cassettes:'Shimano HG · SRAM PG/XG',alerts:['⚠️ Campagnolo : vérifier certification'],ok:['Compatible Shimano et SRAM 11v']},
    '12':{chains:[{ref:'KMC e12 / DLC12',badge:'kmc',note:'vérifier version Shimano vs Eagle'}],largeur:'5,2–5,3 mm',cassettes:'Shimano 12v OU Eagle — selon version',alerts:['⚠️ e12 Shimano ≠ e12 Eagle — vérifier la version'],ok:[]}
  },
  connex:{
    '9':{chains:[{ref:'Connex 9s0',badge:'connex',note:'9v maillon breveté'}],largeur:'6,5 mm',cassettes:'Shimano HG · SRAM PG',alerts:[],ok:['Maillon rapide sans outil']},
    '10':{chains:[{ref:'Connex 10s0',badge:'connex',note:'10v'}],largeur:'6,1 mm',cassettes:'Shimano/SRAM 10v',alerts:[],ok:['Maillon breveté réutilisable']},
    '11':{chains:[{ref:'Connex 11s0 / 11sE',badge:'connex',note:'11v'}],largeur:'5,5 mm',cassettes:'Shimano HG · SRAM PG',alerts:[],ok:['Maillon rapide breveté sans outil']},
    '12':{chains:[{ref:'Connex 12s0',badge:'connex',note:'12v'}],largeur:'5,2 mm',cassettes:'Vérifier selon groupe',alerts:['⚠️ Vérifier compatibilité selon cassette'],ok:[]}
  }
};

let cmpState = {marq:null,vit:null};

function cmpPick(field,val,el){
  cmpState[field]=val;
  el.closest('.compat-chip-row').querySelectorAll('.compat-chip').forEach(c=>c.classList.remove('sel'));
  el.classList.add('sel');
}

function cmpCompute(){
  const {marq,vit}=cmpState;
  if(!marq||!vit){showToast('Sélectionnez fabricant + vitesses',true);return;}
  const brand=CMP_DATA[marq];
  const data=brand&&brand[vit];
  const resEl=document.getElementById('cmpResult');
  resEl.classList.add('show');
  if(!data){
    document.getElementById('cmpChainList').innerHTML='<div class="compat-alert">❌ Combinaison non répertoriée</div>';
    document.getElementById('cmpAlerts').innerHTML='';
    document.getElementById('cmpLargeur').innerHTML='';
    return;
  }
  document.getElementById('cmpChainList').innerHTML=data.chains.map(c=>`
    <div class="compat-chain-item">
      <span class="cc-badge ${c.badge}">${c.badge.toUpperCase()}</span>
      <div><div style="font-weight:600;color:var(--text)">${c.ref}</div><div style="font-size:11px;color:var(--text2);margin-top:2px">${c.note}</div></div>
    </div>`).join('');
  document.getElementById('cmpAlerts').innerHTML=
    data.alerts.map(a=>`<div class="compat-alert">${a}</div>`).join('')+
    data.ok.map(o=>`<div class="compat-ok">✓ ${o}</div>`).join('');
  document.getElementById('cmpLargeur').innerHTML=`
    <div class="compat-chain-item"><span class="cc-badge shimano">LARGEUR</span><div style="font-weight:600;color:var(--text)">${data.largeur}</div></div>
    <div class="compat-chain-item"><span class="cc-badge kmc">CASSETTES</span><div style="font-size:13px;color:var(--text2)">${data.cassettes}</div></div>`;
  resEl.scrollIntoView({behavior:'smooth',block:'nearest'});
}

// ══════════════════════════════════════════
// GLOSSAIRE TECHNIQUE
// ══════════════════════════════════════════
const GLOSSARY=[
  {term:'Axe traversant',en:'Thru-axle',tag:'std',def:'Axe plein fileté (M12 ou M15) vissé dans les pattes. Plus rigide que QR. <code>12×100 av · 12×142 · 12×148 Boost ar</code>.<span class="see-also">→ voir : OLD, Boost</span>'},
  {term:'Araignée',en:'Spider',tag:'fr',def:'Corps du pédalier portant les plateaux. 4 ou 5 bras. Le <strong>BCD</strong> se mesure sur l\'araignée. Sans araignée = Direct Mount.'},
  {term:'BCD',en:'Bolt Circle Diameter',tag:'std',def:'Diamètre du cercle de perçage des plateaux. <code>104mm</code> VTT · <code>110mm</code> route compact · <code>130mm</code> route std. Mesurer centre-à-centre entre 2 vis.'},
  {term:'Boîtier pédalier',en:'Bottom Bracket (BB)',tag:'std',def:'Roulements logés dans le tube de pédalier. Standards : <strong>BSA</strong> fileté · <strong>PF30</strong> · <strong>BB86</strong> · <strong>T47</strong>. Ø intérieur + largeur définissent le standard.'},
  {term:'Boost',en:'Boost spacing',tag:'std',def:'Standard d\'espacement moyeu Trek 2015. Avant <code>12×110mm</code> · Arrière <code>12×148mm</code>. Améliore l\'angle des rayons et la rigidité latérale.'},
  {term:'BSD',en:'Bead Seat Diameter',tag:'std',def:'Diamètre siège de tringle ETRTO. <code>622</code>=700C/29" · <code>584</code>=27,5" · <code>559</code>=26" · <code>406</code>=20". Indépendant de la largeur du pneu.'},
  {term:'BSA',en:'British Standard Threaded BB',tag:'std',def:'Boîtier pédalier fileté anglais. <code>34,8mm · 68mm · 1,37"×24tpi</code>. Côté droit fileté à gauche. Standard le plus répandu.'},
  {term:'Cassette',en:'Cassette',tag:'fr',def:'Ensemble de pignons montés sur le corps de freehub. 6 à 13 vitesses selon marque. Nécessite un corps freehub compatible.'},
  {term:'Center Lock',en:'Center Lock',tag:'std',def:'Fixation rotor Shimano par cannelures centrales + bague <code>40 Nm</code>. Adaptatear 6T→CL disponibles.'},
  {term:'Cintre',en:'Handlebar',tag:'fr',def:'Guidon. Ø central de serrage : <code>22,2 · 25,4 · 31,8 · 35mm</code>. Doit correspondre au collier de la potence.'},
  {term:'Développement',en:'Gear development',tag:'fr',def:'Distance parcourue par tour de pédale (mètres). Formule : <code>(plateau÷pignon) × circonférence roue</code>.'},
  {term:'Direct Mount',en:'Direct Mount chainring',tag:'std',def:'Plateau vissé directement sur le bras de manivelle, sans araignée. Propriétaire par marque : SRAM DM ≠ Shimano HT2.'},
  {term:'Dropper post',en:'Dropper seatpost',tag:'std',def:'Tige de selle télescopique télécommandée. Ø : <code>27,2 · 30,9 · 31,6 · 34,9mm</code>. Commande par câble ou électronique.'},
  {term:'ERD',en:'Effective Rim Diameter',tag:'std',def:'Diamètre efficace de jante — là où les têtes de rayons se posent. Toujours < BSD (de 10–20mm). Indispensable pour calculer la longueur des rayons.'},
  {term:'ETRTO',en:'ETRTO tire code',tag:'std',def:'Code international pneu/jante : <code>largeur-BSD</code> (ex <code>37-622</code>). Élimine les confusions pouces. 700C et 29" = même BSD 622.'},
  {term:'Flat Mount',en:'Flat Mount',tag:'std',def:'Fixation d\'étrier frein plat par dessous. Standard route/gravel depuis 2015. Vis M5 · <code>6–8 Nm</code>.'},
  {term:'Freehub',en:'Freehub body',tag:'std',def:'Corps de roue libre sur le moyeu. Standards : <strong>HG</strong> (Shimano ≤11v) · <strong>Micro Spline</strong> (Shimano 12v MTB) · <strong>XD</strong> (SRAM Eagle) · <strong>XDR</strong> (SRAM route) · <strong>N3W</strong> (Campagnolo).'},
  {term:'Graisse cuivrée',en:'Copper anti-seize',tag:'fr',def:'Anti-grippage cuivre. Zones inox, titane, très exposées. Réduit le couple effectif de 10–20%.'},
  {term:'HG / Hyperglide',en:'HyperGlide',tag:'std',def:'Standard cassette/chaîne Shimano ≤11v. Corps freehub à splines. Compatible SRAM ≤11v. Abandonné en 12v MTB (→ Micro Spline).'},
  {term:'IS / International Standard',en:'IS brake mount',tag:'std',def:'Fixation étrier frein à vis horizontales. Standard VTT original. Adaptateurs obligatoires pour changer taille rotor.'},
  {term:'Jeu de direction',en:'Headset',tag:'fr',def:'Roulements cadre↔fourche. Code SHIS : <code>TYPE+Ø_haut/TYPE+Ø_bas</code>. Types : EC · ZS · IS. Haut et bas peuvent être différents.'},
  {term:'Lockring',en:'Lockring',tag:'std',def:'Bague de verrouillage cassette sur freehub. <code>40 Nm</code> outil cassette + maintien.'},
  {term:'Micro Spline',en:'Micro Spline',tag:'std',def:'Corps freehub Shimano 12v MTB. Incompatible HG. Pignon mini 10t. Introduit avec XT M8100.'},
  {term:'Moyeu',en:'Hub',tag:'fr',def:'Centre de la roue, roulements et freehub. Paramètres : OLD (largeur), PCD des flancs, nombre de trous.'},
  {term:'N3W',en:'N3W (Campagnolo)',tag:'std',def:'Corps freehub Campagnolo nouvelle génération. Cassettes 9–13v. Incompatible HG/XD/XDR.'},
  {term:'OLD',en:'Over Locknut Dimension',tag:'std',def:'Largeur totale moyeu face ext. écrou → face ext. écrou. Doit correspondre à l\'espacement pattes cadre. <code>100·110·130·142·148·157mm</code>.'},
  {term:'OSB',en:'Rim offset',tag:'std',def:'Décalage du lit de rayon par rapport au plan médian de jante. 0 = symétrique. Positif = jante asymétrique (compense la cassette).'},
  {term:'PCD',en:'Pitch Circle Diameter',tag:'std',def:'Diamètre du cercle de perçage des flancs de moyeu. Mesuré centre-à-centre entre 2 trous opposés. Utilisé dans le calcul de longueur de rayons.'},
  {term:'PF30',en:'PF30 Press-Fit BB',tag:'std',def:'Boîtier emboîté. <code>Ø46mm · 68mm large</code>. Axe 30mm. Sujet aux craquements → insert T47 recommandé.'},
  {term:'Pâte carbone',en:'Carbon friction paste',tag:'fr',def:'Pâte abrasive fine pour fixations carbone. Augmente l\'adhérence → couple réduit de 30–40%. Indispensable sur carbone.'},
  {term:'Post Mount',en:'Post Mount',tag:'std',def:'Fixation étrier frein à 2 pattes verticales. Le plus courant VTT. Rotor natif 160mm, adaptateurs pour autres tailles.'},
  {term:'QR',en:'Quick Release',tag:'std',def:'Serrage rapide excentrique. Axe creux 9mm (av) ou 10mm (ar). OLD : <code>100mm</code> av · <code>130/135mm</code> ar.'},
  {term:'Rayon',en:'Spoke',tag:'fr',def:'Fil de tension moyeu→jante. Longueur dépend de ERD, PCD, distances flancs, croisements. Matériaux : inox butted · alu · carbone.'},
  {term:'Rotor',en:'Brake rotor',tag:'fr',def:'Disque de frein acier inox. <code>140·160·180·200·203mm</code>. Fixation 6 trous Torx T25 ou Center Lock.'},
  {term:'SHIS',en:'Standardized Headset ID System',tag:'std',def:'Codification jeux de direction. Format : <code>TYPE+Ø | Ø_colonne</code>. Ex : <code>ZS44/28.6 — EC34/28.6</code>.'},
  {term:'T47',en:'T47 Bottom Bracket',tag:'std',def:'Boîtier fileté nouvelle génération. <code>Ø47mm fileté M47×1mm</code>. Combine grand Ø PF + fiabilité BSA fileté.'},
  {term:'Tapered',en:'Tapered fork/headtube',tag:'std',def:'Colonne conique <code>Ø28,6mm</code> haut → <code>Ø38,1mm</code> bas. Plus rigide sans alourdir la potence. JDD tapered spécifique requis.'},
  {term:'Tension rayon',en:'Spoke tension',tag:'fr',def:'Force de traction d\'un rayon. Cible : <code>80–120 kgf</code> route/gravel · <code>70–110 kgf</code> VTT. Mesure : tensiomètre Park Tool TM-1.'},
  {term:'Tubeless',en:'Tubeless',tag:'std',def:'Montage sans chambre à air. Requis : jante TLR étanche, pneu tubeless, valve étanche, liquide préventif. Pression réduite, anti-crevaison.'},
  {term:'Vitesse / Braquet',en:'Gear ratio',tag:'fr',def:'Braquet = plateau÷pignon. Développement (m/tour) = braquet × circonférence. Plus grand = plus rapide mais plus dur.'},
  {term:'XD Driver',en:'XD Driver',tag:'std',def:'Corps freehub SRAM Eagle 12v MTB. Filetage ext. → 10t mini. Incompatible HG/XDR/Micro Spline.'},
  {term:'XDR',en:'XDR Freehub',tag:'std',def:'Version route du XD SRAM (2mm plus long). Cassettes SRAM eTap AXS 12v. Incompatible XD/HG/Micro Spline.'},
  {term:'ZS / Zero Stack',en:'Zero Stack Headset',tag:'std',def:'JDD à coupelle dans un lamage — affleure le tube. Le plus courant : <code>ZS44/28.6</code>. Aspect intégré sans l\'être.'},
];

let glosFiltered=[...GLOSSARY];

function glosInit(){
  const bar=document.getElementById('glosLetterBar');
  if(!bar) return;
  const letters=[...new Set(GLOSSARY.map(e=>e.term[0].toUpperCase()))].sort();
  bar.innerHTML=letters.map(l=>`<button class="glos-letter-btn" onclick="glosJump('${l}')">${l}</button>`).join('');
  glosRender(GLOSSARY);
}

function glosFilter(){
  const q=(document.getElementById('glosSearch')?.value||'').toLowerCase();
  glosFiltered=q?GLOSSARY.filter(e=>e.term.toLowerCase().includes(q)||e.en.toLowerCase().includes(q)||e.def.toLowerCase().includes(q)):[...GLOSSARY];
  glosRender(glosFiltered);
}

function glosJump(letter){
  document.getElementById('glosSearch').value='';
  glosFiltered=[...GLOSSARY];
  glosRender(glosFiltered);
  setTimeout(()=>{const el=document.getElementById('glos-'+letter);if(el)el.scrollIntoView({behavior:'smooth',block:'start'});},80);
}

function glosRender(entries){
  const container=document.getElementById('glosContent');
  if(!container) return;
  if(!entries.length){container.innerHTML='<div class="glos-empty">🔍 Aucun terme trouvé</div>';return;}
  const groups={};
  entries.forEach(e=>{const l=e.term[0].toUpperCase();if(!groups[l])groups[l]=[];groups[l].push(e);});
  container.innerHTML=Object.keys(groups).sort().map(letter=>`
    <div class="glos-section">
      <div class="glos-letter-hdr" id="glos-${letter}">${letter}</div>
      ${groups[letter].map((e,i)=>`
        <div class="glos-entry">
          <div class="glos-entry-hdr" onclick="glosToggle(this)">
            <div class="glos-term">${e.term}</div>
            <span class="glos-tag ${e.tag}">${e.tag==='std'?'STD':e.tag.toUpperCase()}</span>
            <span style="font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--muted);flex:1;text-align:right;padding-right:4px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${e.en}</span>
            <div class="glos-arr">›</div>
          </div>
          <div class="glos-def">${e.def}</div>
        </div>`).join('')}
    </div>`).join('');
}

function glosToggle(hdr){
  const def=hdr.nextElementSibling;
  const arr=hdr.querySelector('.glos-arr');
  if(!def) return;
  const open=def.classList.contains('open');
  def.classList.toggle('open',!open);
  if(arr)arr.classList.toggle('open',!open);
}

// Init on load
document.addEventListener('DOMContentLoaded',()=>{
  glosInit();
});
</script>

</html>
